<div id=toc></div>

# Table of Contents

- [世界模型](#direction-world_model) [Total: 15]
  - [具身世界模型与规划控制](#direction-world_model-embodied_world_model) [Total: 12]
  - [可交互与物理世界建模](#direction-world_model-interactive_physical_world) [Total: 2]
  - [视频世界模型](#direction-world_model-video_world_model) [Total: 1]
- [多模态生成](#direction-multimodal_generation) [Total: 26]
  - [3D 生成与重建](#direction-multimodal_generation-three_d_generation) [Total: 5]
  - [4D 与动态场景生成](#direction-multimodal_generation-dynamic_4d_generation) [Total: 3]
  - [图像与视频编辑](#direction-multimodal_generation-visual_editing) [Total: 3]
  - [文生图与图像扩散](#direction-multimodal_generation-text_to_image_diffusion) [Total: 1]
  - [文生视频与视频扩散](#direction-multimodal_generation-text_to_video_diffusion) [Total: 2]
  - [生成模型加速与高效化](#direction-multimodal_generation-efficient_generation) [Total: 4]
  - [生成模型对齐与奖励](#direction-multimodal_generation-generation_alignment) [Total: 3]
  - [生成模型评测与数据集](#direction-multimodal_generation-generation_benchmark) [Total: 3]
  - [自回归视觉生成](#direction-multimodal_generation-autoregressive_visual_generation) [Total: 1]
  - [音频与多模态联合生成](#direction-multimodal_generation-audio_av_generation) [Total: 1]
- [多模态理解与生成](#direction-multimodal_understanding_generation) [Total: 31]
  - [多模态幻觉与可靠性](#direction-multimodal_understanding_generation-multimodal_hallucination) [Total: 1]
  - [多模态强化学习与对齐](#direction-multimodal_understanding_generation-multimodal_rl_alignment) [Total: 2]
  - [多模态推理与思维链](#direction-multimodal_understanding_generation-multimodal_reasoning_cot) [Total: 5]
  - [多模态检索](#direction-multimodal_understanding_generation-multimodal_retrieval) [Total: 1]
  - [多模态评测与基准](#direction-multimodal_understanding_generation-multimodal_benchmark) [Total: 8]
  - [多模态高效化与轻量化](#direction-multimodal_understanding_generation-efficient_multimodal) [Total: 4]
  - [空间与 3D 多模态推理](#direction-multimodal_understanding_generation-spatial_3d_reasoning) [Total: 2]
  - [视觉语言模型与 MLLM](#direction-multimodal_understanding_generation-vlm_mllm_general) [Total: 3]
  - [视觉问答与文档理解](#direction-multimodal_understanding_generation-multimodal_vqa_doc) [Total: 4]
  - [视频理解与时序推理](#direction-multimodal_understanding_generation-video_understanding_temporal) [Total: 1]


<div id='direction-world_model'></div>

# 世界模型 [[Back]](#toc)



<div id='direction-world_model-embodied_world_model'></div>

## 具身世界模型与规划控制 [[Back]](#toc)

面向机器人、具身智能、动作规划和控制的世界模型。

### [1] [GaitSpan: Growing Humanoid Locomotion from Walking to Running](https://arxiv.org/abs/2607.12114)
*Kwan-Yee Lin, Zilin Wang, Janelle J. Liu, Stella X.Yu*

Main category: cs.AI, cs.CV, cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 值得关注 (65.8) - 未入选精读

TL;DR: GaitSpan 提出一种框架，将预训练的行走策略扩展为连续速度指令下覆盖行走、慢跑和跑步的人形机器人单一策略，无需重新学习或依赖多专家/动作剪辑。


<details>
  <summary>Details</summary>
Motivation: 现有方法获得步态多样性需要预设步态调度、模仿动作片段、训练多个专家或蒸馏技能，灵活性有限，无法在连续速度指令、不同地形和形态间泛化。本文希望从已有的行走策略出发，自然地生长出更快的运动能力。

Method: GaitSpan 包含三个模块：1) 节奏生成（Rhythm Generation）——用多个内部时钟调制冻结的行走策略，学习指令条件化的规范动作组合；2) 步幅塑形（Stride Shaping）——基于弹簧-负载倒摆（SLIP）动力学的物理目标奖励动态运动模式；3) 残差适应（Residual Adaptation）——捕捉前两者未覆盖的运动细节。

Result: GaitSpan 首次实现单一指令条件化的人形机器人策略覆盖行走、慢跑和跑步的连续速度范围，可跨形态迁移，零样本部署到未见过的 sim-to-sim 和真实地形。相比多专家或人类模仿基线，学习更快且步态性能更强。

Conclusion: GaitSpan 证明了从基础行走策略通过节奏生成、步幅塑形和残差适应可以高效扩展出更丰富的运动能力，为人形机器人的连续速度控制和跨形态泛化提供了新范式。

Classification reason: 该论文研究人形机器人的步态扩展（从行走到跑步），属于具身智能中的运动控制与策略学习，与 [CANONICAL] 具身世界模型与规划控制 高度匹配。

Importance reason: Strong world model and neural simulator alignment, but lacks direct connection to user's priority subtopics like continuous language models or video generation. Signals: world model, humanoid locomotion, continuous speed command, zero-shot transfer, sim-to-real

Abstract: A humanoid that can walk should not relearn locomotion from scratch to jog or run. Yet current approaches often obtain gait diversity by prescribing gait schedules, imitating motion clips, training experts to switch between or distilling skills into one policy. These strategies can produce impressive behaviors, but offer limited flexibility across continuous speed commands, terrains, and morphologies. We study skill growth with GaitSpan, a framework that expands a pretrained, basic walking policy into faster locomotion. It treats walking as a seed skill: reusable motor structure for balance, support, body coordination, and contact transition that can be regenerated at new rhythms, extended into longer/higher strides, and corrected by residual adaptation. This expansion has three aspects: 1) rhythm generation, which modulates the frozen walking policy with multiple internal clocks and learns command-conditioned combinations of the resulting canonical actions; 2) stride shaping, which rewards dynamic locomotion patterns appropriate for higher commanded speeds using a physically grounded objective inspired by spring-loaded inverted pendulum dynamics; and 3) residual adaptation, which captures motion details not accounted for by rhythm generation or stride shaping. GaitSpan is the first to deliver a single command-conditioned humanoid policy that spans walking, jogging, and running-like regimes covering a continuous speed range, transfers across morphologies, and deploys zero-shot on unseen sim-to-sim, and real-world terrains. Compared with baselines either trained with multi-experts or imitation from humans, it learns faster and achieves stronger gait performance.

</details>


### [2] [Reducing Temporal Redundancy for Efficient Vision-Language-Action Inference](https://arxiv.org/abs/2607.12287)
*Yuzhou Wu, Yuxin Zheng, Muchun Niu, Yishan Yang, Tianhao Liu, hanwen kang, Jiajian Jing, Linfeng Zhang, Chuan Wen*

Main category: cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 值得关注 (77.61) - 未入选精读

TL;DR: 本文提出一种系统级加速策略，通过增量式视觉编码（仅更新动态区域token）和压缩扩散采样为2步调度，将VLA模型推理速度提升2倍以上，同时在Libero等基准上保持高达98%的成功率。


<details>
  <summary>Details</summary>
Motivation: VLA模型在机器人操作中表现出强大的泛化能力，但其高推理延迟限制了实时部署。现有VLA管线中存在两类时序冗余：连续相似帧的重复视觉编码，以及扩散策略中的多步迭代采样。

Method: 感知端：仅增量更新动态场景区域的token，而非重新编码整帧图像。策略端：通过面向效率的训练将扩散采样压缩为紧凑的2步调度，同时保持动作精度。

Result: 在Libero、RobotWin和真实机器人平台上实现2倍以上加速，在通用操作基准上达到高达98%的成功率。

Conclusion: 通过减少感知和动作生成两端的时序冗余，可以显著降低VLA模型推理延迟，为机器人实时部署提供有效方案。

Classification reason: 该论文提出面向机器人操作的VLA模型推理加速方法，属于具身智能中的动作规划与控制，与[具身世界模型与规划控制]方向高度匹配。

Importance reason: Directly addresses model efficiency for VLA world models, a high-priority subtopic, with practical speedup. Signals: efficient inference, world model, VLA acceleration, incremental visual encoding, compressed diffusion sampling

Abstract: Vision-Language-Action (VLA) models exhibit strong generalization for robotic manipulation, yet their high inference latency limits real time deployment. We identify two primary sources of temporal redundancy in existing VLA pipelines: repeated visual encoding of highly similar consecutive frames and multi step iterative sampling in diffusion based policies. To address this, we propose a system level acceleration strategy that reduces computation in both perception and action generation. On the perception side, we incrementally update only tokens corresponding to dynamic scene regions instead of re-encoding entire frames. On the policy side, we compress diffusion sampling into a compact 2-step schedule through efficiency oriented training while preserving action precision. Experiments on Libero, RobotWin, and Real Robot Platforms demonstrate over 2 times speedup while maintaining high performance, achieving up to 98% success rate on general manipulation benchmarks. Our codes will be released on Github.

</details>


### [3] [VistaVLA: Geometry- and Semantic-Aware 3D Gaussian-Grounded VLA for Robotic Manipulation](https://arxiv.org/abs/2607.12356)
*Mohan Liu, Zhihao Gu, Xuanyu Chen, Haitian Zhang, Kaimin Mao, Yan Wu, Wei-Yun Yau, Lin Wang*

Main category: cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 高优先级 (85.44) - 未入选精读

TL;DR: VistaVLA提出一种两阶段框架，利用3D高斯原语构建几何与语义感知的3D认知表征，并通过Merge-then-Query（MtQ）机制将密集高斯原语压缩为紧凑上下文token，用于VLA策略学习，在仿真和真实机器人任务中显著提升了操作成功率。


<details>
  <summary>Details</summary>
Motivation: 现有VLA模型缺乏显式的场景级3D表征，难以推理空间布局和几何约束；而引入深度图或点云的方法仅捕获低级结构，缺乏3D空间中的高层语义。受人类认知中3D语义认知地图的启发，作者希望构建一种融合空间布局与语义上下文的3D认知表征，实现持久、视角不变的推理。

Method: VistaVLA采用两阶段框架：第一阶段将多视图视觉-语言特征提升到3D高斯原语中，形成几何锚定的语义token，对齐视图一致的空间定位与2D视觉特征空间；第二阶段引入Merge-then-Query（MtQ）token摘要机制，将密集高斯原语压缩为高度紧凑的空间信息token（实现99%的token缩减），同时保留与动作相关的3D布局和语义上下文，用于VLA策略学习。

Result: 在仿真和真实环境中均验证了有效性。真实场景中，VistaVLA在7个任务上成功率提升22.8%，在具有挑战性的分布外任务上相比VLA-Adapter基线提升30.0%。

Conclusion: VistaVLA通过构建几何与语义感知的3D高斯认知表征，有效提升了VLA模型在机器人操作任务中的空间推理能力和泛化性能，验证了3D语义认知地图在具身智能中的价值。

Classification reason: 该论文提出VistaVLA，一种面向机器人操作的视觉-语言-动作(VLA)模型，通过3D高斯原语构建几何与语义感知的3D认知表征，用于机器人策略学习。这直接属于世界模型方向下的具身世界模型与规划控制（embodied_world_model）这一规范子主题。同时，其VLA架构也涉及多模态理解与生成方向下的视觉语言模型（vlm_mllm_general），但核心贡献在于具身智能中的3D场景表征与策略学习。

Importance reason: Strong world model and 3D representation for robotics, but outside core priority subtopics. Signals: 3D Gaussian primitives, geometry-semantic 3D representation, VLA policy learning, token compression (MtQ), real-world manipulation success

Abstract: Vision-Language-Action (VLA) models have emerged as a powerful end-to-end paradigm for robotic manipulation by mapping language instructions and 2D visual inputs directly to actions. However, these models lack an explicit, scene-level 3D representation, limiting their ability to reason over spatial layouts and geometric constraints. While recent efforts incorporate explicit 3D cues, such as depth maps or point clouds, to improve geometric awareness, they primarily capture low-level structures and lack high-level semantic grounding in 3D space. In human cognition, interaction with the physical world relies on a 3D semantic cognitive map - an internal mental model that integrates spatial layouts with semantic context to enable persistent, viewpoint-invariant reasoning. In light of this, we present VistaVLA, a novel two-stage framework that constructs a geometry- and semantics-aware 3D cognitive representation from 3D Gaussian primitives and grounds it as compact context tokens for VLA policy learning. Specifically, VistaVLA lifts multi-view vision-language features into 3D Gaussian primitives, forming geometry-anchored semantic tokens that align view-consistent spatial grounding with 2D visual feature spaces. To make this 3D representation computationally tractable for effective VLA control, we introduce Merge-then-Query (MtQ), a token summarization mechanism. MtQ compresses dense Gaussian primitives into a highly compact set of spatially informative tokens, achieving a 99% token reduction while preserving action-relevant 3D layouts and semantic context. Extensive evaluations in both simulated and real-world environments demonstrate the effectiveness of VistaVLA. Notably, in real-world scenarios, VistaVLA improves success rates by 22.8% across seven real-world tasks and by 30.0% over the VLA-Adapter baseline on challenging out-of-distribution tasks.

</details>


### [4] [Model-Based Diffusion Optimal Control for Multi-Robot Motion Planning](https://arxiv.org/abs/2607.12423)
*Zhilin He, Yorai Shaoul, Jiaoyang Li*

Main category: cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 普通相关 (56.64) - 未入选精读

TL;DR: 提出 MDOC，一种基于模型的扩散最优控制方法，用于多机器人运动规划。该方法结合已知动力学模型与控制障碍函数约束投影，无需演示数据即可生成动态可行、无碰撞的轨迹，并通过冲突搜索自然扩展到多机器人场景。


<details>
  <summary>Details</summary>
Motivation: 多机器人运动规划面临联合轨迹空间组合爆炸、动态可行性与硬安全约束难以保证等挑战。现有基于扩散模型的方法依赖大量演示数据，且难以在采样过程中严格施加动力学和安全约束。

Method: MDOC 将轨迹规划建模为概率推断问题，使用扩散模型作为规划器。核心创新在于：(1) 结合已知动力学模型进行基于模型的扩散采样；(2) 利用控制障碍函数（CBF）约束投影确保轨迹安全；(3) 通过基于冲突的搜索（CBS）将安全机制自然扩展到多机器人场景。

Result: 在仿真实验中，MDOC 在采样效率、几何平滑度和成功率上持续优于代表性基线规划器，同时减少了计算时间并生成了无碰撞轨迹。

Conclusion: MDOC 证明了无需演示数据、仅依靠已知动力学模型和 CBF 约束即可实现高效的多机器人运动规划，为基于扩散的规划器在安全关键型多机器人系统中的应用提供了新思路。

Classification reason: 该论文研究多机器人运动规划，使用基于模型的控制障碍函数约束的扩散规划器生成无碰撞轨迹，属于机器人运动规划与控制范畴，与 [CANONICAL] 具身世界模型与规划控制 高度匹配。

Importance reason: Strong in robotics planning but lacks direct connection to user's priority subtopics like continuous language models or video generation. Signals: Multi-robot motion planning, diffusion optimal control, model-based planning, control barrier functions, conflict-based search

Abstract: Multi-Robot Motion Planning in continuous environments, where robots must generate dynamically feasible, collision-free trajectories, is challenging due to the combinatorial growth of the joint trajectory space and the difficulty of enforcing dynamic feasibility and hard safety constraints. Recent approaches recast trajectory planning as probabilistic inference, sampling from a posterior over trajectories using diffusion models whose score functions are learned from demonstration data. While showing promising performance, these approaches are limited: they often rely on sizable demonstration datasets and struggle to rigorously enforce dynamics and hard safety constraints during sampling. To this end, we introduce Model-Based Diffusion Optimal Control (MDOC), a model-based diffusion planner that efficiently produces dynamically feasible trajectories without relying on data. Crucially, we show that MDOC's safety mechanism -- combining known dynamics models with Control Barrier Function-constrained projections -- naturally scales to multi-robot planning settings through Conflict-Based Search. Across simulation experiments, this integrated method consistently outperforms representative baseline planners in sample efficiency, geometric smoothness, and success rate, while reducing computation time and producing collision-free trajectories.

</details>


### [5] [Deployable Human Preference Alignment in Robotics: Learning Representative Rewards from Diverse Human Preferences](https://arxiv.org/abs/2607.12466)
*Taehyung Kim, Gwangmo Lee, Minjun Chang, Sunghyun Lim, Jongeun Choi*

Main category: cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 高优先级 (81.01) - 未入选精读

TL;DR: 提出 PREC 框架，通过偏好聚类学习代表性奖励模型，从多样用户偏好中学习紧凑的机器人策略集，解决稀疏噪声偏好下的用户对齐问题。


<details>
  <summary>Details</summary>
Motivation: 现有机器人策略对齐方法面临两难：逐用户对齐策略需要大量偏好标签且易受噪声影响，部署前验证困难；单一共享策略无法捕捉用户异质性偏好，容易忽视少数群体。需要一种既能处理稀疏噪声偏好、又能兼顾多样用户偏好的对齐方法。

Method: PREC 框架包含三个阶段：(1) 忽略偏好标签，跨用户聚合轨迹学习群体共享的轨迹编码器，缓解每用户数据稀疏问题并避免表示学习阶段的标签噪声；(2) 基于学到的表示，联合将用户分配到偏好一致的聚类，并为每个聚类学习一个代表性奖励模型；(3) 为每个聚类优化一个策略。聚类相似用户补偿了每用户标签数量有限的问题，同时维持可控的奖励模型数量以降低部署验证负担。

Result: 在多种模拟 locomotion 环境中，PREC 比基线方法更准确地将标注不同轨迹子集的用户分组到偏好一致的聚类中。在稀疏和噪声反馈下，PREC 训练的三种社会福利指标均优于现有单一共享策略对齐方法，甚至超过逐用户对齐方法。

Conclusion: PREC 通过偏好聚类有效解决了机器人策略部署中多样用户偏好对齐的挑战，在稀疏和噪声偏好场景下实现了更好的社会福利表现，为机器人策略的个性化部署提供了可行方案。

Classification reason: 该论文研究机器人策略与人类偏好对齐，属于具身智能/机器人领域的奖励建模与策略学习，与 [CANONICAL] 具身世界模型与规划控制 方向高度匹配。

Importance reason: Focuses on robotics preference alignment, not directly on the user's priority subtopics like continuous language models or video generation. Signals: Robotics, human preference alignment, reward clustering, sparse feedback, social welfare metrics

Abstract: Aligning robot policies with human preferences is essential for deployment to diverse end users. In per-user alignment approach, preference feedback is often sparse, so learning becomes unstable and vulnerable to human preference noise, and a growing number of individualized policies makes validation difficult before deployment. A single shared policy approach to user alignment avoids this cost but fails to capture heterogeneous preferences and often neglects minority preferences. To address these challenges, we introduce Preference-based REward Clustering (PREC), a novel framework that learns a compact set of policies from binary preference labels provided by diverse users. From a dataset of user trajectories and their preference labels, PREC first sets the labels aside and aggregates trajectories across users to learn a population-level shared trajectory encoder, alleviating limited per-user coverage and avoiding label noise during representation learning. Using this representation, PREC jointly assigns users to preference-coherent clusters and learns a representative reward model per cluster using preference labels, from which a policy is optimized for each cluster. Clustering similar users compensates for the limited number of labels available from each user and mitigates the effect of label noise. At the same time, maintaining a manageable number of reward models reduces the validation burden at deployment. Experiments across diverse simulated locomotion environments show that PREC groups users who label different trajectory subsets into preference-coherent clusters more accurately than baseline methods. Under sparse and noisy feedback, policies trained with PREC improve all three social welfare metrics over an existing single shared-policy user-alignment approach and even outperform per-user alignment approaches.

</details>


### [6] [Mind the Gap: Promises and Pitfalls of Hierarchical Planning in LeWorldModel](https://arxiv.org/abs/2607.12547)
*Niccolò Caselli, Salvatore Lo Sardo, Francesco Massafra, Ippokratis Pantelidis, Samuele Punzo, Sathya Kamesh Bhethanabhotla*

Main category: cs.AI, cs.RO, cs.LG

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 值得关注 (65.06) - 未入选精读

TL;DR: 本文提出 Hi-LeWM，在冻结的低层 LeWorldModel 上添加高层潜空间子目标规划。实验表明层次化结构并非自动提升性能：短程时单步高层规划最优，长程时高层动作空间与推理搜索分布不匹配。通过约束搜索（使用训练轨迹编码的宏动作）可恢复层次化优势，在 PushT 中程和长程任务上分别提升 11.3% 和 14.7%。


<details>
  <summary>Details</summary>
Motivation: 现有 LeWorldModel 在长程目标条件控制任务中性能有限，而时间层次化（temporal hierarchy）有望通过高层抽象规划提升长程任务表现，但其在紧凑世界模型上的有效性尚未被系统研究。

Method: 提出 Hi-LeWM，冻结预训练的低层 LeWM，在其潜空间上添加高层规划器。高层规划器在潜空间中生成子目标（macro-actions），通过搜索选择最优子目标序列，低层控制器执行子目标。实验对比了不同高层规划视野（horizon）、搜索策略以及是否使用真实未来潜子目标作为上界。

Result: 层次化并非自动有效：短程时单步高层规划最优；长程时高层动作空间与推理搜索分布不匹配导致性能下降。使用真实未来潜子目标时低层控制器表现良好，说明高层子目标生成是瓶颈。通过约束搜索（使用训练轨迹编码的宏动作）并调整子目标执行时序，在 PushT 中程和长程上分别提升 11.3% 和 14.7%。

Conclusion: 时间抽象可以提升紧凑冻结的 LeWorldModel 性能，但前提是高层搜索必须与低层控制器兼容。高层子目标生成是层次化规划的主要瓶颈，约束搜索策略是缓解该问题的有效手段。

Classification reason: 论文研究 LeWorldModel 在长程目标条件控制中的层次化规划，属于具身智能中的世界模型与规划控制方向，与 [CANONICAL] 具身世界模型与规划控制 高度匹配。

Importance reason: Directly addresses world model planning and hierarchical control, but lacks direct connection to the user's priority subtopics like continuous language models or video generation. Signals: world model, hierarchical planning, latent subgoal, LeWorldModel, long-horizon control

Abstract: We investigate whether temporal hierarchy can improve LeWorldModel on long-horizon goal-conditioned control. We introduce Hi-LeWM, an extension that freezes the pretrained low-level LeWM and adds high-level planning over latent subgoals. We evaluate Hi-LeWM on PushT and Cube across increasing goal offsets. Hierarchy does not automatically improve performance: at short horizons, the best configuration uses a one-step high-level horizon, while longer horizons reveal a mismatch between the learned high-level action space and the inference-time search distribution. Experiments with true future latent subgoals show that the frozen low-level controller can execute well-aligned intermediate targets, indicating that high-level subgoal generation is the main bottleneck. Unconstrained search can select latent macro-actions that appear favorable under the learned model but produce poor control targets. Constraining search around macro-actions encoded from training trajectories, with appropriate subgoal execution timing, recovers useful hierarchical regimes, improving over flat LeWM by +11.3 percentage points at medium-range horizons and +14.7 percentage points at the longest PushT horizon. Overall, temporal abstraction can benefit compact frozen LeWM, but only when high-level search remains compatible with the low-level controller

</details>


### [7] [TrustVLA: Mechanism-Guided Inference-Time Defense Against Vision-Language-Action Backdoors](https://arxiv.org/abs/2607.12571)
*Pinhan Fu, Xianda Guo, Xuetao Li, Wenke Huang, Ruilin Wang, Weiheng Zhao, Wei Sui, Mang Ye*

Main category: cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 普通相关 (56.64) - 未入选精读

TL;DR: TrustVLA 提出了一种机制引导的推理时防御方法，针对 VLA 模型中的视觉触发后门攻击。通过识别后门攻击的"紧凑因果足迹"（attention-seeded、空间紧凑、因果可掩码），利用 Dirichlet 证据框架监测每 token 每层的认知不确定性，实现检测、定位和恢复，无需重新训练即可防御 BadVLA 和 INFUSE 等攻击。


<details>
  <summary>Details</summary>
Motivation: VLA 模型在机器人部署中无法被终端用户审计，被投毒的 VLA 模型在干净观测下表现正常，但微小的视觉触发器就能在故障被观察到之前重定向长程机器人策略。现有视觉或语言防御方法很少解释触发后的 VLA 表征是什么样，也无法在不重新训练的情况下恢复行为。因此需要一种可解释的、无需重训练的推理时防御方法。

Method: TrustVLA 首先识别出后门攻击的共性机制——"紧凑因果足迹"（compact causal footprint）：一个 attention-seeded、空间紧凑且因果可掩码的小视觉区域。基于此，方法将可信分类中的 Dirichlet 证据框架适配到 VLA 策略中，监测每 token 每层的认知不确定性（epistemic uncertainty）。具体包括三步：(i) 通过异常证据演化检测攻击；(ii) 通过反事实机制分数下降定位紧凑支持区域；(iii) 通过局部修复恢复观测。仅需少量干净校准集。

Result: 在 OpenVLA/LIBERO 和 π0.5 迁移评估中，TrustVLA 显著降低了攻击成功率，同时保持了干净任务性能，验证了其作为无需重训练的机制引导防御方法的有效性。

Conclusion: TrustVLA 通过揭示 VLA 后门攻击的共性内部机制（紧凑因果足迹），提出了一种机制引导的推理时防御框架，能够在不重新训练的情况下检测、定位和恢复视觉触发的后门攻击，为 VLA 模型的安全部署提供了实用方案。

Classification reason: 该论文研究 VLA（视觉-语言-动作）模型的后门攻击与防御，VLA 模型是面向机器人具身策略的模型，属于具身世界模型与规划控制方向。论文关注机器人策略的安全性与鲁棒性，与 embodied_world_model 这一 [CANONICAL] 子主题高度匹配。

Importance reason: Focuses on VLA backdoor defense, not directly on user's priority subtopics like continuous language models or video generation. Signals: VLA backdoor defense, inference-time defense, compact causal footprint, Dirichlet evidence, robotics security

Abstract: Vision-Language-Action (VLA) models are deployed through pipelines that end users cannot audit, and a poisoned VLA can behave normally on clean observations while a small visual trigger redirects a long-horizon robot policy before any failure becomes observable. Existing vision or language defenses rarely explain what a triggered VLA representation looks like or how to recover behavior without retraining. We study this gap through two independently proposed VLA attacks from groups with distinct injection strategies, BadVLA and INFUSE; the latter persists after downstream clean adaptation. Across the evaluated poisoned models, we identify a recurring internal mechanism: a \emph{compact causal footprint}, namely a small visual support that is attention-seeded, spatially compact, and \emph{causal} in a precise sense -- masking it returns a clean-calibrated evidence-evolution score to the normal operating region. This footprint motivates TrustVLA, a mechanism-guided inference-time defense that adapts the Dirichlet evidence framework from trusted classification to monitor per-token, per-layer epistemic uncertainty in VLA policies. With only a small clean calibration set, TrustVLA (i)~detects abnormal evidence evolution, (ii)~localizes the compact support by counterfactual mechanism-score drop, and (iii)~recovers the observation by localized inpainting. Across OpenVLA/LIBERO and $\pi_{0.5}$ transfer evaluations, TrustVLA reduces attack success while preserving clean-task performance, providing a retraining-free, mechanism-guided defense for visual-triggered VLA backdoors.

</details>


### [8] [Instance-Enriched Semantic Maps for Visual Language Navigation](https://arxiv.org/abs/2607.12630)
*Jiho Hong, Eunae Kang, Sanghyun Kim, Young-Sik Shin*

Main category: cs.CV, cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 普通相关 (59.89) - 未入选精读

TL;DR: 提出实例增强语义地图（Instance-Enriched Semantic Maps），通过开放词汇全景分割构建2.5D实例级语义地图，结合LLM进行目标选择与查询路由，在VLN任务中显著提升物体检索和导航成功率，同时存储效率相比3D场景图降低约96%。


<details>
  <summary>Details</summary>
Motivation: 现有VLN方法构建的语义空间地图缺乏实例级物体细节，且对多样化用户查询的鲁棒性不足，限制了在复杂室内环境中的可靠导航。

Method: (1) 通过开放词汇全景分割从RGB-D观测构建2.5D实例级语义地图，保留垂直区分并捕获小物体，存储丰富的语义属性和自然语言描述；(2) 基于LLM的目标选择机制，动态路由查询到类型专精的专家模块，通过分数级融合实现一致的目标选择；(3) 采用2.5D表示实现存储高效性。

Result: 2.5D表示相比3D基线在预测归一化AUC上提升超过27%；导航实验中物体检索提升超过17%，导航成功率提升超过23%；存储相比3D场景图方法减少约96%。

Conclusion: 实例增强语义地图通过实例级2.5D语义建图与LLM驱动的查询处理，有效提升了VLN在复杂室内环境中的导航性能与鲁棒性，同时大幅降低了存储开销。

Classification reason: 该论文研究视觉语言导航（VLN），属于具身智能中的导航与规划任务，与 [CANONICAL] 具身世界模型与规划控制 高度匹配。

Importance reason: Solid VLN work but lacks direct connection to user's priority subtopics like continuous language models or autoregressive generation. Signals: VLN, instance-level semantic map, open-vocabulary panoptic segmentation, LLM-based query routing, 2.5D representation

Abstract: Visual Language Navigation (VLN) aims to enable an embodied agent to navigate complex environments by following natural language instructions. Recent approaches build semantic spatial maps and leverage Large Language Models (LLMs) for reasoning and decision making. Despite these advances, existing systems lack instance-level object detail and robustness to diverse user queries, limiting reliable navigation in complex indoor environments. To address these limitations, we propose Instance-Enriched Semantic Maps, a unified framework with three key contributions: (1) Instance-level two-and-a-half-dimensional (2.5D) rich information mapping that constructs maps from color and depth observations via open-vocabulary panoptic segmentation, preserving vertical distinctions and capturing small objects, while storing diverse semantic attributes and natural language captions enriched with room-level context. (2) Robust query processing via LLM-based target selection, which dynamically routes queries across type-specialized experts and integrates their outputs through score-level fusion, enabling consistent goal selection across diverse query formulations. (3) Storage-efficient semantic representation that achieves approximately 96% reduction compared to three-dimensional (3D) scene-graph approaches while preserving sufficient spatial information for navigation. The proposed 2.5D representation outperforms the 3D baseline by over 27% in prediction-normalized Area Under the Curve (AUC). In navigation experiments, our method achieves over 17% improvement in object retrieval and over 23% in navigation success compared to the baseline across diverse query types. The project page is available at .

</details>


### [9] [Jetson-PI: Towards Onboard Real-Time Robot Control via Foresight-Aligned Asynchronous Inference](https://arxiv.org/abs/2607.12659)
*Zebin Yang, Qi Wang, Yunhe Wang, Xiurui Guo, Bo Yu, Shaoshan Liu, Jiafeng Xu, Hao Dong, Meng Li*

Main category: cs.AI, cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 高优先级 (80.01) - 未入选精读

TL;DR: Jetson-PI提出了一种面向Jetson Orin等低功耗设备的VLA模型高效部署方法，通过前瞻对齐异步校正（Foresight-Aligned Asynchronous Correction）解决异步推理中的感知-执行错位和反应时间长的问题，在LIBERO基准上控制频率提升8.66倍，成功率提升14.8%。


<details>
  <summary>Details</summary>
Motivation: VLA模型在具身任务中表现优异，但部署在Jetson Orin等低功耗设备上时计算复杂度过高，导致推理延迟大、控制频率低。异步推理虽可部分掩盖延迟，但会引入感知-执行错位和反应时间长两个关键问题。

Method: 提出Jetson-PI方法：(1) 训练轻量级未来校正模块，根据已提交的动作预测未来环境表征，使动作专家能直接从未来时间步预测动作；(2) 引入基于置信度的调度优化，自适应平衡VLM和动作专家的调用；(3) 系统级加速包括CUDA图复用、GPU驻留中间缓冲和流展开。

Result: 在NVIDIA Jetson Orin上，相比原生PyTorch和，控制频率分别提升8.66倍和5.41倍；在LIBERO基准上平均成功率比VLASH高14.8%。

Conclusion: Jetson-PI通过前瞻对齐异步校正有效解决了VLA模型在低功耗设备上的部署难题，实现了实时机器人控制，为边缘端具身智能应用提供了可行方案。

Classification reason: 该论文研究VLA模型在低功耗设备上的高效部署，涉及具身智能中的动作规划与控制，属于世界模型方向下的[具身世界模型与规划控制]规范子主题。

Importance reason: Directly addresses efficient inference for world models on low-power devices, aligning with model efficiency and world model priorities. Signals: efficient inference, world model, VLA model deployment, asynchronous inference, Jetson Orin

Abstract: Vision-Language-Action (VLA) models have achieved impressive performance on diverse embodied tasks. However, deploying VLA models on low-power onboard devices, such as the Jetson Orin, remains challenging due to their high computational complexity, which leads to substantial inference latency and low control frequency. Asynchronous inference can partially mask this latency by parallelizing action execution and subsequent inference, but it introduces two critical issues: perception-execution misalignment and long reaction time. In this paper, we propose Jetson-PI, a method for efficient VLA deployment on onboard devices via Foresight-Aligned Asynchronous Correction. To address misalignment, we train a lightweight future correction module that predicts future environment representation conditioned on committed actions, enabling the action expert to directly predict actions from the future time step. To reduce reaction time, we introduce confidence-based scheduling optimization that adaptively balances VLM and action expert invocations, complemented by system-level accelerations including CUDA graph reuse, GPU-resident intermediate buffering, and flow unrolling. Extensive experiments demonstrate that Jetson-PI achieves 8.66x and 5.41x improvements in control frequency compared with naive PyTorch and on NVIDIA Jetson Orin, while outperforming VLASH by 14.8\% in average success rate on the LIBERO benchmark. The code of our asynchronous algorithm is available on , and our efficient -based inference engine is available on .

</details>


### [10] [Directional Constraints for Efficient Exploration in Safe Reinforcement Learning](https://arxiv.org/abs/2607.12784)
*Paolo Magliano, Puze Liu, Jan Peters, Davide Tateo, Raffaello Camoriano*

Main category: cs.RO, cs.LG

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 普通相关 (56.39) - 未入选精读

TL;DR: 本文提出ATACOM-DC（方向性约束），扩展了ATACOM安全层框架，通过区分接近和远离约束边界的动作来选择性激活约束，从而在安全强化学习中改善安全-性能权衡，在多种机器人控制任务上验证了有效性。


<details>
  <summary>Details</summary>
Motivation: 安全强化学习在机器人真实部署中至关重要，但现有方法在约束下学习会降低学习速度并可能导致次优任务性能。作者希望在不牺牲任务性能的前提下保证安全性，解决安全约束与学习效率之间的权衡问题。

Method: 在ATACOM安全层框架基础上，引入方向性约束（Directional Constraints），区分动作是接近还是远离约束边界，仅在必要时激活约束强制执行，从而减少不必要的约束干预，提升安全-性能权衡。

Result: 在多种具有挑战性的机器人控制仿真任务上评估了约束违反成本和任务性能，ATACOM-DC相比基线方法在保持安全性的同时显著提升了任务表现。

Conclusion: 方向性约束是一种有效的安全强化学习方法，能够在保证安全性的前提下提升学习效率和任务性能，为机器人安全部署提供了实用方案。

Classification reason: 该论文提出ATACOM-DC方法，在安全强化学习框架中引入方向性约束，用于机器人控制任务。这属于具身智能中机器人规划与控制的核心问题，与[具身世界模型与规划控制]子主题高度匹配。

Importance reason: Focuses on safe RL for robotics, not aligned with user's priority on continuous multimodal models or video generation. Signals: Safe RL, directional constraints, ATACOM extension, robotics control, safety-performance trade-off

Abstract: Reinforcement Learning has revolutionized the landscape of robotic research, allowing robust learning of complex robotic skills in simulation. However, real-world deployment in open-ended environments requires strong safety guarantees to prevent dangerous or harmful behaviors. Safe Reinforcement Learning methods address this requirement by enforcing safety constraints. Nevertheless, learning under constraints often reduces learning speed and could lead to suboptimal task performance, as the agent must solve a more complex constrained optimization problem compared to unconstrained settings. To tackle this issue, in this work, we propose an extension of the ATACOM framework, a state-of-the-art reliable safety layer that can be integrated with existing Reinforcement Learning algorithms to enforce constraints derived from prior knowledge of the system or learned directly from data. Our proposed method, named ATACOM Directional Constraints (ATACOM-DC), significantly improves the safety-performance trade-off by introducing directional constraints that distinguish between actions approaching and moving away from constraint boundaries, activating constraint enforcement only when necessary. We evaluate our method across a range of challenging robotic control tasks in simulation, analyzing both constraint-violation costs and achieved task performance. Code and additional material at .

</details>


### [11] [ChunkFlow: Towards Continuity-Consistent Chunked Policy Learning](https://arxiv.org/abs/2607.12992)
*Zhao Yang, Yinan Shi, Mingyuan Yao, Wenyao Xue, Yawei Jueluo, Longjun Liu*

Main category: cs.RO

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 普通相关 (59.89) - 未入选精读

TL;DR: ChunkFlow提出了一种面向VLA模型chunked action策略的连续性一致训练与执行框架，通过分区设计、重叠混合和连续性损失函数解决chunk边界预测不一致导致的抖动问题，在CALVIN、LIBERO和真实机器人上提升了成功率与稳定性的权衡。


<details>
  <summary>Details</summary>
Motivation: VLA模型为满足实时性要求采用chunked action head，但相邻chunk重叠区域的预测不一致导致边界抖动，降低时序连贯性和任务成功率。现有推理时混合方法仅重新加权不一致的预测而不修正底层错误，在有偏或噪声历史下会导致残差累积。

Method: ChunkFlow将每个chunk划分为冻结区、可编辑区和未来区，在执行时使用确定性重叠混合，训练时对原始预测施加seam损失以及一阶和二阶连续性损失。引入历史损坏和计划采样提高对执行历史错误的鲁棒性，并通过AWAC微调阶段在不移除结构正则化项的情况下适应策略。

Result: 在CALVIN、LIBERO和真实机器人实验上，ChunkFlow在保持低延迟推理的同时，实现了更好的成功率-稳定性权衡。理论分析表明，在温和平滑性假设下，预混合的seam差异随重叠增加而衰减。

Conclusion: ChunkFlow通过将chunk结构与边界执行对齐，有效缓解了chunked policy中的边界抖动问题，为实时机器人控制中VLA模型的部署提供了一种实用的训练-执行一体化框架。

Classification reason: 该论文研究VLA模型中的chunked action策略学习，解决边界抖动问题以提升机器人任务成功率，属于具身智能中的动作规划与控制，因此映射到[CANONICAL]具身世界模型与规划控制。

Importance reason: Addresses VLA model chunked action consistency, but is in robotics, not the user's priority subtopics. Signals: robotics, VLA model, chunked policy, continuity loss, boundary jitter

Abstract: Vision-language action (VLA) models increasingly adopt chunked action heads to satisfy real-time constraints; however, this introduces boundary jitter: overlapping regions between consecutive chunks often yield inconsistent predictions, degrading temporal coherence and the task success rate. Existing methods, such as inference-time blending, merely reweight mismatched proposals without correcting underlying errors, leading to residual accumulation under biased or noisy histories. We propose ChunkFlow, a seam-aware training-and-execution framework for chunked policies that aligns chunk structure with boundary execution. It partitions each chunk into frozen, editable, and future zones, applies deterministic overlap blending at execution, and trains raw predictions with seam and first- and second-order continuity losses. History corruption and scheduled sampling improve robustness to executed-history errors, while an AWAC fine-tuning stage adapts the policy without removing these structural regularizers. Under mild smoothness assumptions, pre-blending seam discrepancies provably decay with increasing overlap. Experiments on CALVIN, LIBERO, and real robots show an improved success-stability trade-off with low-latency inference. Project page: .

</details>


### [12] [TerraZero: Procedural Driving Simulation for Zero-Demonstration Self-Play at Scale](https://arxiv.org/abs/2607.13028)
*Zhouchonghao Wu, Akshay Rangesh, Weixin Li, Wei-Jer Chang, Zachary Lee, Tim Wang, Wei Zhan*

Main category: cs.AI, cs.RO, cs.LG

Direction: 世界模型

Subtopic: 具身世界模型与规划控制

Importance: 高优先级 (92.93) - 未入选精读

TL;DR: TerraZero 是一个程序化驾驶模拟器与自博弈训练栈，通过C引擎CPU模拟+GPU推理的零拷贝架构实现1.3M agent-steps/s的高效仿真，仅利用真实地图几何数据，通过随机化交通参与者、动力学、奖励等生成无限场景，纯强化学习从零训练策略，无需人类演示。该策略在InterPlan长尾基准上首次超越更大规模的学习型规划器，在val14上取得最佳碰撞与TTC分数，在Waymo Sim Agents上优于其他无演示方法。


<details>
  <summary>Details</summary>
Motivation: 训练鲁棒的自动驾驶智能体需要一个足够快的模拟器以支持大规模强化学习，足够真实以将行为锚定在真实地图结构中，且足够多样化以覆盖安全关键的长尾场景——而日志数据中很少包含这些场景。现有模拟器要么速度慢，要么缺乏异构智能体、多动力学模型和完整交通规则等保真度。

Method: TerraZero 包含一个可配置的C引擎，在CPU上运行仿真、在GPU上通过零拷贝路径执行策略推理，单服务器GPU可达1.3M agent-steps/s。它将日志数据仅作为真实地图几何来源，用随机化的基于规则的交通参与者、信号控制器填充地图，并随机化智能体动力学、奖励和尺寸，使每张地图产生无限场景。策略通过纯强化学习的自博弈配方从零训练，无人类演示和推理时的后备规划器。

Result: TerraZero 是首个在InterPlan长尾基准上排名第一的完全学习型策略，超越更大规模的学习型规划器；在常规驾驶val14上排名前列且是最安全的（最佳碰撞和TTC分数）；在Waymo Open Sim Agents真实性评测中优于其他无演示方法，与最强的参考锚定自博弈方法竞争力相当。同一框架同时支持汽车/卡车的驾驶策略和联合控制车辆/行人/自行车手的模拟智能体。

Conclusion: TerraZero 证明了纯强化学习结合程序化生成的无限驾驶场景可以训练出零样本泛化到不同城市和交通规则（如左侧行驶）的鲁棒自动驾驶策略，无需任何人类演示或后备规划器，在多个基准上达到领先水平。

Classification reason: 该论文提出TerraZero，一个程序化驾驶模拟器与自博弈训练框架，用于训练鲁棒的自动驾驶策略。这属于具身智能中的机器人/自动驾驶规划控制，与[具身世界模型与规划控制]（embodied_world_model）高度匹配。

Importance reason: Strongly aligns with world model and efficient inference priorities via a procedural driving simulator that enables zero-demonstration self-play at scale. Signals: world model, efficient inference, procedural simulation, zero-demonstration self-play, autonomous driving

Abstract: Training robust autonomous driving agents requires a simulator that is fast enough for reinforcement learning at scale, realistic enough to ground behavior in real-world map structure, and diverse enough to cover the safety-critical long tail that logged data rarely contains. We present TerraZero, a procedural driving simulator and self-play training stack. A configurable C engine runs simulation on the CPU and policy inference on the GPU over a zero-copy path, sustaining 1.3M agent-steps per second on a single server-grade GPU, far faster than existing object-level simulators, while keeping fidelity lighter single-agent systems omit: heterogeneous agents, multiple dynamics models, and full traffic-rule enforcement. TerraZero treats logged data only as a source of real-world map geometry, populating each map with randomized rule-based road users and signal controllers and randomizing agent dynamics, rewards, and sizes per episode, so a map yields an unbounded set of scenarios. Every reported policy trains from scratch by reinforcement learning alone on a compute-efficient self-play recipe across GPUs, with zero human demonstrations and no fallback planner at inference. Policies generalize zero-shot across cities and datasets, including emergent left-hand-traffic driving without explicit supervision. As an ego policy, TerraZero is the first fully learned policy to top the InterPlan long-tail benchmark, ahead of larger learned planners; on routine-driving val14 it ranks among the best approaches and is the safest, posting the best collision and time-to-collision scores. On Waymo Open Sim Agents realism the same recipe outperforms other demonstration-free methods and is competitive with the strongest reference-anchored self-play method. One stack serves both roles: driving policies across dynamics for cars and trucks, and sim agents that jointly control vehicles, pedestrians, and cyclists.

</details>


<div id='direction-world_model-interactive_physical_world'></div>

## 可交互与物理世界建模 [[Back]](#toc)

物理一致的世界模型、交互式模拟器、神经仿真器与环境动力学建模。

### [13] [The GEST-Engine: From Event Graphs to Synthetic Video. A Full Technical Report](https://arxiv.org/abs/2607.12231)
*Nicolae Cudlenco, Mihai Masala, Marius Leordeanu*

Main category: cs.CV

Direction: 世界模型

Subtopic: 可交互与物理世界建模

Importance: 高优先级 (90.9) - 未入选精读

TL;DR: GEST-Engine是一个从自然语言文本到全标注多演员视频的完整系统。其核心是显式世界模型——用形式化的时空事件图（GEST）表示世界状态，在商业游戏引擎中确定性执行。系统包含LLM驱动的文本到GEST生成、四阶段执行管线（图解析验证、实体与动作接地、时序编排、执行与捕获），输出RGB视频、深度图、实例分割、骨骼姿态、空间关系图、边界框、事件到帧映射及自然语言描述，零额外标注成本。


<details>
  <summary>Details</summary>
Motivation: 现有视频生成模型（如扩散模型）缺乏对物体恒存性、多演员协调和时序一致性的保证，且生成数据难以获得密集精确的标注。作者希望构建一个基于显式世界模型的系统，从语义规范出发确定性生成视频，保证时空一致性，同时零成本输出密集标注，为视频理解提供训练数据和评测基准。

Method: GEST-Engine包含：(1) 形式化表示——时空事件图（GEST），描述演员、物体、动作、事件时序关系；(2) 文本到GEST生成——LLM Director通过工具调用规划故事，经程序化状态后端验证确保可执行；(3) 四阶段执行管线：图解析与验证、实体与动作接地（映射到游戏引擎资源）、时序编排（Allen区间代数+Floyd-Warshall传递闭包）、执行与捕获；(4) 在商业游戏引擎（GTA V）中通过开源多人脚本框架确定性执行，单次模拟输出RGB、深度、分割、姿态、空间关系图、边界框、事件映射和文本描述。

Result: 系统能够从文本描述生成多演员视频，保证物体恒存性、多演员协调和时序一致性。所有标注（深度、分割、姿态、空间关系等）零成本自动生成。支持大规模并行渲染，可在多台虚拟机上进行语料库生产。生成的视频可用于训练数据、评测基准和视频理解诊断工具。

Conclusion: GEST-Engine展示了显式世界模型在视频生成与标注中的价值：通过形式化事件图+游戏引擎确定性执行，克服了隐式生成模型在时空一致性上的局限，同时以零边际成本提供密集精确的多模态标注。该系统为视频理解研究提供了高质量的训练数据和评测工具。

Classification reason: 该论文提出GEST-Engine，核心是一个显式的世界模型（Graph of Events in Space and Time），在游戏引擎中确定性执行，生成多演员视频及密集标注。这属于可交互与物理世界建模（interactive_physical_world）范畴，因为其维护了完整的可检查世界状态，并在可交互的游戏引擎环境中运行，属于物理世界模拟器/环境模型。

Importance reason: Strongly aligns with world model and video generation priorities via explicit, executable event graphs for synthetic video. Signals: explicit world model, text-to-video generation, zero-annotation multi-modal output, LLM-driven planning, temporal consistency

Abstract: We present the GEST-Engine, a complete system that goes from natural-language text to fully-annotated multi-actor video. At its core is an explicit world model: rather than encoding state as a learned latent, the engine maintains a complete, inspectable representation of the world (which actors exist, where they are, what they are doing, which objects they hold, and how events relate in time and space), expressed as a formal Graph of Events in Space and Time (GEST) and realized deterministically inside the open world of a commercial game engine driven through an open-source multiplayer scripting framework. GESTs are produced either procedurally or by an agentic text-to-GEST system in which an LLM Director plans a story through tool calls validated by a programmatic state backend, so every generated specification is executable by construction. A GEST then enters a four-stage execution pipeline: graph parsing and validation, entity and action grounding, temporal orchestration (Allen-style constraints resolved by Floyd-Warshall transitive closure), and execution and capture. In a single simulation pass the engine emits frame-aligned RGB video, dense per-pixel depth, instance segmentation, per-actor skeletal pose, per-frame pairwise spatial-relation graphs, 2D bounding boxes, event-to-frame temporal mappings, and natural-language descriptions, all at zero marginal annotation cost. We further describe an in-game world editor, runtime capability extraction, a text-generation pipeline, and a production system that renders corpora at scale across parallel virtual machines. Because every frame traces back to a semantic specification, the engine guarantees object permanence, multi-actor coordination, and temporal consistency by construction, making its output valuable as training data, evaluation benchmarks, and diagnostic tools for video understanding.

</details>


### [14] [From Observation to Insight: Mechanistic World Models and the Quest for Autonomous Discovery](https://arxiv.org/abs/2607.12474)
*Ingmar Posner, Anson Lei, Bernhard Schölkopf*

Main category: cs.AI

Direction: 世界模型

Subtopic: 可交互与物理世界建模

Importance: 高优先级 (90.19) - 未入选精读

TL;DR: 本文提出"机制世界模型"（Mechanistic World Models）这一新设计范式，主张将可复用的因果解释机制置于世界模型的核心，而非仅关注预测映射。论文从科学哲学出发，推导了实现自主科学发现所需的计算能力，并形式化了机制中心世界模型的架构，旨在推动AI从预测走向真正的科学发现。


<details>
  <summary>Details</summary>
Motivation: 当前AI在科学领域虽能实现高精度预测（如蛋白质折叠、天气预报），但预测不等于科学发现。科学理解依赖于揭示产生观测数据的可复用解释机制，而当代机器学习仍以预测映射而非解释性结构为核心组织方式。本文旨在弥合这一差距，提出一种以机制为中心的世界模型范式。

Method: 本文为概念性/理论性工作，从科学哲学中汲取洞见，推导出科学发现所需的计算能力，识别鼓励解释性知识涌现的设计原则和归纳压力，形式化机制中心世界模型的解剖结构，并展示机械可解释性、因果表征学习、方程发现和模块化架构等研究方向如何构成该范式的互补要素。

Result: 提出了Mechanistic World Models作为概念基础和计算蓝图，统一了多个分散研究方向（机械可解释性、因果表征学习、方程发现、模块化架构）的互补思想，为AI超越预测性预报走向自主科学发现提供了理论框架。

Conclusion: Mechanistic World Models为将AI从预测性预报推进到自主科学发现提供了概念基础和计算蓝图。该范式强调以可复用机制为中心组织知识，有望推动AI系统真正具备科学发现能力。

Classification reason: 该论文提出"Mechanistic World Models"（机制世界模型）这一新范式，强调世界模型应关注可复用的因果机制而非纯预测映射，属于世界模型方向中关于物理世界建模与可解释性机制的核心讨论，与[可交互与物理世界建模]子主题高度契合。

Importance reason: Proposes a new world model paradigm directly aligned with your high-priority world_model direction and continuous multimodal reasoning. Signals: world model, mechanistic interpretability, causal representation learning, autonomous discovery, aligns with world_model direction

Abstract: Recent advances in foundation models have transformed AI for Science, enabling remarkably accurate predictive performance across domains ranging from protein folding to weather forecasting. Yet prediction alone does not constitute scientific discovery. Scientific understanding depends on uncovering the reusable explanatory mechanisms that generate observations, whereas contemporary machine learning remains fundamentally organised around predictive mappings rather than explanatory structure. In this paper, we argue that scientific discovery is fundamentally a problem of knowledge organisation. To this end, we introduce Mechanistic World Models, a new design paradigm that places reusable mechanisms at the centre of representation, computation and learning. Drawing on insights from the philosophy of science, we derive the computational capabilities required for discovery, identify the design principles and inductive pressures that encourage explanatory knowledge to emerge, and formalise the anatomy of a mechanism-centric world model. Finally, we show how diverse research directions including mechanistic interpretability, causal representation learning, equation discovery and modular architectures capture complementary ingredients of this paradigm while lacking a unified framework. We propose Mechanistic World Models as a conceptual foundation and computational blueprint for moving AI beyond predictive forecasting towards autonomous scientific discovery.

</details>


<div id='direction-world_model-video_world_model'></div>

## 视频世界模型 [[Back]](#toc)

视频生成式世界模型、未来预测、长时序视频建模与动作条件视频生成。

### [15] [FlowWAM: Optical Flow as a Unified Action Representation for World Action Models](https://arxiv.org/abs/2607.13017)
*Yixiang Chen, Peiyan Li, Yuan Xu, Qisen Ma, Jiabing Yang, Kai Wang, Jianhua Yang, Dong An, He Guan, Gaoteng Liu, Jianlou Si, Jun Huang, Jing Liu, Nianfeng Liu, Yan Huang, Liang Wang*

Main category: cs.CV, cs.RO

Direction: 世界模型

Subtopic: 视频世界模型

Importance: 高优先级 (95.24) - Top 1 精读候选

TL;DR: FlowWAM 提出将光流作为统一的、视频原生的动作表示，构建双流扩散框架，在预训练视频生成器上同时实现世界模型模式（用目标光流引导未来视频生成）和策略模式（生成光流进行动作预测），在 RoboTwin 和 WorldArena 上取得最优性能。


<details>
  <summary>Details</summary>
Motivation: 现有 World Action Models (WAMs) 利用预训练视频生成器进行世界建模和动作预测，但面临动作表示难题：数值动作与视频生成器不对齐，而视觉动作表示忽略了帧间时序运动结构。需要一种既能与视频生成器对齐、又能携带足够运动线索的统一动作表示。

Method: FlowWAM 采用光流作为统一动作表示，构建双流扩散框架。光流视频与 RGB 视频格式相同且编码逐像素位移，可在共享的预训练视频生成器中联合建模。策略模式下生成光流用于动作预测；世界模型模式下使用目标光流序列引导未来视频生成。由于光流可从无动作标签的原始视频中提取，支持大规模无标签视频预训练。

Result: 在 RoboTwin 操作任务上，FlowWAM 在 Clean 和 Random 设置下分别达到 92.94% 和 92.14% 的成功率，优于 VLA 和 WAM 基线。在 WorldArena 世界建模上取得最佳 EWMScore (63.71)，轨迹准确率相对提升 18.4%。

Conclusion: 光流作为视频原生的动作表示，能够有效桥接预训练视频生成器与动作控制，同时提升世界模型和策略模式的性能，且支持大规模无标签视频预训练，为 WAM 提供了一种统一有效的动作表示方案。

Classification reason: 该论文提出 FlowWAM，使用光流作为统一动作表示，结合预训练视频生成器实现世界模型（未来视频预测）和策略（动作预测）两种模式，属于视频世界模型范畴，因此映射到 canonical subtopic "视频世界模型"。

Importance reason: Directly advances world model and video generation with a novel flow-based action representation, highly aligned with user's priority on world models and video generation. Signals: world model, video generation, optical flow action representation, dual-stream diffusion, state-of-the-art on RoboTwin and WorldArena

Abstract: World Action Models (WAMs) are able to leverage pretrained video generators for both world modeling and action prediction. However, directly leveraging such video generators for control raises a new challenge: how to represent actions in a suitable form that aligns with pretrained video generators while carrying enough motion cues for accurate control. Existing numerical actions fail to satisfy the former, and prior visual action representations overlook the temporal motion structure across frames. We address this issue with FlowWAM, a dual-stream diffusion framework that adopts optical flow as a unified, video-native action representation. Flow videos share the same format as RGB videos and encode rich per-pixel displacement. By jointly modeling them within a shared pretrained video generator, FlowWAM can naturally implement two modes of WAMs. In policy mode, FlowWAM generates flow for action prediction, while in world-model mode, it uses target flow sequences to guide future video generation. Moreover, since flow can be easily extracted from raw videos without action labels, FlowWAM can leverage large-scale action-unlabeled video datasets for pretraining. We empirically find that our flow-based action representation delivers gains across both modes. On RoboTwin manipulation, FlowWAM raises the success rate to 92.94% on the Clean setting and 92.14% on Random, outperforming both VLA and WAM baselines. On WorldArena world modeling, it achieves the best overall EWMScore (63.71) with an 18.4% relative improvement in trajectory accuracy. More results can be found on our project website: .

</details>


<div id='direction-multimodal_generation'></div>

# 多模态生成 [[Back]](#toc)



<div id='direction-multimodal_generation-three_d_generation'></div>

## 3D 生成与重建 [[Back]](#toc)

3D 物体/场景生成、3DGS、NeRF、Mesh 生成、神经渲染与重光照。

### [16] [MetaView: Monocular Novel View Synthesis with Scale-Aware Implicit Geometry Priors](https://arxiv.org/abs/2607.12000)
*Yufei Cai, Xuesong Niu, Hao Lu, Kun Gai, Kai Wu, Guosheng Lin*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 3D 生成与重建

Importance: 普通相关 (53.06) - 未入选精读

TL;DR: MetaView 是一个基于扩散模型的单目新视角合成框架，通过结合隐式几何先验（来自前馈几何感知网络）和度量深度信息，在单张大视角变化下实现几何一致且可控的新视角渲染，显著优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 现有视觉生成模型缺乏对空间结构的一致感知。显式几何先验方法虽能保证空间一致性但限制了大视角泛化，而隐式场景建模方法灵活但缺乏精确相机控制和几何一致性。作者希望结合两者优势，实现大视角变化下几何一致且可控的单目新视角合成。

Method: 提出 MetaView 框架，核心思路是将隐式几何建模与最小但必要的显式3D线索结合：(1) 使用前馈几何感知网络提取隐式几何先验来正则化结构，避免引入限制性的重建管线；(2) 利用度量深度将生成锚定到度量尺度上。整体基于扩散模型实现单目新视角合成。

Result: 在挑战性的单目大视角变化场景下，MetaView 显著优于现有方法，展现出优越的泛化能力。

Conclusion: MetaView 通过隐式几何先验与度量深度的巧妙结合，在保持几何一致性的同时实现了大视角变化下的灵活新视角合成，为单目新视角生成提供了有效的新范式。

Classification reason: 该论文提出基于扩散模型的单目新视角合成方法，属于从单张图像生成新视角的3D场景生成任务，与[3D 生成与重建]（three_d_generation）这一规范主题高度匹配。

Importance reason: Novel view synthesis with diffusion and implicit geometry is solid but not aligned with user's priority on continuous language/multimodal models or autoregressive generation. Signals: diffusion-based novel view synthesis, implicit geometry priors, metric depth for scale, monocular large viewpoint changes, 3D generation

Abstract: Current visual generation models are capable of producing high-quality content, yet they lack a coherent perception of the spatial structure. Existing generative novel view synthesis methods typically introduce explicit geometry priors, which enforce spatial consistency but inherently restrict generalization in large view changes. In contrast, recent interactive generative methods favor implicit scene modeling, offering greater flexibility at the cost of precise camera control and geometry consistency. In this paper, we propose MetaView, a diffusion-based monocular novel view synthesis framework that enables rendering under large view changes from a single image. Our key insight is to combine implicit geometry modeling with minimal yet essential explicit 3D cues: we incorporate implicit geometry priors from a feed-forward geometry perception network to regularize structure without imposing restrictive reconstruction pipelines, while leveraging metric depth to anchor the generation to a metric scale. This design allows MetaView to achieve both geometry consistency and precise controllability. Extensive experiments demonstrate that, under challenging monocular large viewpoint changes, MetaView significantly outperforms existing methods and exhibits superior generalization. Our code is publicly available at .

</details>


### [17] [SeamGen: Artist-Aligned UV Seam Generation via Graph Flow Matching](https://arxiv.org/abs/2607.12379)
*Hao Xu, Yuqing Zhang, Yiqian Wu, Xueqi Ma, Ding Liang, Yan-Pei Cao, Ying-Tian Liu, Xiaogang Jin*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 3D 生成与重建

Importance: 值得关注 (77.79) - 未入选精读

TL;DR: SeamGen 提出了一种基于图流匹配（Graph Flow Matching）的生成式 UV 接缝生成方法，通过学习专业艺术家标注的接缝布局数据分布，生成符合艺术家偏好和生产需求的 UV 布局，并支持无训练的条件约束与局部细化。


<details>
  <summary>Details</summary>
Motivation: UV 接缝放置是 3D 内容创建中劳动密集的关键步骤，现有自动方法依赖手工设计的优化目标或预训练模型的语义代理，难以对齐工业管线中艺术家的实际偏好和接缝模式。

Method: 提出 SeamGen，使用流匹配生成模型学习网格边级接缝标签的分布。设计 Mesh Transformer 骨干网络，交错使用局部图注意力（mesh edges）和全局自注意力（vertices），以捕获细粒度几何线索和长程拓扑连贯性。利用流模型的免训练修补能力实现局部接缝细化和约束引导生成。

Result: 大量实验表明，SeamGen 生成的 UV 布局比基于失真优化和语义代理的基线方法更符合艺术家偏好的接缝模式，具有更优的感知质量。

Conclusion: SeamGen 通过从专业接缝布局数据中学习先验，有效解决了自动 UV 接缝生成与艺术家偏好对齐的问题，为 3D 内容创建提供了实用的生成式解决方案。

Classification reason: 该论文研究 UV 接缝生成（UV seam generation），属于 3D 内容创建管线中的关键步骤，与 3D 网格/模型生成密切相关。因此映射到 [CANONICAL] subtopic "3D 生成与重建"（three_d_generation）。

Importance reason: Novel graph flow matching for UV seam generation, but outside user's priority subtopics and negative for 3D generation. Signals: Graph flow matching, UV seam generation, Mesh Transformer, artist-aligned generation, training-free inpainting

Abstract: UV seam placement is a critical yet labor-intensive step in 3D content creation, requiring artists to balance chart shape, seam concealment, and alignment with semantic and geometric features. Existing automatic methods are primarily based on per-object optimization, relying on handcrafted objectives to avoid distortion or on proxies from pretrained models to inject semantic information. However, these strategies are not always well aligned with seams used in industrial production pipelines, often resulting in layouts that deviate from artist-preferred seam patterns and practical production requirements. To address these limitations, we propose SeamGen, a generative model for UV seam generation that aligns with artist preferences and production requirements. Instead of depending on manually designed objectives and constraints, SeamGen learns the distribution of per-edge seam labels from a large corpus of existing seam layouts using a flow-matching generative model. A key challenge is that typical Transformer architectures used in flow matching models are designed for sequential representations, such as point clouds, and cannot naturally account for mesh topology. To enable mesh-native learning, we design a Mesh Transformer backbone that interleaves local graph attention over mesh edges with global self-attention across vertices, capturing both fine-grained geometric cues and long-range topological coherence. To further improve inference-time controllability and quality, we exploit the training-free inpainting capability of flow models for both localized seam refinement and constraint-guided seam generation. Extensive experiments show that by learning priors from professional seam layout data, SeamGen produces UV layouts that better align with artist-authored preferences and achieve superior perceptual quality compared with distortion-based and semantic-proxy baselines.

</details>


### [18] [ExtraGS: Enhancing Endoscopic View Extrapolation via Diffusion-Guided 3D Gaussian Splatting](https://arxiv.org/abs/2607.12785)
*Cheng-Tai Hsieh, Jiwei Shan, Han Fang, Jianshu Hu, Tao Ni, Lijun Han, Yutong Ban, Shing Shin Cheng, Hesheng Wang*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 3D 生成与重建

Importance: 普通相关 (47.99) - 未入选精读

TL;DR: ExtraGS 提出一种扩散引导的 3D Gaussian Splatting 框架，通过不确定性引导的虚拟相机采样和扩散模型伪观测精炼，增强内窥镜视角外推能力，显著减少 extrapolation 伪影。


<details>
  <summary>Details</summary>
Motivation: 传统内窥镜视野有限，而基于 NeRF/3DGS 的新视角合成方法在训练视角外 extrapolate 时会产生严重伪影，无法可靠地重建未观察到的解剖结构。

Method: 1) 从初始 3DGS 重建出发；2) 不确定性引导的虚拟相机采样策略，主动探索盲区；3) 扩散模型对采样视角的渲染结果进行精炼，生成合理的伪观测；4) 置信度加权的微调策略，将伪观测融入优化过程，同时保护已有可靠区域。

Result: 在多个公开内窥镜数据集上，ExtraGS 显著减少了 extrapolation 伪影，在新视角合成任务上达到了最先进性能。

Conclusion: 扩散引导的 3DGS 结合不确定性驱动的主动采样，能有效增强内窥镜视角外推能力，为机器人辅助微创手术提供更可靠的感知。

Classification reason: 该论文使用 3D Gaussian Splatting 进行内窥镜视角外推（novel view synthesis），属于 3D 场景重建与神经渲染范畴，与 [CANONICAL] subtopic "3D 生成与重建" 高度匹配。

Importance reason: Focuses on medical endoscopic view extrapolation, which is outside the user's priority subtopics and negative keywords. Signals: 3D Gaussian Splatting, diffusion-guided refinement, endoscopic view extrapolation, uncertainty-guided sampling, medical application

Abstract: Robot-assisted minimally invasive surgery (MIS) critically depends on reliable endoscopic perception for navigation and safety. However, conventional endoscopes provide only a limited field of view, leaving large portions of surrounding anatomy unobserved. Recent neural rendering approaches, such as Neural Radiance Fields and 3D Gaussian Splatting, enable novel view synthesis from endoscopic videos, but their reliance on sparse observations often leads to severe artifacts when extrapolating beyond the training this work, we propose ExtraGS, a framework for enhancing endoscopic view extrapolation via diffusion-guided 3D Gaussian Splatting. Starting from an initial reconstruction, we introduce an uncertainty-guided virtual camera sampling strategy to actively explore blind spots and maximize information gain. The rendered views from these sampled locations are refined using a diffusion model to recover plausible anatomical structures, producing pseudo observations that guide further optimization. To prevent the generated content from degrading reliable regions, we adopt a confidence-weighted fine-tuning strategy when incorporating these pseudo experiments on multiple public endoscopic datasets demonstrate that ExtraGS significantly reduces extrapolation artifacts and achieves state-of-the-art performance in endoscopic novel view synthesis.

</details>


### [19] [DermDepth: Toward Monocular Metric Scale 3D Reconstruction Models for Dermatology](https://arxiv.org/abs/2607.13010)
*Héctor Carrión, Narges Norouzi*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 3D 生成与重建

Importance: 值得关注 (66.86) - 未入选精读

TL;DR: DermDepth 是首个面向皮肤病学的单视图度量尺度3D重建模型，结合合成数据集 D-Synth 训练，将度量尺度误差从16倍以上降至1.1倍以下，支持皮肤镜和宏观图像的3D重建与表面法线估计。


<details>
  <summary>Details</summary>
Motivation: 皮肤病学中病灶尺寸、形态和纹理的测量与追踪对伤口和皮肤癌筛查至关重要，但现有研究过度依赖2D方法，缺乏3D信息。作者希望无需额外硬件或多视角拍摄，仅用单张图像即可实现密集的度量尺度3D重建。

Method: 构建了 D-Synth 合成皮肤镜数据集（含像素级精确3D标注），基于此训练 DermDepth 单视图度量尺度3D重建模型，并在少量真实临床样本上微调，实现从毫米到厘米尺度的泛化。

Result: 在真实皮肤镜数据上，度量尺度误差从16倍以上降至1.1倍以下；在三个真实世界基准上泛化良好，覆盖不同肤色、慢性伤口病例，测量结果与医学文献报告的疾病尺寸基本一致。

Conclusion: 单视图度量尺度3D重建在皮肤病学领域是可行的，合成数据训练结合少量真实数据微调可有效解决尺度模糊问题，为临床皮肤病变的3D量化评估提供了新途径。

Classification reason: 该论文提出单视图单目度量尺度3D重建模型用于皮肤病学，属于3D生成与重建方向，与[CANONICAL] subtopic "3D 生成与重建"（three_d_generation）高度匹配。

Importance reason: Specialized dermatology 3D reconstruction lacks alignment with user's priority on continuous multimodal models and video generation. Signals: domain-specific dermatology, monocular metric 3D, synthetic dataset D-Synth, no continuous token or flow matching, no autoregressive or video generation

Abstract: Dermatological practice routinely involves measuring and tracking lesion size, morphology and texture, as critical components of wound or skin cancer screening, monitoring and diagnosis. To accomplish this task, practitioners often image the skin surface with commonly available off-the-shelf camera sensors. This has led to an overwhelming research focus on 2D methods while these objectives naturally benefit from 3D information. In this paper, we demonstrate that dense monocular 3D reconstructions, metric scale measurements and rich surface normal texture estimates are achievable for both dermoscopic and macroscopic cases without the need for additional hardware or multiple captures. We present DermDepth, the first single-view metric scale 3D model for the dermatological domain and D-Synth, the first synthetic dermoscopic dataset with pixel-perfect 3D information. Our experiments show training DermDepth on D-Synth corrects metric scale error from over 16x to under 1.1x for real dermoscopic data, while preserving geometric quality and increasing texture richness. Fine-tuning on a small amount of real clinical samples generalizes our method across three real-world benchmarks spanning the few mm to hundred cm range, diverse skin-tones, chronic wound cases and produces measurements broadly consistent with disease size reported in medical literature. All code, data and models are available at .

</details>


### [20] [Compos3D: Interactive Part-Based Composition for Creative Control in Generative 3D Models](https://arxiv.org/abs/2607.12193)
*Faraz Faruqi, Sean J. Liu, George Fitzmaurice, Justin Matejka*

Main category: cs.CV, cs.HC

Direction: 多模态生成

Subtopic: 3D 生成与重建

Importance: 普通相关 (51.49) - 未入选精读

TL;DR: Compos3D 提出了一种基于部件组合（remixing）的交互式 3D 生成系统，用户从文本/图像生成多个候选模型，选择感兴趣的区域/部件并组合成最终设计，相比传统重新生成工作流提供了更强的创意控制与用户满意度。


<details>
  <summary>Details</summary>
Motivation: 当前生成式 3D 建模工作流依赖多次重新生成，用户控制力有限且结果不可预测，缺乏精细的局部编辑与组合能力。

Method: 提出 Compos3D 系统，支持用户从文本或图像提示生成多个 3D 候选模型，通过 2D 图像区域或 3D 网格分割选择感兴趣部件，并将它们组合成一个连贯的 3D 设计，系统自动合成并优化低层级几何细节。

Result: 通过受控用户实验对比 remixing 与 regeneration 两种工作流，结果表明 remixing 工作流在创意控制、意图对齐和用户满意度方面均显著优于传统重新生成方式。

Conclusion: 部件组合式工作流为生成式 3D 建模提供了更可控、更符合用户意图的交互范式，并给出了未来 AI 辅助 3D 建模的设计建议。

Classification reason: 该论文提出 Compos3D，一种基于部件组合的交互式 3D 生成工作流，属于 3D 内容生成范畴，与 [CANONICAL] 的 "3D 生成与重建" 子主题高度匹配。

Importance reason: Focuses on interactive 3D part composition, which is not a priority subtopic and lacks strong alignment with user's key boost keywords. Signals: 3D generation, interactive composition, user study, creative control, not autoregressive or diffusion video/image generation

Abstract: While generative AI has unlocked new opportunities for 3D content creation, current workflows often rely on multiple regenerations, which provides limited control and unpredictable outcomes. We present Compos3D, a system that introduces a compositional workflow for generative 3D modeling through remixing. Instead of repeatedly regenerating models, users generate multiple candidates from text or image prompts, select parts of interest via 2D image regions or 3D mesh segments, and assemble them into a coherent design. The system synthesizes these compositions into a refined 3D model, preserving high-level intent while resolving low-level geometry. To evaluate this approach, we conducted a controlled user study comparing remixing and regeneration workflows across both 2D and 3D modalities. Results show that the remixing workflow provides participants with greater creative control, stronger alignment with their intent, and higher satisfaction. We conclude with design recommendations for future AI-assisted 3D modeling workflows.

</details>


<div id='direction-multimodal_generation-dynamic_4d_generation'></div>

## 4D 与动态场景生成 [[Back]](#toc)

动态 3D/4D 场景生成与重建、4D 高斯泼溅、动态神经场。

### [21] [Beyond Perfect Priors: Adaptive Gaussian Graph for 4D Driving Reconstruction in the Wild](https://arxiv.org/abs/2607.12214)
*Xiaoyun Dong, Qian Xu, Yun Wang, Yang Lu, Jen-Ming Wu, Jianping Wang*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 4D 与动态场景生成

Importance: 高优先级 (80.36) - 未入选精读

TL;DR: 本文提出 Adaptive Gaussian Graph (AGG)，一种自校正的4D驾驶场景重建框架，能够在缺乏精确先验（如相机位姿、LiDAR深度）的野外视频（互联网/AI生成视频）中鲁棒地进行4D重建。AGG通过语义引导的Tick-Tock策略解耦静态背景与动态代理学习，并利用自适应拓扑进化模块修正图结构，在Wild-30新基准上优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 现有高斯场景图（GSG）方法严重依赖精确的先验信息（如相机位姿、LiDAR深度或人工标注），在野外视频中这些先验往往有噪声，导致优化歧义（如相机与代理位姿纠缠）和拓扑失败（如物体缺失），产生严重渲染伪影。因此需要一种无需精确先验、能在野外视频中鲁棒进行4D驾驶场景重建的方法。

Method: 提出 Adaptive Gaussian Graph (AGG) 框架，包含两个核心模块：(1) 语义引导的 Tick-Tock 策略，利用2D基础模型特征显式解耦静态背景/相机位姿更新与动态代理学习；(2) 自适应拓扑进化模块，主动修正图结构——生成缺失代理、重新分类错误分配的高斯体、剪枝误检。同时构建了 Wild-30 基准数据集（包含互联网和生成视频）。

Result: 在 KITTI 和 Wild-30 数据集上的大量实验表明，AGG 在视觉保真度和鲁棒性方面一致优于现有最先进方法，特别是在存在噪声先验的野外场景中表现突出。

Conclusion: AGG 通过自校正机制有效解决了野外4D驾驶场景重建中因噪声先验导致的优化歧义和拓扑失败问题，为无需精确先验的鲁棒4D重建提供了新范式，对自动驾驶仿真具有重要意义。

Classification reason: 该论文关注4D驾驶场景重建，使用高斯场景图方法进行动态场景的4D重建，属于多模态生成方向下的[4D 与动态场景生成]（dynamic_4d_generation）规范子主题。

Importance reason: Focuses on 4D driving reconstruction with noisy priors, which is niche and not aligned with user's priority subtopics on continuous models, autoregressive generation, or world models. Signals: 4D driving reconstruction, adaptive Gaussian graph, noisy priors, autonomous driving simulation, Wild-30 benchmark

Abstract: Reconstructing 4D driving scenes in the wild (e.g., internet and AI-generated videos) is critical for diverse autonomous driving simulation. While recent Gaussian Scene Graph (GSG) methods achieve impressive visual quality, they heavily rely on precise priors, such as accurate camera poses and LiDAR depth, or manual annotations. When initialized with noisy priors estimated from in-the-wild videos, existing GSG methods suffer from optimization ambiguity (e.g., entangling camera and agent poses) and topological failures (e.g., missing objects), causing severe rendering artifacts. To enable robust in-the-wild reconstruction, we introduce Adaptive Gaussian Graph (AGG), a self-correcting 4D framework. Our Semantically-Guided Tick-Tock Strategy leverages 2D foundation features to explicitly decouple static background and camera pose updates from dynamic agent learning. Concurrently, our Adaptive Topology Evolution module actively rectifies graph structures by spawning missing agents, reassigning misclassified Gaussians, and pruning false positives. To rigorously evaluate this in-the-wild setting, we introduce Wild-30, a challenging benchmark of internet and generative videos. Extensive experiments on KITTI and Wild-30 validate that AGG consistently outperforms state-of-the-art approaches in visual fidelity and robustness under noisy priors.

</details>


### [22] [Implicit 4D Gaussian Splatting for Fast Motion with Large Inter-Frame Displacements](https://arxiv.org/abs/2607.12362)
*Seung-gyeom Kim, Areum Kim, Yongjae Yoo, Sukmin Yun*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 4D 与动态场景生成

Importance: 普通相关 (58.98) - 未入选精读

TL;DR: SPIN-4DGS 提出了一种基于时空位置隐式网络的 4D 高斯泼溅方法，通过显式收集时空位置并用轻量级前馈网络预测高斯属性，解决了快速运动和大帧间位移下 4DGS 重建质量差、运动物体丢失的问题，在 CMU Panoptic 数据集上显著超越现有基线。


<details>
  <summary>Details</summary>
Motivation: 现有 4D 高斯泼溅方法在快速运动和大帧间位移场景下表现不佳，高斯属性难以学习，快速运动物体常从重建中丢失。作者希望设计一种能鲁棒处理大位移快速运动的 4D 动态场景重建方法。

Method: 提出 SPIN-4DGS（Spatiotemporal Position Implicit Network for 4DGS），核心思想是：1）显式收集所有时空位置的高斯属性，而非建模时间位移；2）为避免显式优化所有时空位置带来的巨大内存开销，使用轻量级前馈网络预测高斯属性；3）网络在基于光栅化的重建损失下训练，学习高斯间的共享表征，捕捉时空一致性。

Result: 在 CMU Panoptic 数据集的挑战性体育场景上，SPIN-4DGS 在大位移下持续取得更高保真度，PSNR 和 SSIM 均有明显提升。例如在 Basketball 场景中，相比最强基线 D3DGS，PSNR 提升 +1.83。

Conclusion: SPIN-4DGS 通过隐式网络预测时空位置高斯属性的方式，有效解决了 4DGS 在快速运动和大帧间位移下的重建退化问题，实现了更稳定、高质量的动态场景高斯泼溅。

Classification reason: 该论文提出 SPIN-4DGS，一种改进的 4D 高斯泼溅方法，专门处理快速运动和大帧间位移下的动态场景重建。这属于多模态生成方向下的 [CANONICAL] 子主题 "4D 与动态场景生成"（dynamic_4d_generation），该子主题明确涵盖 4D 高斯泼溅与动态场景重建。

Importance reason: Novel implicit network for 4DGS improves fast motion reconstruction, but is not in user's priority subtopics. Signals: 4D Gaussian Splatting, fast motion, large inter-frame displacement, implicit network, CMU Panoptic dataset

Abstract: Recent 4D Gaussian Splatting (4DGS) methods often fail under fast motion with large inter-frame displacements, where Gaussian attributes are poorly learned during training, and fast-moving objects are often lost from the reconstruction. In this work, we introduce Spatiotemporal Position Implicit Network for 4DGS, coined SPIN-4DGS, which learns Gaussian attributes from explicitly collected spatiotemporal positions rather than modeling temporal displacements, thereby enabling more faithful splatting under fast motions with large inter-frame displacements. To avoid the heavy memory overhead of explicitly optimizing attributes across all spatiotemporal positions, we instead predict them with a lightweight feed-forward network trained under a rasterization-based reconstruction loss. Consequently, SPIN-4DGS learns shared representations across Gaussians, effectively capturing spatiotemporal consistency and enabling stable high-quality Gaussian splatting even under challenging motions. Across extensive experiments, SPIN-4DGS consistently achieves higher fidelity under large displacements, with clear improvements in PSNR and SSIM on challenging sports scenes from the CMU Panoptic dataset. For example, SPIN-4DGS notably outperforms the strongest baseline, D3DGS, by achieving +1.83 higher PSNR on the Basketball scene.

</details>


### [23] [Hallo4D: Multi-Modal Hallucination Mitigation for Consistent Spatio-Temporal Generation](https://arxiv.org/abs/2607.12752)
*Hongbo Wang, Huaibo Huang, Jie Cao, Jin Liu, Haoyang Tong, Ran He*

Main category: cs.AI, cs.CV

Direction: 多模态生成

Subtopic: 4D 与动态场景生成

Importance: 普通相关 (53.06) - 未入选精读

TL;DR: Hallo4D 是一个统一且模型无关的框架，通过"生成-检测-纠正"范式利用大多模态语言模型（LMM）识别并修正 3D/4D 内容生成中的时空不一致性，无需重新训练或修改架构，在多种 3D/4D 生成设置中优于强基线方法。


<details>
  <summary>Details</summary>
Motivation: 现有 3D/4D 生成方法依赖 2D 扩散监督，缺乏显式的几何一致性机制，导致空间幻觉（如重复结构、几何错位）和时间不一致（如抖动、身份闪烁、结构漂移）。需要一种通用且可扩展的方法来缓解这些问题。

Method: Hallo4D 采用"生成-检测-纠正"三阶段范式：(1) 利用 LMM 从多视角/多帧渲染中识别并总结时空不一致性；(2) 通过 LMM 驱动的多模型投票选择候选修正，进行共识驱动的图像空间一致性优化；(3) 引入运动感知关键帧采样、LMM 引导的初始化和外观对齐提升时序一致性，以及曝光感知优化和可见性剪枝增强鲁棒性。

Result: 在多种 3D 和 4D 生成设置上的大量实验表明，Hallo4D 在缓解时空幻觉方面持续优于强基线方法，提供了可扩展且可泛化的解决方案。

Conclusion: Hallo4D 通过 LMM 驱动的检测-纠正机制有效缓解了 3D/4D 生成中的时空不一致性问题，且无需重新训练或修改模型架构，是一种通用且实用的解决方案。

Classification reason: 论文提出 Hallo4D 框架，用于缓解 3D/4D 内容生成中的时空幻觉（空间重复结构、几何错位、时间抖动、身份闪烁等），属于 4D 动态场景生成范畴，因此映射到 [CANONICAL] subtopic "4D 与动态场景生成"。

Importance reason: Focuses on 4D generation hallucination mitigation using LMMs, which is tangentially related to your multimodal generation priority but lacks direct connection to your core continuous language/multimodal model or autoregressive video generation subtopics. Signals: 4D generation, hallucination mitigation, LMM-based correction, model-agnostic framework, spatio-temporal consistency

Abstract: While recent advances in 3D generation have enabled impressive visual synthesis, existing methods often rely on 2D diffusion supervision without explicit mechanisms for geometric consistency, leading to spatial hallucinations such as duplicated structures and misaligned geometry. These issues become more severe in 4D generation, where maintaining consistency across viewpoints and temporal evolution introduces additional challenges, including jitter, identity flicker, and structural drift. We present \textbf{Hallo4D}, a unified and model-agnostic framework for mitigating spatiotemporal hallucinations in 3D and 4D content generation. Hallo4D introduces a generation-detection-correction paradigm that leverages large multimodal language models (LMMs) to identify and summarize spatial and temporal inconsistencies from multi-view and multi-frame renderings. These insights guide a consensus-driven image-space consistency optimization, where an LMM-based selector evaluates candidate corrections through multi-model voting, without requiring retraining or architectural modifications. To further improve temporal consistency and optimization efficiency, Hallo4D incorporates motion-aware keyframe sampling, LMM-guided initialization, and appearance alignment. We additionally introduce exposure-aware optimization and visibility pruning to enhance robustness under challenging viewpoints. Extensive experiments demonstrate that Hallo4D consistently outperforms strong baselines across diverse 3D and 4D generation settings, providing a scalable and generalizable solution for consistency-aware content generation.

</details>


<div id='direction-multimodal_generation-visual_editing'></div>

## 图像与视频编辑 [[Back]](#toc)

图像编辑、视频编辑、Inpainting、可控编辑、风格化等。

### [24] [What Would You Click? Personalized Video Thumbnail Generation with Preference-aware Highlight Retrieval](https://arxiv.org/abs/2607.12882)
*Zhiyu He, Zecheng Zhao, Tong Chen, Zi Huang, Yiqun Liu, Min Zhang*

Main category: cs.IR, cs.MM

Direction: 多模态生成

Subtopic: 图像与视频编辑

Importance: 普通相关 (54.09) - 未入选精读

TL;DR: 本文提出个性化视频缩略图生成任务，通过两阶段框架（个性化高亮检索 + VLM引导扩散生成）为用户定制视频缩略图，在公开数据集上取得SOTA性能。


<details>
  <summary>Details</summary>
Motivation: 现有视频缩略图生成方法通常对所有用户生成通用结果，忽略了个体偏好的多样性。本文旨在解决这一空白，提出个性化视频缩略图生成任务。

Method: 两阶段框架：第一阶段，个性化高亮检索器（personalized highlight retriever）捕捉细粒度用户-视频交互，结合视频摘要选择与用户偏好对齐的视觉锚点；第二阶段，VLM引导的扩散管道从锚点中提取语义视觉线索，注入扩散模型生成个性化缩略图，保持视觉连贯性和忠实性。

Result: 在两个公开数据集上，该方法在检索基线和生成基线上均取得SOTA性能。用户研究进一步表明该方法能提升点击偏好和用户参与度。

Conclusion: 个性化视频缩略图生成是提升用户参与度的有效途径，本文提出的偏好感知检索+可控生成框架能够有效平衡个性化与视觉质量。

Classification reason: 该论文涉及个性化视频缩略图生成，属于可控图像生成/编辑任务，与 canonical 子主题"图像与视频编辑"（visual_editing）相关。同时涉及 VLM 引导的扩散生成，也与多模态理解与生成方向相关。

Importance reason: The paper focuses on personalized thumbnail generation, which is a niche application not aligned with the user's priority subtopics like continuous language models or world models. Signals: Personalized video thumbnail generation, Two-stage retrieval-generation framework, VLM-guided diffusion, User preference modeling, Not in priority subtopics

Abstract: Video thumbnails are a key factor for attracting user clicks on video platforms, and are increasingly supported by automation. However, existing thumbnail generation methods typically produce generic results shared across users, overlooking the diversity of individual preferences. We therefore introduce personalized video thumbnail generation, a novel task that aims to create thumbnails tailored to user-specific preferences. It is challenging in two aspects: (i) identifying visual anchors (i.e., key frames) from each video to guide the generation, which requires a balance between personalization and informativeness that existing highlight detection methods fail to achieve; and (ii) generating personalized thumbnails that are both visually coherent and faithful to the original video. As a response, we propose a two-stage framework that tightly couples preference-aware retrieval with controllable generation. In the first stage, a personalized highlight retriever captures fine-grained user-video interactions and incorporates video semantics through summarization, enabling the selection of diverse visual anchors aligned with both user preferences and video contexts. In the second stage, a VLM-guided diffusion pipeline transforms these anchors into thumbnails by extracting and injecting semantically grounded visual cues, improving personalization while preserving visual coherence and fidelity. Experiments on two public datasets show our method delivers state-of-the-art performance compared with both retrieval-based and generative baselines. A user study further demonstrates improved click preference, highlighting its effectiveness in enhancing user engagement. The code is available at .

</details>


### [25] [Let RGB Be the Language of Vision](https://arxiv.org/abs/2607.12450)
*Timing Yang, Jinrui Yang, Xinlong Li, Yuhan Wang, Haoran Li, Yanqing Liu, Guoyizhe Wei, Jixuan Ying, Chen Wei, Rama Chellappa, Yuyin Zhou, Cihang Xie, Alan Yuille, Feng Wang*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 图像与视频编辑

Importance: 高优先级 (84.9) - 未入选精读

TL;DR: 本文提出RINO（RGB In RGB Out）框架，将分割、深度估计、姿态引导生成等不同视觉任务的输入输出统一表示为RGB图像，通过一个通用的图像编辑骨干模型实现零样本跨任务迁移，无需任务特定微调。


<details>
  <summary>Details</summary>
Motivation: 当前视觉模型通常为每个任务设计专门的架构和输出格式，缺乏像语言模型那样统一的表示和接口。作者希望将各种视觉信息（掩码、深度图等）统一表示为RGB图像，将视觉任务转化为统一的RGB到RGB图像编辑问题，从而实现一个模型处理多种任务。

Method: 提出RINO框架，核心思想是将所有视觉任务的输入和输出都表示为RGB图像。对于密集理解任务（如分割、深度估计），将输出（掩码、深度图）编码为RGB图像；对于密集条件生成任务（如姿态到图像生成），将条件（姿态图）作为RGB输入。基于一个通用的图像编辑骨干模型（无需任务特定微调），通过统一的RGB接口实现跨任务零样本迁移。

Result: RINO在分割、深度估计等密集理解任务以及姿态到图像生成等密集条件生成任务上展示了具有竞争力的零样本性能，证明了统一RGB表示的有效性。

Conclusion: RINO表明将不同视觉任务统一为RGB图像编辑问题是可行的，为构建统一的视觉语言系统提供了新思路，使多样化的视觉任务可以通过共享的视觉语言来表达和解决。

Classification reason: 该论文将各类视觉任务统一为RGB到RGB的图像编辑问题，核心方法是基于通用图像编辑骨干实现零样本跨任务迁移，因此映射到[CANONICAL] subtopic "图像与视频编辑" (visual_editing)。同时其统一多任务的思想也与"统一多模态模型"相关。

Importance reason: Proposes a unified RGB-in-RGB-out framework for diverse vision tasks, aligning with the user's high priority on unified multimodal models and continuous visual representation. Signals: unified multimodal framework, zero-shot cross-task transfer, RGB as visual language, image editing backbone, dense understanding and generation

Abstract: This work introduces a unified formulation for vision models, where diverse forms of visual information beyond natural images, such as masks, depth maps, and other structured visual signals, are all represented as RGB images, while general visual tasks can be converted into a common RGB-to-RGB image editing problem. In this paradigm, different types of visual information internally share the same encoding and decoding architecture and parameters as natural images, enabling a single model to transfer across tasks through a unified visual interface, in a way analogous to how language models operate over text. We refer to this formulation as RGB In and RGB Out (RINO). Built upon a generic image editing backbone without task-specific fine-tuning, RINO demonstrates robust and competitive zero-shot performance on both dense understanding tasks such as segmentation and depth estimation (where we unify outputs as RGB), and dense-conditioned generation tasks such as pose-to-image generation (where we unify inputs as RGB). We hope this study provides useful insights toward general unified vision-language systems, where diverse visual tasks can be expressed, interpreted, and solved through a shared visual language. Code is available at .

</details>


### [26] [DiTailed: Ensuring Visual Object Consistency in Text-Image-to-Image Flow Matching Models](https://arxiv.org/abs/2607.12539)
*Francesco Taioli, Daniel Coelho, Iaroslav Melekhov, Roberto Alcover-Couso, Jose Miguel Grande Saiz, Virginia Fernandez Arguedas, Artur Bekasov*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 图像与视频编辑

Importance: 值得关注 (79.44) - 未入选精读

TL;DR: 本文针对文本引导图像编辑中视觉对象一致性保持不足的问题，提出三个贡献：(1) ABO-Edit 数据集，包含12,000+三元组用于研究对象一致性；(2) 发现图像编辑 rectified flow 模型中条件嵌入空间在训练中未直接监督，却编码了最终生成图像的预测；(3) 提出 FlowMirror，一种无需参数修改的辅助损失函数，通过监督该条件嵌入空间来提升编辑质量。


<details>
  <summary>Details</summary>
Motivation: 现有文本引导图像编辑模型在编辑过程中经常无法保持被编辑对象的关键属性（视觉对象一致性），这是一个被忽视但重要的问题。作者希望系统性地解决这一局限性。

Method: (1) 构建 ABO-Edit 数据集：从艺术家设计的3D资产渲染出12,000+三元组（源图像、编辑提示、目标图像），含多视角覆盖和人工验证。(2) 发现 rectified flow 模型中条件嵌入空间（conditioning embedding space）虽未直接受监督，但在高噪声水平下已编码最终生成图像的预测。(3) 提出 FlowMirror：一种无需额外参数的辅助损失函数，通过监督该条件嵌入空间来提升对象一致性，无需改变模型架构。

Result: 在多个指标上，FlowMirror 相比基线方法提升了生成质量，有效改善了编辑过程中的视觉对象一致性。

Conclusion: 本文揭示了 rectified flow 模型中条件嵌入空间的预测特性，并利用这一发现提出了 FlowMirror 损失函数，在不改变架构的前提下显著提升了图像编辑的对象一致性。ABO-Edit 数据集也为该方向提供了有价值的评测资源。

Classification reason: 该论文研究文本引导的图像编辑中保持视觉对象一致性的问题，属于图像编辑范畴，映射到 [CANONICAL] subtopic "图像与视频编辑" (visual_editing)。

Importance reason: Focuses on image editing consistency, which is not a top priority subtopic and lacks strong alignment with user's configured direction weights and boost keywords. Signals: image editing, object consistency, flow matching, dataset contribution, no parameter modification

Abstract: Despite remarkable progress in text-guided image editing, generative models frequently fail to preserve visual object consistency, defined as the preservation of a subject's key attributes throughout the editing process. We address this limitation through three contributions. First, we introduce ABO-Edit, a dataset specifically designed to study object consistency, comprising over 12,000 triplets of source images, editing prompts, and high-quality target images rendered from artist-designed 3D assets, with multi-view coverage and human-verified quality control. Second, we uncover an overlooked property of image-editing rectified flow models: the conditioning embedding space, not directly supervised during training, encodes a prediction of the final generated image even at high noise levels. Third, exploiting this finding, we propose FlowMirror, a parameter-free auxiliary loss that supervises this conditioning embedding space. Without architectural changes, our method improves generation quality across several metrics over baselines.

</details>


<div id='direction-multimodal_generation-text_to_image_diffusion'></div>

## 文生图与图像扩散 [[Back]](#toc)

文本到图像生成、图像扩散模型、DiT 等基础图像生成方法。

### [27] [Controllable Generation of Diverse Dermatological Imagery for Fair and Efficient Malignancy Classification](https://arxiv.org/abs/2607.12987)
*Héctor Carrión, Narges Norouzi*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 文生图与图像扩散

Importance: 普通相关 (50.59) - 未入选精读

TL;DR: 本文提出 cgDDI 框架，通过可控生成多样化皮肤病图像来缓解医学图像中肤色和疾病分布不均衡问题，在合成数据训练下达到 86.4% 的恶性分类准确率，结合真实数据微调达到 90.9% 的 SOTA 性能。


<details>
  <summary>Details</summary>
Motivation: 皮肤病诊断模型在不同人群上的公平性不足，主要原因是缺乏针对深色皮肤和罕见疾病的高质量标注图像。现有数据增强方法难以在保持病灶特征的同时改变肤色和位置。

Method: 提出 cgDDI 混合框架：(1) 合成逼真健康皮肤样本而不改变其他输入属性；(2) 通过非参数方法将单样本罕见病灶映射到新肤色和新位置；(3) 支持仅需 10 个训练样本的高效参数化生成。框架兼容人工和自动分割掩码，可扩展到无预置病灶掩码的数据集。

Result: 在 DDI 数据集上，纯合成训练达到 86.4% 恶性分类准确率，结合真实数据微调达到 90.9% SOTA 性能。跨数据集实验在 F17k 上提升 13.9% 准确率。生成了 656 张图像扩展至 26.6 万+合成图像。

Conclusion: cgDDI 通过可控生成有效缓解了皮肤病数据中的肤色和疾病分布不均衡问题，提升了分类公平性和准确性，且生成框架具有较好的可扩展性。

Classification reason: 该论文提出可控皮肤病图像生成框架，属于图像生成范畴，与 [CANONICAL] 文生图与图像扩散 方向相关（虽非标准文生图，但核心是条件图像生成与扩散模型）。

Importance reason: Focuses on medical fairness and dermatology, which is outside the user's priority subtopics and includes negative keywords like clinical and medical diagnosis. Signals: Medical domain, fairness in dermatology, controllable generation, synthetic data augmentation, malignancy classification

Abstract: Accurate dermatological diagnosis naturally necessitates equitable performance across diverse populations, yet a systematic lack of expertly annotated images, especially for underrepresented skin tones and rare diseases, impedes progress toward measurably fair methods. We introduce cgDDI (Controllable Generation of Diverse Dermatological Imagery), a hybrid framework that (1) synthesizes realistic healthy skin samples without disturbing other input properties, (2) maps single-sample rare lesions onto novel skin-tones and locations non-parametrically, and (3) allows for efficient parametric generation with as few as 10 training samples. The framework supports both human and automated segmentation masking, enabling scalability to datasets without pre-made lesion masks. We grow a 656-image dataset by more than 400x and validate across two datasets: biopsy-confirmed Diverse Dermatology Images (DDI) and expert-verified Fitzpatrick17k (F17k). On the DDI benchmark, we achieve malignancy classification accuracy of 86.4% under synthetic-only training and 90.9% state-of-the-art performance with real data fine-tuning, alongside leading fairness metrics. Cross-dataset experiments show +13.9% accuracy improvements on unseen F17k data despite minimal disease overlap. We openly release 266k+ synthetic images, code, and generative models to further support fairness research at .

</details>


<div id='direction-multimodal_generation-text_to_video_diffusion'></div>

## 文生视频与视频扩散 [[Back]](#toc)

文本到视频生成、视频扩散模型、视频 DiT 等视频生成方法（不含自回归与世界模型）。

### [28] [WanToFight: Real-Time Generative Game Engine for Multi-Player Combat Interaction](https://arxiv.org/abs/2607.12592)
*Li Hu, Guangyuan Wang, Peng Zhang, Bang Zhang*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 文生视频与视频扩散

Importance: 高优先级 (95.06) - Top 2 精读候选

TL;DR: WanToFight 是一个基于视频扩散 Transformer 的实时生成式游戏引擎，支持双人《拳皇'97》对战，通过流式自回归生成器、玩家关联模块和键盘注入模块，在单张 RTX 5090 上以 30FPS 实时运行，首次实现了多人控制、实时推理、复杂物理交互与对抗性游戏玩法的统一。


<details>
  <summary>Details</summary>
Motivation: 现有生成式游戏引擎仅支持单人第一人称或非实时协作场景，无法同时处理多人控制、实时推理、复杂物理交互和对抗性游戏玩法。作者希望填补这一空白，构建首个支持实时双人对战的生成式游戏引擎。

Method: 基于 Wan-1.3B 视频扩散 Transformer，构建三个核心组件：1) 流式自回归生成器，采用块因果注意力和滚动 KV 缓存实现实时推理；2) 视觉锚定玩家关联模块，将每个玩家的键盘信号绑定到对应角色身份；3) 门控局部因果键盘注入模块，通过单人到完整对战的课程学习策略训练。使用四步 DMD 蒸馏学生模型和剪枝 VAE 解码器，在 512×384 分辨率下达到 30FPS。

Result: 在单张 NVIDIA RTX 5090 上，以 512×384 分辨率、30FPS 实时生成完整的双人对战游戏画面，支持复杂的物理交互和对抗性玩法。

Conclusion: WanToFight 是首个将多人控制、实时推理、复杂物理交互和对抗性游戏玩法结合于一体的生成式游戏引擎，展示了视频扩散模型在实时交互式游戏领域的巨大潜力。

Classification reason: 该论文基于 Wan-1.3B 视频扩散 Transformer 构建实时生成式游戏引擎，核心是视频扩散模型在交互式游戏场景中的应用，因此映射到多模态生成方向下的 [CANONICAL] 文生视频与视频扩散 子主题。同时涉及动作条件视频生成与交互式模拟，也匹配世界模型方向。

Importance reason: Pioneers real-time multi-player generative game engine with video diffusion transformer, directly advancing user's priority in video generation and world models. Signals: video diffusion transformer, real-time 30FPS generative game engine, multi-player adversarial gameplay, streaming autoregressive generation, rolling KV cache

Abstract: We present WanToFight, a generative game engine that simulates real-time, two-player The King of Fighters '97 (KOF~'97) gameplay from keyboard input. Prior generative game engines target either single-player first-person settings or non-real-time cooperative scenarios; multi-player control, real-time inference, complex physical interaction, and adversarial gameplay have not been jointly addressed. WanToFight closes this gap with three components built on the Wan-1.3B video diffusion transformer: a streaming autoregressive generator with block-causal attention and a rolling KV cache; a visually grounded Player Association module that binds each player's keyboard signal to a character identity; and a gated, locally causal keyboard injection module trained with a single-player-to-full-gameplay curriculum. A four-step DMD-distilled student paired with a pruned VAE decoder sustains 30FPS at 512x384 on a single NVIDIA RTX 5090 over the duration of a complete match. To our knowledge, WanToFight is the first generative game engine to combine multi-player control, real-time inference, complex physical interaction, and adversarial gameplay in one system.

</details>


### [29] [The Seriality Gap in Video Diffusion Models](https://arxiv.org/abs/2607.13031)
*Jorge Diaz Chao, Konpat Preechakul, Yuxi Liu, Yutong Bai*

Main category: cs.CV, cs.LG

Direction: 多模态生成

Subtopic: 文生视频与视频扩散

Importance: 高优先级 (86.43) - 未入选精读

TL;DR: 本文发现标准双向视频扩散模型在处理因果链增长的任务时性能下降，称为"序列性差距"（seriality gap），即视频扩散模型的去噪循环无法提供可扩展的序列计算，而自回归/分块生成和增加网络深度能有效缓解该问题。作者通过多球硬球动力学实验和理论证明，去噪步数不会增加骨干网络之外的序列计算能力。


<details>
  <summary>Details</summary>
Motivation: 视频扩散模型在视觉生成上表现出色，但其是否具备处理需要逐步序列推理的物理模拟任务尚不清楚。作者希望探究视频扩散模型在因果链增长的任务（如多球碰撞动力学）中的表现，并识别其根本性局限。

Method: 设计多球硬球动力学作为受控实验环境，对比标准双向视频扩散模型与自回归/分块生成方法在不同因果链长度下的性能。通过长度匹配的单球控制实验隔离依赖事件结构的影响，并进行干预研究（增加去噪步数、增加网络深度、采用自回归生成等）。最后给出理论证明。

Result: 标准双向视频扩散模型在因果链增长时性能显著下降，而去噪步数增加无法弥补；自回归/分块生成和增加网络深度能有效提升性能。理论证明确定性视频预测中，去噪步数不增加序列计算能力。

Conclusion: 视频扩散模型存在序列性差距这一结构性障碍，不适合需要可扩展序列计算的推理和模拟任务。这为未来视频生成模型的设计提供了重要启示，提示需要引入更多序列计算机制。

Classification reason: 论文研究视频扩散模型在多球硬球动力学模拟中的序列性差距问题，核心属于视频扩散模型的分析与改进，因此映射到 [CANONICAL] 文生视频与视频扩散（text_to_video_diffusion）子主题。

Importance reason: Identifies a fundamental limitation of video diffusion models for causal reasoning, directly relevant to your priority on world models and video generation. Signals: video diffusion limitation, seriality gap, causal chain reasoning, autoregressive generation benefit, world model relevance

Abstract: When one ball strikes another, then another, video models should predict the consequences of each bounce. In controlled experiments on multi-ball hard-sphere dynamics, we find that the performance of standard bidirectional video diffusion degrades as the causal chain lengthens, even when provided more denoising steps. In a length-matched single-ball control, where ball-ball interactions are absent, the degradation largely disappears, isolating dependent-event structure rather than video length as the cause. Across intervention studies, methods that increase effective serial computation improve performance disproportionately, including autoregressive/blockwise generation and architectural depth. We identify this pattern as the seriality gap: a mismatch between tasks requiring growing serial computation and video diffusion models whose denoising loop does not provide scalable serial compute. We then prove that, for deterministic video prediction, denoising steps do not add serial computation beyond the backbone, indicating a structural obstacle for video diffusion on serial reasoning and simulation tasks.

</details>


<div id='direction-multimodal_generation-efficient_generation'></div>

## 生成模型加速与高效化 [[Back]](#toc)

扩散加速、少步采样、蒸馏、量化、稀疏化等生成模型效率优化。

### [30] [Self-Consistent Flow: Unifying Velocity and Endpoint Prediction for Rectified Flow Models](https://arxiv.org/abs/2607.12171)
*Xu Han, Jiajing Hu, Li-Ping Liu*

Main category: cs.AI, cs.CV, cs.LG

Direction: 多模态生成

Subtopic: 生成模型加速与高效化

Importance: 高优先级 (93.65) - 未入选精读

TL;DR: 本文提出Self-Consistent Flow (SC-Flow)，通过轻量级一致性损失联合训练单个网络同时预测局部速度和数据端点，统一了两种参数化的优势，显著提升了Rectified Flow模型的生成质量和路径直线性。


<details>
  <summary>Details</summary>
Motivation: Rectified Flow模型中，网络可以训练预测两种不同目标（瞬时速度或数据端点），但两种参数化各有优劣：端点预测训练信号清晰稳定，速度预测在数据流形附近采样动态稳定。现有工作尚未充分理解其机制差异，也缺乏有效结合两者的方法。

Method: 提出Self-Consistent Flow (SC-Flow)，在标准Rectified Flow训练基础上增加一个轻量级一致性损失，迫使网络对速度预测和端点预测的输出保持一致。该方法无需改变网络架构，仅增加极小的计算开销，即可同时获得两种参数化的优势。

Result: 在图像生成任务上的大量实验表明，SC-Flow显著稳定了优化过程，改善了生成路径的直线性（straightness），在生成质量上相比标准Rectified Flow基线取得了显著提升。

Conclusion: 通过统一速度预测和端点预测，SC-Flow有效结合了两者的优势，为Rectified Flow模型提供了一种简单高效的改进方案，有望推广到更广泛的生成任务中。

Classification reason: 该论文提出SC-Flow方法，通过统一速度预测和端点预测来改进Rectified Flow模型，提升生成路径的直线性（straightness）和采样效率，属于扩散/流匹配模型的加速与优化，因此归入[CANONICAL] subtopic "生成模型加速与高效化" (efficient_generation)。

Importance reason: Unifies velocity and endpoint prediction for rectified flow, directly improving generation quality and path straightness with minimal overhead, aligning with your priority on efficient inference and flow matching. Signals: rectified flow improvement, consistency loss for dual prediction, lightweight training overhead, boosts generation quality and straightness, directly relevant to continuous language/multimodal models and flow matching

Abstract: In rectified-flow-based generative models, the neural network can be trained to predict two different targets, such as the instantaneous velocity or the data endpoint, to perform denoising. Although prior work shows that these parameterizations lead to different empirical behaviors, the mechanisms underlying their respective advantages remain to be underexplored, and how to combine them effectively is still unclear. In this work, we analyze how learning errors from different parameterizations affect the generation performance. We show that predicting the data endpoint has a clear training signal that stabilizes training, whereas predicting the velocity maintains stable sampling dynamics near the data manifold. Motivated by these insights, we propose Self-Consistent Flow (SC-Flow), a new method that unifies the benefits of both parameterizations. By employing a lightweight consistency loss, SC-Flow jointly trains a single network to predict both the local velocity and the data endpoint, and the consistency between the two predictions improves the model's performance. The method requires no major architectural changes and adds minimal computational overhead. Extensive experiments on image generation tasks demonstrate that SC-Flow substantially stabilizes optimization and improves the straightness of generation paths, leading to significant gains in generation quality over standard rectified-flow baselines.

</details>


### [31] [ACID: Adaptive Caching for vIDeo generation](https://arxiv.org/abs/2607.12358)
*Om Agrawal, Saurabh Agarwal, Aditya Akella*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 生成模型加速与高效化

Importance: 高优先级 (93.17) - 未入选精读

TL;DR: ACID 是一种轻量级、无需训练的缓存加速方法，通过动态监测扩散去噪过程中漂移信号的变化率，在关键时间步使用低阈值、其他步骤使用高阈值，从而在不显著降低视频质量的前提下大幅提升视频扩散模型的推理速度。


<details>
  <summary>Details</summary>
Motivation: 现有视频扩散模型推理速度慢，缓存加速方法（如 TeaCache、EasyCache、DiCache）使用固定阈值控制漂移信号累积，导致速度与质量之间存在看似固有的权衡。作者发现这种权衡并非本质性的，而是固定阈值策略的产物。

Method: ACID 作为轻量级、无需训练的包装器，通过监控现有缓存方法中漂移信号的变化率，在漂移变化剧烈的关键时间步自动切换到低阈值（保守缓存），在其他步骤使用高阈值（激进缓存），从而动态调整缓存策略。

Result: 在 TeaCache、EasyCache、DiCache 三种缓存方法和 HunyuanVideo、Wan 2.1、CogVideoX 三个视频扩散模型上，ACID 一致地扩展了视觉质量与推理速度的 Pareto 前沿。在 TeaCache + HunyuanVideo 上，相比无缓存基线加速 2.16 倍，相比保守固定阈值基线额外加速 38%，质量损失可忽略（PSNR < 0.3 dB, SSIM < 0.01, LPIPS < 0.01）。

Conclusion: ACID 证明了视频扩散模型缓存加速中速度与质量的权衡并非本质性的，通过动态阈值策略可以有效突破固定阈值的 Pareto 前沿。该方法信号无关、即插即用，具有良好的通用性和实用性。

Classification reason: 该论文提出视频扩散模型的缓存加速方法，属于生成模型推理加速优化，与 [CANONICAL] subtopic "生成模型加速与高效化" (efficient_generation) 高度匹配。

Importance reason: Directly addresses model efficiency for video generation, a high-weight subtopic, with a novel adaptive caching method that improves speed-quality Pareto frontier. Signals: video diffusion acceleration, training-free caching, adaptive threshold, drift signal monitoring, HunyuanVideo

Abstract: Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold {\tau}. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding {\tau} constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

</details>


### [32] [RFMSR: Residual Flow Matching for Image Super-Resolution](https://arxiv.org/abs/2607.12753)
*Shuwei Huang, Tianyao Luo, Jicheng Liu, Daizong Liu, Pan Zhou*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 生成模型加速与高效化

Importance: 高优先级 (83.9) - 未入选精读

TL;DR: RFMSR 提出残差流匹配框架用于图像超分辨率，将源分布中心化到低质量图像潜空间以减少传输距离，并通过两阶段训练策略实现高质量单步生成同时保留多步精化能力。


<details>
  <summary>Details</summary>
Motivation: 现有基于流匹配的超分辨率方法从纯高斯先验出发，丢弃了低质量输入中已有的丰富结构信息；同时，现有的单步加速技术往往牺牲模型的多步推理能力。本文旨在解决这两个问题。

Method: 提出残差流匹配（RFMSR）框架，将源分布中心化到低质量（LQ）潜空间，缩短传输距离并保留结构先验。采用两阶段训练策略：Phase I 通过条件流匹配预训练速度场；Phase II 对单步预测施加端到端监督，同时保留所有时间步的速度损失，实现高质量单步生成且不牺牲多步精化能力。

Result: 大量实验表明，RFMSR 在感知质量上达到或超越了现有最先进方法。

Conclusion: 残差流匹配结合两阶段训练策略能有效提升图像超分辨率的生成质量与效率，实现单步与多步推理的兼顾。

Classification reason: 该论文提出基于残差流匹配的图像超分辨率方法，核心贡献在于单步生成加速（Phase II训练策略实现单步推理）且不牺牲多步精化能力，属于生成模型加速与高效化（efficient_generation）范畴。

Importance reason: Proposes residual flow matching for efficient single-step super-resolution, but lacks direct connection to user's priority subtopics like continuous language models or autoregressive generation. Signals: flow matching for super-resolution, single-step generation, vision-only framework, residual flow, two-phase training

Abstract: Image super-resolution (ISR) has witnessed remarkable progress with diffusion models and flow matching. The dominant text-to-image (T2I) based approaches leverage large-scale foundation models as generative priors, achieving impressive perceptual quality but at the cost of massive model sizes and prohibitive training expenses. Recent flow-matching-based vision-only approaches have made significant strides; however, they adopt standard flow formulations that transport from a pure Gaussian prior to the data distribution, discarding the rich structural information already present in the low-quality (LQ) input. Furthermore, existing single-step acceleration techniques often forfeit the model's multi-step inference capability. In this paper, we propose Residual Flow Matching for Image Super-Resolution (RFMSR), a vision-only framework that centers the source distribution at the LQ latent, reducing transport distance and preserving structural priors throughout the flow trajectory. We further introduce a two-phase training strategy: Phase I pretrains the velocity field via conditional flow matching, while Phase II applies end-to-end supervision to the single-step prediction while retaining the velocity loss across all timesteps, achieving high-quality single-step generation without sacrificing multi-step refinement. Extensive experiments demonstrate that RFMSR achieves comparable or even superior perceptual quality compared to state-of-the-art (SOTA) methods. The source code is available at .

</details>


### [33] [A Shortcut to Statistically Steady-State Turbulence with Flow Matching](https://arxiv.org/abs/2607.13022)
*Gianluca Galletti, Gerald Gutenbrunner, William Hornsby, Lorenzo Zanisi, Naomi Carey, Stanislas Pamela, Johannes Brandstetter, Fabian Paischer*

Main category: physics.plasm-ph, cs.LG

Direction: 多模态生成

Subtopic: 生成模型加速与高效化

Importance: 值得关注 (79.09) - 未入选精读

TL;DR: 本文提出GyroFlow，一种基于flow matching的潜空间生成模型，直接估计陀螺动理学湍流的统计稳态分布，绕过耗时的瞬态模拟阶段，在5D相空间中生成饱和湍流快照，显著加速计算并优于自回归、降阶及其他生成方法。


<details>
  <summary>Details</summary>
Motivation: 许多非线性物理系统（如等离子体湍流）需要先经历漫长的瞬态演化才能达到统计稳态，而直接数值模拟必须完整解析这一瞬态过程，计算成本极高。现有的降阶模型和自回归代理模型要么缺乏通用性，要么存在误差累积问题。因此，作者希望绕过显式时间演化，直接建模稳态湍流的统计分布。

Method: 在潜空间中使用flow matching（流匹配）生成模型，在5D相空间中从噪声直接生成饱和湍流快照，以无量纲运行参数为条件。同时提出FGyD指标，在预训练的陀螺动理学模型潜空间中计算分布度量，用于评估生成质量。

Result: GyroFlow在生成稳态湍流统计量方面优于自回归模型、降阶模型和其他生成方法，同时提供显著的计算加速。生成的快照可用于热启动数值代码，进一步加速后续模拟。

Conclusion: 基于flow matching的潜空间生成模型能够有效绕过物理系统的瞬态模拟阶段，直接生成统计稳态湍流，为高维非线性物理系统的计算加速提供了新范式。

Classification reason: 该论文提出GyroFlow，一种基于flow matching的潜空间生成模型，用于直接生成湍流稳态快照，绕过耗时的瞬态模拟，实现显著加速。这属于生成模型加速与高效化（efficient_generation）范畴，因为核心贡献是通过生成式方法替代传统数值模拟中的瞬态计算，大幅提升效率。同时论文使用flow matching（连续潜空间流）技术，但与连续语言/多模态模型方向的核心主题（语言建模、视觉理解）无关，因此不归入continuous_language_multimodal。

Importance reason: Focuses on plasma turbulence simulation, not aligned with user's priority subtopics on continuous multimodal models or video/image generation. Signals: flow matching, latent generative model, steady-state turbulence, physics simulation, computational speedup

Abstract: Many nonlinear physical systems exhibit an initial transient phase in which perturbations grow before nonlinear interactions lead to a statistically steady state. While this saturated regime is of primary interest, direct numerical simulations must resolve the full transient dynamics before reaching it, incurring significant computational cost. In Computational Fluid Dynamics, reduced-order approaches such as Large Eddy Simulation mitigate computational cost by modeling small-scale dynamics, enabling tractable approximations of turbulent flows. In contrast, for systems such as gyrokinetics, comparably effective closures for the full dynamics are not generally available, and high-fidelity simulations remain necessary. Existing surrogate modeling approaches for these systems are autoregressive, hence they suffer from accumulating error. We instead propose to bypass explicit time evolution by directly modeling the distribution of saturated states under an ergodicity assumption, stating that ensemble averages over samples are equivalent to time averages of a single long simulation. We introduce GyroFlow, a latent generative model that directly estimates steady-state statistics of gyrokinetic turbulence in 5D phase space, without resolving the transient phase. GyroFlow generates saturated snapshots from noise, conditioned on dimensionless operating parameters and outperforms autoregressive, reduced-order, and other generative approaches, while providing substantial speedup. To evaluate generation quality we propose FGyD, a distributional metric computed in the latent space of a pretrained gyrokinetic model, and show that it correlates with downstream flux accuracy and solver convergence. Finally, GyroFlow can be used to warm-start the numerical code used to produce the data.

</details>


<div id='direction-multimodal_generation-generation_alignment'></div>

## 生成模型对齐与奖励 [[Back]](#toc)

生成模型对齐、RLHF、奖励建模、偏好优化、guidance 与可控性。

### [34] [SymbOmni: Evolving Agentic Omni Models via Symbolic Concept Learning](https://arxiv.org/abs/2607.12042)
*Jinxiu Liu, Jianru Li, Tanqing Kuang, Xuanming Liu, Kangfu Mei, Yandong Wen, Weiyang Liu*

Main category: cs.CV, cs.LG

Direction: 多模态生成

Subtopic: 生成模型对齐与奖励

Importance: 高优先级 (89.43) - 未入选精读

TL;DR: SymbOmni 提出了一种通过符号概念学习实现累积进化的智能体全模态模型，核心是 Symbolic Concept Box 记忆模块，通过 induction-transduction 循环将经验抽象为可复用的符号化工作流指令，并利用语言反馈的 verbalized backpropagation 实现无需梯度微调的持续自我改进，在图像生成质量和任务成功率上超越现有闭源模型，同时降低 40% 以上的 token 消耗。


<details>
  <summary>Details</summary>
Motivation: 现有视觉生成模型存在"永久新手"问题：无法累积学习、无法自主进化，缺乏将经验结构化为可复用知识的机制，导致每次任务都需要从头推理，组合泛化能力差、知识保留效率低。

Method: 提出 SymbOmni 框架，核心是 Symbolic Concept Box（可优化记忆模块），将底层操作抽象为可复用的符号化工作流指令（Symbolic Workflow Instructions）。通过 induction-transduction 循环运作：经验被抽象为符号概念（induction），然后自适应组合解决新任务（transduction）。训练使用 verbalized backpropagation，通过语言反馈实现持续自我改进，无需基于梯度的模型微调。

Result: SymbOmni 在迭代创作任务上显著优于现有基于智能体的系统，并在图像质量和任务成功率上超越闭源模型（如 Nano Banana、GPT-Image-1）；token 消耗降低超过 40% 的同时保持竞争性生成质量；在多个在线学习基准上实现累积增益，达到新 SOTA。

Conclusion: SymbOmni 通过符号概念学习有效解决了生成模型的"永久新手"问题，实现了无需梯度微调的持续自我进化，在生成质量、效率和持续学习能力上均取得显著提升，为构建真正可进化的智能体全模态模型提供了新范式。

Classification reason: 该论文提出 SymbOmni，一个通过符号概念学习实现累积进化的智能体全模态模型，核心是 Symbolic Concept Box 记忆模块和 induction-transduction 循环，使用语言反馈进行 verbalized backpropagation 实现持续自我改进。论文主要关注视觉生成任务（文生图/视频）的迭代创作，属于多模态生成方向。其核心思想是通过符号化概念抽象实现模型对齐与可控性提升，与 [CANONICAL] subtopic "生成模型对齐与奖励"（generation_alignment）高度相关，因为论文强调通过语言反馈进行自我改进、减少 token 消耗、提升生成质量与任务成功率，本质上是一种生成模型的对齐与优化方法。同时该工作也涉及理解与生成一体化，因此也匹配 multimodal_understanding_generation 方向。

Importance reason: Proposes a novel symbolic memory and verbalized backpropagation for agentic omni-models, enabling cumulative learning without gradient fine-tuning. Signals: agentic omni-model, symbolic concept learning, verbalized backpropagation, cumulative evolution, token efficiency

Abstract: Visual generation is increasingly ubiquitous in diverse domains, from text-to-image/video synthesis to multimodal interactive creation. Yet prevailing monolithic models remain fundamentally constrained by their inability to learn cumulatively and evolve autonomously, which is a limitation we term the "perpetual novice" problem. They lack mechanisms for structuring experience into reusable knowledge and therefore rely on brittle, "from-scratch" reasoning for each task, resulting in poor compositional generalization and inefficient knowledge retention. Motivated by these limitations, we propose SymbOmni, an agentic omni-model designed for cumulative evolution through Symbolic Concept Learning. At its core is the Symbolic Concept Box, an optimizable memory module that abstracts low-level operations into reusable Symbolic Workflow Instructions. SymbOmni operates through an induction-transduction cycle: experiences are abstracted into symbolic concepts (induction), which are then adaptively composed to solve novel tasks (transduction). The training is done by verbalized backpropagation with language-based feedback to enable continuous self-improvement without gradient-based model fine-tuning. Comprehensive experiments validate that (I) SymbOmni significantly outperforms existing agent-based systems for iterative creation and also surpasses closed-source models (e.g., Nano Banana, GPT-Image-1) in both image quality and task success rates; (II) SymbOmni effectively reduces token consumption by over 40% while maintaining competitive generation quality; and (III) SymbOmni enables effective continual learning by achieving cumulative gains across multiple online-learning benchmarks and setting a new state of the art.

</details>


### [35] [Contrastive-Augmented Flow Matching for Style-Content Disentanglement](https://arxiv.org/abs/2607.12404)
*Yusong Li, Pingchuan Ma, Ming Gui, Vincent Tao Hu, Björn Ommer*

Main category: cs.CV

Direction: 多模态生成

Subtopic: 生成模型对齐与奖励

Importance: 高优先级 (82.94) - 未入选精读

TL;DR: 本文提出对比增强流匹配（CAtFM），在可逆流匹配框架中引入对比正则化，通过对预测端点的对比监督来隐式促进内容与风格表征的解耦，在 CLIP/DINO/ALIGN 嵌入空间上验证了该方法在内容/风格检索、聚类分离和开放集鲁棒性上的提升。


<details>
  <summary>Details</summary>
Motivation: 扩散模型和流匹配模型在生成任务中常产生纠缠或不对齐的内容-风格表征，缺乏显式的解耦机制，限制了可控生成和组合泛化能力。

Method: 在可逆流匹配框架中，对预测端点（而非中间潜变量或速度场）施加对比学习正则化，强制跨分布传输的语义一致性，使解耦隐式涌现，无需假设内容/风格完全纯化或因子化。

Result: 在合成数据、域内风格和真实基准（ImageNet、WikiArt、DomainNet、DTD）上，CAtFM 在内容/风格检索、嵌入聚类分离度和开放集鲁棒性上均优于生成式和判别式基线。

Conclusion: CAtFM 提供了一种简单有效的方式，将判别式约束与确定性传输耦合，在分布偏移下提升解耦质量和鲁棒性。

Classification reason: 该论文提出对比增强流匹配（CAtFM），通过对比正则化促进内容-风格解耦，属于生成模型中对齐与可控性优化范畴，因此映射到 canonical 子主题「生成模型对齐与奖励」。

Importance reason: Proposes contrastive regularization for flow matching disentanglement, but lacks direct connection to user's priority subtopics like continuous language models or video generation. Signals: flow matching, contrastive learning, style-content disentanglement, CLIP/DINO/ALIGN embedding, open-set robustness

Abstract: Learning representations that separate content and style is crucial for controllable generation and compositional generalization. However, diffusion and flow-based models trained primarily with generative objectives often produce entangled or misaligned factors. To address this gap, we introduce Contrastive Augmented Flow Matching (CAtFM), a framework that integrates contrastive regularization into an invertible flow matching formulation to promote structured content-style representations. Rather than constraining intermediate latents or velocity fields, we apply contrastive supervision to predicted endpoints during training, enforcing semantic consistency across transported distributions while allowing disentanglement to emerge implicitly, without assuming strictly pure or fully factorized content and style representations. Our main experiments operate in the CLIP embedding space, with additional validation using frozen DINO and ALIGN encoders. Across synthetic data, in-domain styles, and real-world benchmarks (ImageNet, WikiArt, DomainNet, and DTD), CAtFM improves content and style retrieval, enhances embedding cluster separation, and achieves stronger open-set robustness compared to generative and discriminative baselines. Overall, CAtFM provides a simple way to couple discriminative constraints with deterministic transport, improving disentanglement and robustness under distribution shift.

</details>


### [36] [The Geometry of Memorization: Finite-Time Spectral Sensitivity as a Diagnostic for Flow Matching Models](https://arxiv.org/abs/2607.12616)
*Shuchan Wang*

Main category: cs.LG

Direction: 多模态生成

Subtopic: 生成模型对齐与奖励

Importance: 值得关注 (79.71) - 未入选精读

TL;DR: 本文提出有限时间谱灵敏度（FTSS）指标，通过分析 flow matching 模型轨迹的状态转移矩阵奇异值，无需梯度或外部数据即可检测生成模型是否过拟合记忆训练样本，揭示了数据稀缺下有效维度塌缩的几何病理。


<details>
  <summary>Details</summary>
Motivation: 连续时间生成模型（如 flow matching）理论上追求直线轨迹，但实际网络会发展出复杂的路径形变。当训练数据不足时，模型容易过拟合记忆训练样本，现有检测记忆化的方法需要外部成员查询或基线数据对比，缺乏仅依赖内部轨迹动力学的诊断工具。

Method: 提出 Finite-Time Spectral Sensitivity (FTSS) g(t)，通过追踪状态转移矩阵的均方根奇异值，作为稳定秩（stable rank）的连续代理指标。该指标仅需前向传播，无需梯度计算。通过分析 g(t) 在时间上的演化模式，区分泛化模型（稳定有效维度）与过拟合模型（谱塌缩）。

Result: 在合成数据和真实数据上的实验表明，FTSS 能够有效检测 flow matching 模型的记忆化行为。泛化良好的模型在轨迹上保持稳定的有效维度，而过拟合模型表现出明显的谱塌缩现象，且该指标与数据稀缺程度高度相关。

Conclusion: FTSS 提供了一种无需外部数据或成员查询的生成模型记忆化检测方法，揭示了数据稀缺下 flow matching 模型的几何病理，为生成模型的可控性和可靠性评估提供了新的内部诊断工具。

Classification reason: 该论文研究 flow matching 生成模型中的记忆化（memorization）检测问题，提出基于轨迹动力学的内部诊断方法 FTSS。这属于生成模型的可控性与质量保障范畴，与 canonical 子主题「生成模型对齐与奖励」（generation_alignment）中的 guidance 与可控性方向高度相关，因此复用该子主题。

Importance reason: Novel diagnostic for flow matching memorization, but tangential to user's priority on continuous models and generation. Signals: flow matching, memorization detection, spectral collapse, gradient-free diagnostic, data scarcity geometry

Abstract: Continuous-time generative frameworks construct probability paths between base and target domains by optimizing time-dependent velocity fields. While theoretical targets favor straight trajectories, empirical networks develop complex path deformations. This paper presents the Finite-Time Spectral Sensitivity (FTSS) g(t), a gradient-free, forward-pass metric that exposes flow geometry by tracking the root-mean-square singular value of the state-transition matrix. Serving as a continuous proxy for stable rank, g(t) reveals a distinct geometric pathology under data scarcity: while generalizing models maintain stable effective dimensions, overfitting causes a spectral collapse. We leverage this structural phenomenon to develop an internal geometric audit based on g(t). Our framework detects generative memorization using purely internal trajectory dynamics, removing the need for external membership queries or baseline data comparison.

</details>


<div id='direction-multimodal_generation-generation_benchmark'></div>

## 生成模型评测与数据集 [[Back]](#toc)

生成模型评测基准、新数据集、自动评测指标、人类偏好评估。

### [37] [Lost in Visual Translation: A VLM-Assisted Perceptual-Semantic Coherence Framework for EEG-to-Image Reconstruction](https://arxiv.org/abs/2607.12364)
*Sukriti Tiwari, BHVSP Subrahmanyam, Nidhi Goyal, Sai Amrit Patnaik*

Main category: cs.AI, cs.CV

Direction: 多模态生成

Subtopic: 生成模型评测与数据集

Importance: 普通相关 (41.24) - 未入选精读

TL;DR: 本文分析了现有图像质量指标（SSIM、LPIPS、CLIP）在EEG到图像重建任务中的不足——它们无法区分视觉保真度和语义可恢复性。作者提出了一种基于VLM的评测框架，通过四个VLM对图像对进行结构化问答，产生容忍性感知对齐分数（T-PAS）和容忍性语义对齐分数（T-SAS），并蒸馏为BCI一致性分数（BCS），实现了对EEG重建图像更合理的评测。


<details>
  <summary>Details</summary>
Motivation: 现有图像质量评测指标（SSIM、LPIPS、CLIP）在EEG到图像重建任务中存在严重缺陷：像素级指标与语义一致性几乎零相关，表征级指标混淆了感知错误和语义错误。EEG重建图像通常模糊、扭曲、细节缺失，但可能保留了可恢复的语义信息，现有指标无法合理评估这种"语义可恢复性"，因此需要一种BCI感知的评测框架。

Method: 1) 分析了6,855对真实/重建图像对（来自ATM、ENIGMA、BrainVis、DreamDiffusion四个数据集），使用语义探针、描述严格性分析、盲点率分析和受控退化实验揭示现有指标缺陷。2) 提出BCI感知评测框架：使用四个VLM（视觉语言模型）通过结构化问题评估图像对，产生T-PAS（容忍性感知对齐分数）和T-SAS（容忍性语义对齐分数）。3) 将VLM共识蒸馏为紧凑的BCS（BCI一致性分数）评估器。4) 通过人类验证评估框架可靠性。

Result: BCS评估器在T-PAS上MAE为0.079（r=0.700），在T-SAS上MAE为0.082（r=0.850）。人类验证显示高度可靠的一致性判断，Cohen's kappa=0.882±0.174，Krippendorff's alpha=0.882，支持感知-语义可恢复性优于通用视觉相似性的评测理念。

Conclusion: 本文提出的BCI感知评测框架有效解决了EEG到图像重建中现有指标无法区分视觉保真度和语义可恢复性的问题。VLM辅助的评测方法能够更合理地评估模糊但语义可恢复的重建图像，为脑机接口领域的图像重建提供了更合适的评测标准。

Classification reason: 该论文提出了一种针对EEG到图像重建任务的评测框架（BCS），核心贡献在于设计新的评测指标（T-PAS、T-SAS、BCS）来评估生成图像的质量，属于生成模型评测范畴。该论文同时涉及多模态理解（使用VLM进行语义评估），因此也匹配multimodal_understanding_generation方向。最合适的现有[CANONICAL]子主题是"生成模型评测与数据集"（generation_benchmark），因为论文核心是提出新的评测方法和指标。

Importance reason: The paper focuses on EEG-to-image evaluation metrics, which is outside the user's priority subtopics and lacks alignment with key boost keywords. Signals: EEG-to-image reconstruction, VLM-based evaluation framework, perceptual-semantic coherence, BCI-Coherence Score, not in priority subtopics

Abstract: EEG-to-image evaluation should distinguish visual fidelity from recoverable meaning. Yet EEG-derived reconstructions are blurry, distorted, and low-detail, causing SSIM, LPIPS, and CLIP to penalize semantically recoverable outputs or reward plausible but incorrect ones. We analyze 6,855 ground-truth/reconstruction pairs from ATM, ENIGMA, BrainVis, and DreamDiffusion using semantic probes, caption harshness and blind-spot rates, and controlled degradations. Pixel metrics show near-zero correlation with semantic consistency, while representation metrics conflate perceptual and semantic errors. We therefore introduce a BCI-aware framework in which four VLMs assess image pairs through structured questions, producing Tolerant Perceptual Alignment Scores (T-PAS) and Tolerant Semantic Alignment Scores (T-SAS). Their consensus is distilled into the BCI-Coherence Score (BCS), a compact evaluator achieving a T-PAS MAE of 0.079 (r = 0.700) and a T-SAS MAE of 0.082 (r = 0.850) on our data. Human validation shows highly reliable joint coherence judgments, with Cohen's kappa = 0.882 +/- 0.174 and Krippendorff's alpha = 0.882, supporting perceptual-semantic recoverability over generic visual similarity. Code and resources are available at .

</details>


### [38] [FormalAnalyticGeo: A Neural-Symbolic Based Framework for Multimodal Analytic Geometry Problem Generation](https://arxiv.org/abs/2607.12982)
*Ruoran Xu, Wending Gao, Qiufeng Wang*

Main category: cs.AI, cs.MA, cs.SC

Direction: 多模态生成

Subtopic: 生成模型评测与数据集

Importance: 值得关注 (66.19) - 未入选精读

TL;DR: 本文提出 FormalAnalyticGeo，一个基于神经符号方法的可扩展框架，用于全自动生成多模态解析几何问题。通过设计 CDL（条件描述语言）作为形式化中间表示，结合 SDF 渲染引擎和四个专用 LLM 组件（生成器、形式化器、测量器、质量验证器），构建了包含 7000+ 验证问题的 AnalyticGeo7K 数据集，中位相对误差仅 0.70%。


<details>
  <summary>Details</summary>
Motivation: 解析几何问题因标注样本稀缺而未被充分探索。现有图表生成方法无法处理约束驱动的布局，生成模型缺乏渲染带标注圆锥曲线的几何精度。需要一种可扩展的自动生成方法来解决数据稀缺问题。

Method: 设计 CDL（条件描述语言）作为形式化中间表示，桥接自由形式的问题文本与精确的图表渲染。框架包含四个专用 LLM 组件：Generator 生成多样化的解析几何问题，Formalizer 将问题转换为 CDL 并通过 SDF 引擎渲染图表，Measurer 通过视觉测量提取真实答案，Quality Verifier 在三阶段检查输出质量。结构化反馈驱动自动重试，形成无需人工标注的闭环。

Result: 构建了 AnalyticGeo7K 数据集（7000+ 验证问题），中位真实相对误差为 0.70%，82.3% 的答案落在精确符号解的 5% 以内。

Conclusion: FormalAnalyticGeo 框架能够以高几何精度自动生成大规模解析几何多模态问题，有效缓解了该领域的数据稀缺问题，为 MLLM 数学推理评测提供了高质量数据资源。

Classification reason: 该论文提出了一个自动生成解析几何多模态问题的框架，并构建了 AnalyticGeo7K 数据集。这属于多模态生成方向中的数据集构建工作，与 [CANONICAL] subtopic "生成模型评测与数据集"（generation_benchmark）高度匹配。同时该工作也涉及多模态理解与生成方向，因为生成的问题可用于评测 MLLM 的数学推理能力。

Importance reason: Focuses on dataset generation for analytic geometry, which is a niche educational application, not a core advance in continuous multimodal models or video/image generation. Signals: neural-symbolic framework, multimodal geometry dataset, formal intermediate representation, SDF rendering, LLM-based generation pipeline

Abstract: Math reasoning has achieved significant progress with the rapid advancement of Multimodal Large Language Models (MLLMs), however analytic geometry remains largely underexplored, primarily due to the scarcity of annotated samples. Existing diagram generation approaches struggle with analytic geometry: template methods cannot handle constraint-driven layouts, and generative models lack the geometric precision to render annotated conic curves correctly. We present FormalAnalyticGeo, a scalable framework for fully automatic generation of multimodal analytic geometry problems. Leveraging the rigor of formal languages, we design the framework around CDL (Condition Description Language), a formal intermediate representation that bridges free-form problem text with precise diagram rendering via a Signed Distance Field (SDF) engine. The framework employs four specialized LLM components in sequence: a Generator that produces diverse analytic geometry problems, a Formalizer that converts each problem into CDL for SDF-based rendering, a Measurer that extracts ground-truth answers through vision-based measurement on the rendered diagrams, and a Quality Verifier that checks outputs at three stages. Structured feedback from the Quality Verifier drives automatic retry, forming a closed loop that eliminates any need for human annotation. Applying FormalAnalyticGeo at scale yields AnalyticGeo7K, a dataset of over 7K verified multimodal problems, each with aligned text, diagram, formal annotation, and ground show that the generated problems achieve a median ground-truth relative error of 0.70\%, with 82.3\% of answers falling within 5\% of the exact symbolic solution. Our framework and dataset will be publicly released.

</details>


### [39] [Metric-Guided Synthetic Image Data Rendering for Deep Learning compatible with Agentic AI](https://arxiv.org/abs/2607.12874)
*Martina Radoynova, Samuel Pantze, Trina De, Ulrik Günther, Artur Yakimovich*

Main category: cs.CV, q-bio.QM

Direction: 多模态生成

Subtopic: 生成模型评测与数据集

Importance: 普通相关 (47.99) - 未入选精读

TL;DR: 本文提出 GraNatPy，一个通过量化指标（如梯度相似性）指导合成图像渲染场景优化的 Python 包，并展示了渲染数据的真实感、多样性和规模与目标检测零样本性能的正相关关系。同时将过程式数据渲染转化为智能体技能 SynthClaw，实现参数自动优化。


<details>
  <summary>Details</summary>
Motivation: 科学应用中的深度学习计算机视觉需要大量人工标注数据，成本高且易出错。合成数据生成可简化流程，但缩小真实与合成图像之间的域差距缺乏系统性的量化指导，现有方法依赖主观视觉判断。

Method: 提出 GraNatPy Python 包，利用多种图像质量指标（如梯度相似性）量化评估渲染场景的真实感，指导场景参数优化。在病毒噬菌斑检测任务中，通过混合真实与合成数据提升小目标检测性能。进一步将过程式渲染流程封装为智能体技能 SynthClaw，实现参数自动优化。

Result: 量化指标提升与渲染数据真实感、多样性及规模正相关，并提升目标检测模型的零样本性能。梯度相似性指标对小目标检测性能有显著影响，混合真实与合成数据可进一步改善检测效果。

Conclusion: 基于指标的合成数据渲染优化是一种有效且系统的方法，可缩小域差距并提升下游任务性能。将渲染流程智能体化（SynthClaw）为自动化科学图像数据生成提供了可行路径。

Classification reason: 该论文提出 GraNatPy 包，通过梯度相似性等指标系统指导合成图像渲染的质量提升，并评估合成数据对目标检测模型性能的影响，属于生成模型评测与数据集方向下的合成数据质量评估与指标设计，因此映射到 [CANONICAL] subtopic "generation_benchmark"。

Importance reason: Focuses on synthetic data rendering for scientific vision, not on core continuous multimodal or autoregressive generation priorities. Signals: synthetic data rendering, metric-guided optimization, agentic skill for rendering, domain gap reduction, virological plaque assays

Abstract: Deep learning computer vision for scientific applications requires collecting and annotating large datasets in a laborious, expensive and error-prone process. Synthetic data generation through 3D modelling and rendering may simplify this process and increase the accuracy of annotations by generating them programmatically. However, minimising the domain gap between real and synthetic images visually is subjective and lacks systematic quantitative guidance. We present GraNatPy, a Python package with metrics to guide improvement of the rendered scene. We show that quantifiable increase in realism, diversity and size of rendered dataset correlates with improved visual perception of the scene and higher zero-shot performance of an object detection model. Furthermore, we demonstrated using photographs of virological plaque assays that gradient similarity affects performance on small object detection, which can be improved by mixing real and synthetic data. Finally, we turn procedural data rendering into an agentic skill (SynthClaw) to automate the procedural parameter optimisation.

</details>


<div id='direction-multimodal_generation-autoregressive_visual_generation'></div>

## 自回归视觉生成 [[Back]](#toc)

自回归图像与视频生成，包括 next-token、MaskGIT、VAR、MAR、LlamaGen 等范式。

### [40] [ARDepth: Auto-regressive Monocular Depth Estimation with Progressive Visual Conditioning](https://arxiv.org/abs/2607.12433)
*Zijie Wang, Wei Zhang, Weiming Zhang, Xiao Tan, Weikai Chen, Xiaoxu Li, Guanbin Li*

Main category: cs.AI, cs.CV

Direction: 多模态生成

Subtopic: 自回归视觉生成

Importance: 普通相关 (61.5) - 未入选精读

TL;DR: ARDepth 提出将单目深度估计建模为结构化自回归生成过程，通过 Scale-Progressive Conditioning 注入多尺度视觉特征，并利用 Semantic-Aware Guidance 提供场景级语义先验，逐步从粗到细地生成深度图，在保持全局几何一致性的同时捕捉细粒度局部细节。


<details>
  <summary>Details</summary>
Motivation: 现有扩散模型进行单目深度估计时，隐式假设深度可通过迭代去噪恢复为全局平滑场，但这未能反映场景几何结构的分片、尺度依赖的组织方式。实际上，几何结构在空间尺度上逐步涌现——粗布局、表面和边界以层次化方式构建。作者受此启发，提出将深度估计建模为结构化自回归生成。

Method: ARDepth 将深度估计视为随空间分辨率递增的自回归生成过程。核心设计包括：(1) Scale-Progressive Conditioning (SPC)，在每个生成阶段注入多尺度视觉特征；(2) Semantic-Aware Guidance (SAG)，提供场景级语义先验以增强全局结构一致性。模型从低分辨率粗深度开始，逐步生成更高分辨率的深度图，每个阶段以上一阶段的输出和对应尺度的视觉特征为条件。

Result: 实验结果表明，ARDepth 在多个深度估计基准上取得了强性能，生成的深度图在跨尺度上保持结构一致性，验证了自回归生成作为几何建模新范式的有效性。

Conclusion: ARDepth 验证了自回归生成作为单目深度估计替代范式的可行性，通过渐进式视觉条件注入和语义引导，实现了全局一致且局部精细的深度预测，为几何建模任务提供了新的思路。

Classification reason: 该论文将单目深度估计建模为自回归生成过程，随空间分辨率递增逐步构建深度表征，属于自回归视觉生成范式。虽然任务为深度估计而非图像/视频生成，但其核心方法（自回归逐尺度生成）与 [CANONICAL] 自回归视觉生成 高度契合，因此复用该 subtopic。

Importance reason: Novel application of autoregressive generation to depth estimation aligns with user's priority on autoregressive visual generation and continuous multimodal models. Signals: autoregressive generation, monocular depth estimation, scale-progressive conditioning, semantic-aware guidance, structured generation

Abstract: Diffusion models have recently become the dominant paradigm for monocular depth estimation (MDE). However, they implicitly assume that depth can be recovered as a globally smooth field through iterative denoising, which does not explicitly reflect the piecewise and scale-dependent organization of scene geometry. In practice, geometric structure emerges progressively across spatial scales, where coarse layout, surfaces, and boundaries are constructed in a hierarchical manner. Motivated by this observation, we introduce ARDepth, which formulates depth estimation as structured auto-regressive generation. Instead of recovering depth through global refinement, ARDepth progressively constructs depth representations as spatial resolution increases. To support this generative process, we introduce Scale-Progressive Conditioning (SPC) to inject multi-scale visual features at each generation stage, and Semantic-Aware Guidance (SAG) to provide scene-level semantic priors that enhance global structural consistency. Together, these designs enable the model to capture fine-grained local details while maintaining coherent global geometry. Empirical results demonstrate that our approach achieves strong performance and produces structurally consistent depth predictions across scales, validating auto-regressive generation as a promising alternative paradigm for geometric modeling.

</details>


<div id='direction-multimodal_generation-audio_av_generation'></div>

## 音频与多模态联合生成 [[Back]](#toc)

音乐/语音生成、TTS、音视频联合生成、跨模态条件音频生成。

### [41] [Audio-Native Speech Recognition with a Frozen Discrete-Diffusion Language Model](https://arxiv.org/abs/2607.13013)
*Harsha Vardhan Khurdula, Abhinav Kumar Singh, Yoeven D Khemlani, Vineet Agarwal*

Main category: cs.AI, cs.SD

Direction: 多模态生成

Subtopic: 音频与多模态联合生成

Importance: 值得关注 (79.44) - 未入选精读

TL;DR: 本文提出使用冻结的离散扩散语言模型（DiffusionGemma，26B MoE）进行语音识别。通过Whisper编码器提取音频特征，轻量投影器映射到模型嵌入空间，并引入CTC损失解决梯度无法回传到投影器的问题。模型在LibriSpeech test-clean上达到6.6% WER，支持约8步并行解码，单适配器可处理6种语言。


<details>
  <summary>Details</summary>
Motivation: 当前自动语音识别（ASR）主要由自回归解码器主导，逐token生成转录。本文探索是否可以使用离散扩散语言模型以并行方式同时精炼整个转录，从而突破自回归逐token生成的效率瓶颈。

Method: 使用冻结的DiffusionGemma（26B MoE离散扩散语言模型）作为骨干，Whisper编码器提取音频特征，轻量投影器将音频特征映射到模型嵌入空间。采用低秩适配器（LoRA）让冻结骨干关注新模态。发现标准训练目标（扩散损失）无法有效将梯度回传到投影器，因此引入连接主义时序分类（CTC）损失通过冻结输出头打破僵局。仅训练约42M参数（骨干的0.16%）。

Result: 在LibriSpeech test-clean上达到6.6%词错误率（WER），无论话语长度如何，约8个并行去噪步骤即可完成转录。单个适配器在6种语言上训练，在英语、印地语和普通话上进行了评估。

Conclusion: 离散扩散语言模型可以有效地用于语音识别，通过并行去噪实现非自回归转录。CTC损失对于音频到文本的梯度传播至关重要。该方法在保持较低WER的同时显著减少了推理步骤，展示了扩散模型在ASR领域的潜力。

Classification reason: 该论文使用离散扩散语言模型进行语音识别（ASR），属于音频生成/处理范畴。虽然论文侧重语音识别而非生成，但其核心方法是离散扩散语言模型（DiffusionGemma）用于音频转录，与"音频与多模态联合生成"（audio_av_generation）这一规范主题相关，因为该主题涵盖语音/音频处理与跨模态条件生成。

Importance reason: Applies discrete diffusion to speech recognition but uses frozen backbone and CTC trick, not advancing continuous language models or video generation. Signals: discrete diffusion language model, frozen backbone, CTC loss for audio grounding, 6.6% WER on LibriSpeech, 26B MoE model

Abstract: Automatic speech recognition is dominated by autoregressive decoders that emit one token at a time. We ask whether a discrete diffusion language model can transcribe speech instead, refining a whole transcript in parallel over a small number of denoising steps. We train an audio-native interface for DiffusionGemma, a 26B mixture-of-experts model that generates text by uniform, random-token discrete diffusion rather than the absorbing-mask scheme common to recent diffusion language models. A frozen Whisper encoder supplies acoustic features, a lightweight projector maps them into the model embedding space, and low-rank adapters let the frozen backbone attend to the new modality. About 42M parameters are trained, which is 0.16 percent of the backbone. We find that the natural training objectives fail to ground the audio because their gradient reaches the projector only through attention that has already dismissed it. A connectionist temporal classification loss applied through the frozen output head breaks this deadlock. The resulting model reaches 6.6 percent word error rate on LibriSpeech test-clean, transcribes in roughly eight parallel steps regardless of utterance length, and uses a single adapter trained on six languages, which we evaluate here on English, Hindi, and Mandarin.

</details>


<div id='direction-multimodal_understanding_generation'></div>

# 多模态理解与生成 [[Back]](#toc)



<div id='direction-multimodal_understanding_generation-multimodal_hallucination'></div>

## 多模态幻觉与可靠性 [[Back]](#toc)

视觉幻觉缓解、事实性、可靠性、忠实性提升。

### [42] [Do You Remember? Toward Memory-Centric Multimodal AI](https://arxiv.org/abs/2607.11919)
*Xuguang Yu, Weigang Zheng, Minyue Yu*

Main category: cs.AI, cs.CV, cs.NE, cs.LG

Direction: 多模态理解与生成

Subtopic: 多模态幻觉与可靠性

Importance: 值得关注 (79.8) - 未入选精读

TL;DR: 本文提出DoYouRemember架构，将重建性记忆引入MLLM（VQ-VAE压缩+LoRA微调LLM+扩散解码器），发现LLM隐藏状态几乎不包含可恢复的视觉信息（理解但不记忆），通过局部EMA更新训练共享记忆矩阵M（229K参数，16倍压缩）解决梯度消失问题，并在信息论框架下统一解释：记忆是有损压缩，回忆是解压缩，幻觉是有损解压缩的固有属性。


<details>
  <summary>Details</summary>
Motivation: 当前MLLM缺乏人类的重建性记忆能力：它们通过冻结的视觉编码器处理图像，一次性输出文本后丢弃内部表征。作者希望将重建性记忆引入MLLM，并探究LLM隐藏状态中是否保留可恢复的视觉信息。

Method: 提出三阶段DoYouRemember架构：(1) VQ-VAE将图像压缩为离散视觉token；(2) LoRA微调的LLM联合关注视觉和文本token；(3) 扩散解码器从LLM隐藏状态重建图像。训练共享记忆矩阵M时采用局部EMA更新（每个图像仅更新64个槽中的top-8），避免全局梯度回传导致的梯度消失问题。

Result: 在1000张3D面部皮肤纹理图和99000张无标签面部图像上实验发现：LLM隐藏状态中几乎无可恢复视觉信息（扩散解码器从VQ-VAE token可重建清晰图像，但从LLM隐藏状态只能产生纯噪声）。训练的记忆矩阵M（229K参数，16倍压缩）在未见测试图像上接近VQ上界；扩展到1024个槽时超越VQ上界（LPIPS 0.056 vs 0.071），因为M的连续表征避免了VQ量化误差。

Conclusion: LLM理解图像但不记忆图像；记忆是有损压缩，回忆是解压缩，幻觉是有损解压缩的固有属性而非缺陷。局部EMA更新可有效训练共享记忆矩阵，其连续表征优于离散VQ。

Classification reason: 论文核心研究MLLM的记忆机制与幻觉的信息论解释，将幻觉定义为有损解压缩的固有属性而非缺陷，与多模态幻觉与可靠性（multimodal_hallucination）这一规范主题高度吻合。

Importance reason: Novel memory-centric MLLM architecture with information-theoretic framing, but focuses on multimodal hallucination and reliability, not user's priority subtopics. Signals: reconstructive memory, VQ-VAE compression, LoRA fine-tuning, diffusion decoder, gradient cancellation

Abstract: Human memory is reconstructive, not a faithful recording. Current multimodal LLMs (MLLMs) lack this capability: they process images through a frozen visual encoder, produce a one-shot text output, and discard internal representations. We present DoYouRemember, a three-stage architecture introducing reconstructive memory into MLLMs: (1) a VQ-VAE compresses images into discrete visual tokens, (2) a LoRA-fine-tuned LLM jointly attends to visual and text tokens, and (3) a Diffusion Decoder reconstructs images from the LLM's hidden states. On 1,000 3D facial skin texture maps and 99,000 unlabeled facial images, we find that LLM hidden states contain approximately zero recoverable visual information -- the same Decoder producing clear reconstructions from VQ-VAE tokens (pre-LLM) produces pure noise from LLM hidden states (post-LLM), demonstrating that the LLM understands images but does not remember them. Training a shared memory matrix M under backpropagation systematically fails due to gradient cancellation (O(1/sqrt(N)) attenuation). We identify three root causes and show that local EMA updating resolves all three: each image updates only its top-8 slots out of 64, preserving inter-slot diversity. The resulting M (229K parameters, 16x compressed) approaches the VQ upper bound on unseen test images. Scaling to 1,024 slots surpasses it (LPIPS 0.056 vs. 0.071), as M's continuous representation avoids VQ quantization error. We unify these findings under an information-theoretic framework: memory is lossy compression, recall is decompression, and hallucination is an inherent property of lossy decompression rather than a defect.

</details>


<div id='direction-multimodal_understanding_generation-multimodal_rl_alignment'></div>

## 多模态强化学习与对齐 [[Back]](#toc)

多模态 RL、RLHF、DPO、偏好学习、多模态对齐。

### [43] [A Learning-Rate-Gated Failure of GRPO in a Small Language and Vision-Language Model Web Agent: A Controlled Null and Its Mechanism](https://arxiv.org/abs/2607.12640)
*Chengguang Gan, Zhixi Cai, Yunhao Liang, Hanjun Wei, Shiwen Ni, Qinghao Zhang*

Main category: cs.AI, cs.CL

Direction: 多模态理解与生成

Subtopic: 多模态强化学习与对齐

Importance: 普通相关 (49.29) - 未入选精读

TL;DR: 本文系统研究了 GRPO 强化学习在 4B-8B 规模的语言和视觉语言模型 web agent 上的效果，发现中等至高学习率会损害已掌握任务的性能，而 GRPO 仅在存在提升空间（采样策略优于贪心策略）时才有帮助。论文通过控制实验揭示了学习率门控的失败机制，包括 degrade 和 collapse 两种模式的双重分离，以及小模型特有的有效秩与能力耦合关系。


<details>
  <summary>Details</summary>
Motivation: GRPO 等强化学习方法被广泛用于提升监督微调后的模型性能，但其在小规模语言和视觉语言模型 web agent 上的实际效果尚不明确。作者希望探究 GRPO 是否真正为小模型增加了新技能，还是仅仅重塑了监督模型已有的行为。

Method: 设计了包含 18 个运行的控制网格实验，系统变化学习率、KL 权重、随机种子、初始化和裁剪等超参数。在文本和 Set-of-Marks 截图两种观测模式下，对 4B 和 8B 规模的 web agent 进行 GRPO 训练，并通过配对检验、25 个评估种子、6 个训练种子等严格统计方法评估效果。同时通过局部化分析（grafting）和有效秩分析揭示失败机制。

Result: 没有配置能可靠提升强监督基线在已掌握任务上的成功率；中等至高学习率在文本轨道上会显著降低性能。当任务存在提升空间时（采样策略成功率高于贪心策略），GRPO 可使成功率提升 22 个点。中等学习率导致 degrade 模式（定位到 attention 和 MLP 块），高学习率导致 collapse 模式（无法定位到单一模块）。4B 模型后期层的有效秩与能力存在耦合，8B 模型则无此现象。

Conclusion: GRPO 仅在存在提升空间时才有帮助，否则可能损害已掌握任务的性能。学习率是关键门控因素，存在 degrade 和 collapse 两种失败模式，且小模型存在尺度依赖的有效秩-能力耦合现象。这为小规模模型上应用 GRPO 提供了重要的实践指导。

Classification reason: 该论文研究 GRPO（Group Relative Policy Optimization）在小规模语言和视觉语言模型 web agent 上的强化学习效果，属于多模态 RL 与对齐方向，映射到 canonical subtopic "多模态强化学习与对齐" (multimodal_rl_alignment)。

Importance reason: Focuses on GRPO failure in small web agents, not on user's priority subtopics like continuous models or video generation. Signals: GRPO failure analysis, small model web agent, learning rate gating, controlled null experiment, not aligned with user's priority subtopics

Abstract: Reinforcement learning with verifiable rewards, and Group Relative Policy Optimization (GRPO) in particular, is now run routinely on a supervised checkpoint in the hope of producing a stronger agent. We ask whether it adds skill to a small language and vision-language model web agent at the 4B to 8B scale, or whether it mostly reshapes behavior the supervised model already has. Across a control grid of 18 runs that varies learning rate, KL weight, seed, initialization, and clipping, no configuration credibly improves the success rate of a strong supervised baseline on tasks the agent has largely mastered. On the text track, moderate to high learning rates make it credibly worse. The null holds under paired testing, 25 evaluation seeds, 6 training seeds, changes to the recipe, both text and Set-of-Marks screenshot observations, and scaling the backbone to 8B; the credible harm is a text-track finding and is only nominal under Set-of-Marks. To show that the null reflects the setting and not a broken pipeline, we run the identical harness, reward, and recipe on tasks whose reward is reachable by sampling, and there the success rate rises by 22 points with a paired interval that excludes zero. GRPO therefore helps only when there is headroom to climb, meaning the sampled policy already succeeds more often than the greedy one. We then explain the failure. A middle learning rate degrades the agent and a high one collapses it, and the two regimes form a double dissociation: grafting localizes the degrade regime to the attention and MLP blocks, while the collapse regime cannot be traced to any single group, and the embedding change that dominates the weight movement is causally inert. At 4B, effective rank in the late layers tracks capability in both directions; at 8B the two come apart. This coupling is specific to the smaller model, so we report it as scale-dependent.

</details>


### [44] [MQAdapter: Multi-Modal Quantum Adapter for Coarse-to-Fine VLM Fine-tuning](https://arxiv.org/abs/2607.12418)
*Yumiao Zhao, Bo Jiang, Min Lu, Xiao Wang, Jin Tang*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态强化学习与对齐

Importance: 普通相关 (52.54) - 未入选精读

TL;DR: MQAdapter提出了一种基于量子计算的粗到细VLM少样本微调方法，利用Top-K候选类别作为语义锚点，通过跨模态量子学习机制在希尔伯特空间中建模高阶交互，生成更具判别性的视觉表征，在15个数据集上验证了有效性。


<details>
  <summary>Details</summary>
Motivation: 现有VLM在少样本分类中能较好过滤候选类别（高Top-K准确率），但在视觉相似类别间的细粒度判别上表现不佳（Top-1准确率低）。现有VLM适配器方法仅关注全局对齐，未能利用语义相似类别来精炼细粒度视觉表征。

Method: 提出MQAdapter，采用粗到细两阶段策略：1）粗筛阶段：检索与输入图像最相似的Top-K候选类别作为语义锚点；2）细粒度精炼阶段：将视觉和文本特征编码为量子态，利用量子纠缠和叠加在希尔伯特空间中建模高阶跨模态交互，生成更具判别性的表征。该方法参数高效，可集成到现有微调算法中。

Result: 在15个数据集上的实验表明，MQAdapter在少样本分类任务上优于现有方法，且所需可训练参数更少。

Conclusion: MQAdapter通过量子计算机制有效提升了VLM在细粒度分类上的判别能力，是一种参数高效的粗到细微调方法，可与其他微调算法兼容。

Classification reason: 该论文研究VLM的少样本微调对齐方法（粗到细的适配器），属于多模态对齐与偏好学习范畴，与[多模态强化学习与对齐]（multimodal_rl_alignment）方向高度相关。

Importance reason: Uses quantum computing gimmick for VLM fine-tuning, but lacks direct connection to user's priority subtopics like continuous language models or video generation. Signals: quantum adapter for VLM, coarse-to-fine few-shot learning, cross-modal quantum learning, parameter-efficient fine-tuning, Top-K candidate filtering

Abstract: Large-scale Vision-Language Models have demonstrated impressive transfer learning capabilities across a wide range of tasks. For few-shot classification, we observe that VLMs exhibit a notable ability to filter candidate categories and thus achieve high Top-K accuracy. However, they often struggle with fine-grained discrimination among visually similar categories, resulting in unsatisfactory Top-1 performance, as shown in Figure 1. Existing studies on VLM adapters generally focus on global alignment between visual and textual representations in the feature space, but fail to exploit semantically similar categories to refine fine-grained visual representations. Based on these observations, we propose a novel coarse-to-fine VLM fine-tuning approach for few-shot learning that leverages quantum computation, termed the Multi-Modal Quantum Adapter (MQAdapter). Specifically, MQAdapter first retrieves the Top-K category candidates most similar to the input image and uses them as semantic anchors. It then employs a cross-modal quantum learning mechanism to refine visual features under the guidance of these anchors. The core of this mechanism is the encoding of visual and textual features into quantum states. By leveraging quantum entanglement and superposition in a high-dimensional Hilbert space, MQAdapter effectively models higher-order cross-modal interactions, producing more discriminative representations than traditional Euclidean adapters. MQAdapter is parameter-efficient and can be integrated with various existing fine-tuning algorithms to achieve further performance gains. Evaluations on 15 datasets demonstrate the effectiveness of MQAdapter while requiring fewer trainable parameters.

</details>


<div id='direction-multimodal_understanding_generation-multimodal_reasoning_cot'></div>

## 多模态推理与思维链 [[Back]](#toc)

多模态 CoT、视觉推理、数学/代码/科学推理、推理时计算扩展。

### [45] [IQA-T1: Tool-based Visual Evidence Reasoning for Image Quality Assessment](https://arxiv.org/abs/2607.12375)
*Jinjian Wu, Jiaqi Tang, Wei Wei, Yingying Yan, Jianmin Chen, Botong Geng, Lei Zhang, Qifeng Chen*

Main category: cs.AI, cs.CV, eess.IV

Direction: 多模态理解与生成

Subtopic: 多模态推理与思维链

Importance: 值得关注 (75.81) - 未入选精读

TL;DR: IQA-T1 提出一种基于工具的视觉证据推理框架，让 MLLM 在图像质量评估过程中自主调用分析工具（如噪声残差图、梯度统计、频谱分析）生成结构化视觉证据，并将这些证据逐步融入推理链，从而提升 IQA 的泛化能力和可解释性。同时构建了包含 11k 条多模态推理链的 Q-Tool 数据集。


<details>
  <summary>Details</summary>
Motivation: 现有基于 MLLM 的 IQA 方法依赖语义偏置的内部表征进行推理，对低层感知退化（如噪声、模糊等）不敏感，泛化性和可解释性有限。作者希望引入显式的感知观测工具来增强 MLLM 的推理能力。

Method: 提出 IQA-T1 框架，在推理过程中让 MLLM 自主调用多个专用分析工具（如噪声残差图、梯度统计、频率谱分析等），生成结构化视觉证据，并将这些证据逐步融入推理链，形成工具增强的多步推理过程。同时构建 Q-Tool 数据集，包含 11k 条基于工具生成证据的多模态推理链，用于训练和评估。

Result: 在 7 个 IQA 基准上取得最佳综合性能，同时生成可解释的、基于证据的质量评估结果。

Conclusion: 通过工具增强的视觉证据推理，IQA-T1 有效提升了 MLLM 在图像质量评估任务上的泛化能力和可解释性，为开放环境下的 IQA 提供了新范式。

Classification reason: 该论文提出基于工具的视觉证据推理框架（IQA-T1），通过调用分析工具生成结构化视觉证据（噪声残差图、梯度统计、频谱等）并逐步融入MLLM推理过程，属于多模态推理（尤其是视觉推理）范畴，与[多模态推理与思维链]（multimodal_reasoning_cot）高度匹配。

Importance reason: Focuses on IQA interpretability via tool-based reasoning, but lacks direct connection to user's priority subtopics like continuous models or video generation. Signals: MLLM reasoning, visual evidence, tool-based framework, IQA generalization, Q-Tool dataset

Abstract: Image Quality Assessment (IQA) in open-world environments remains challenging due to limited generalization and interpretability. Recent approaches based on multimodal large language models (MLLMs) introduce textual reasoning for quality prediction, yet their judgments rely heavily on semantically biased internal representations, making them insensitive to low-level perceptual degradations. We propose IQA-T1, a tool-based visual evidence reasoning framework that augments MLLM reasoning with explicit perceptual observations. During inference, the model autonomously invokes specialized analysis tools to generate structured visual evidence, such as noise residual maps, gradient statistics, and frequency spectra, which are progressively integrated into the reasoning process. To support this paradigm, we construct Q-Tool, a dataset containing 11k multimodal reasoning chains grounded in tool-generated evidence. Extensive experiments on seven IQA benchmarks show that IQA-T1 achieves the best overall performance across datasets while producing interpretable and evidence-grounded quality assessments. Code and dataset are available at .

</details>


### [46] [MaxSAT-Based Feedback for Guiding Vision-Language Models in Sudoku](https://arxiv.org/abs/2607.12711)
*Pedro Orvalho, Guillem Alenyà, Felip Manyà*

Main category: cs.AI, cs.LO

Direction: 多模态理解与生成

Subtopic: 多模态推理与思维链

Importance: 普通相关 (52.54) - 未入选精读

TL;DR: 本文提出一种神经符号方法，通过 MaxSAT 求解器为 VLM 在 Sudoku 推理中提供逻辑一致性反馈，将 VLM 的候选放置编码为软子句，Sudoku 约束作为硬子句，利用 MaxSAT 找出最大一致子集并转化为文本和视觉反馈引导 VLM 迭代修正，实验表明该方法能提升逻辑一致性和解题成功率。


<details>
  <summary>Details</summary>
Motivation: VLM 在结构化视觉推理任务（如网格谜题）中虽有感知能力，但缺乏显式的逻辑一致性机制，常生成违反约束的答案。现有方法要么完全依赖 VLM 自身推理，要么用符号求解器直接计算答案，缺乏将两者有机结合的方式。

Method: 提出神经符号框架：VLM 生成候选数字放置后，将其编码为 MaxSAT 软子句，Sudoku 规则作为硬子句；MaxSAT 求解器找出最大一致子集，识别不一致的放置；将反馈转化为结构化文本描述和视觉标注，引导 VLM 在下一次迭代中修正错误。

Result: 在 Sudoku 数据集上对多个开源和闭源 VLM 进行评测，MaxSAT 反馈显著提升了逻辑一致性和解题实例数量，尤其在全局棋盘修正模式下效果最佳。

Conclusion: 符号优化（MaxSAT）可以增强 VLM 在结构化视觉推理中的可靠性，神经符号结合是提升 VLM 逻辑一致性的有效路径。

Classification reason: 该论文研究 VLM 在 Sudoku 结构化视觉推理任务中的逻辑一致性，使用 MaxSAT 约束求解器提供反馈以增强推理能力，属于多模态推理范畴，映射到 [CANONICAL] subtopic "多模态推理与思维链" (multimodal_reasoning_cot)。

Importance reason: Focuses on neuro-symbolic reasoning for Sudoku, which is a niche application not aligned with user's priority subtopics on continuous models, video/image generation, or world models. Signals: MaxSAT feedback for VLM, logical consistency in grid puzzles, neuro-symbolic refinement, not aligned with user's priority subtopics, no continuous language or generation focus

Abstract: Vision--Language Models (VLMs) have recently demonstrated promising performance on structured visual reasoning tasks, including grid-based puzzles. However, despite strong perceptual capabilities, these models lack explicit mechanisms for enforcing logical consistency and frequently generate assignments that violate underlying constraints. In this paper, we propose a neuro-symbolic approach that integrates formal constraint reasoning into the VLM solving process via a Maximum Satisfiability (MaxSAT) oracle. Rather than computing solutions directly, the symbolic component acts as a consistency validator and refinement engine. Candidate placements generated by the VLM are encoded as soft clauses in a partial MaxSAT formulation, while Sudoku constraints remain hard clauses. When inconsistencies arise, the MaxSAT solver identifies a largest mutually consistent subset of assignments, which is then translated into structured textual and visual feedback to guide subsequent refinements. We evaluate our approach on a Sudoku dataset across multiple open-source and closed-access VLMs. Results show that MaxSAT-based feedback improves logical consistency and increases the number of solved instances, particularly in full-board refinement mode. These findings demonstrate that symbolic optimisation can enhance the reliability of vision-language reasoning.

</details>


### [47] [Visual Access Boundaries in Vision-Language Model Reasoning](https://arxiv.org/abs/2607.12815)
*Hiroto Osaka, Shohei Taniguchi, Gouki Minegishi, Kai Yamashita, Masahiro Suzuki, Yutaka Matsuo*

Main category: cs.AI

Direction: 多模态理解与生成

Subtopic: 多模态推理与思维链

Importance: 高优先级 (89.43) - 未入选精读

TL;DR: 本文提出 Visual Access Sweep 因果干预方法，研究 VLM 在 CoT 推理中是否持续依赖图像 token 访问。实验发现 CoT 并不主要通过延长图像 token 访问来提升性能，而是通过扩展语言侧对隐藏状态信息的计算。CoT 的增益受限于视觉属性的可读性（readout），而非计数能力本身。


<details>
  <summary>Details</summary>
Motivation: CoT 被广泛用于 VLM 的测试时扩展，但其工作机制尚不明确：CoT 推理时是否持续需要图像 token 的访问？还是主要依赖早期前向传播中已提取的视觉信息？理解这一问题有助于揭示 VLM 推理的瓶颈。

Method: 提出 Visual Access Sweep 因果干预方法，沿层深度和生成时间维度遮蔽生成 token 查询对图像 token 键的注意力，定义 Visual Access Boundary (VAB) 为保持任务精度的最小访问区域。在 Qwen2.5-VL 和 InternVL3 的 6 种模型配置上进行实验，比较 no-CoT 直接回答和 CoT 提示下的 VAB。

Result: CoT 和 no-CoT 都表现出有限的 VAB；在 Qwen2.5-VL-32B 和 InternVL3 上，CoT 的 VAB 层与 no-CoT 边界最多相差两层，尽管 CoT 生成了更长的推理序列。CoT 在视觉属性可可靠读取时有效，在不可靠时无效。符号属性 oracle 实验表明，当提供真实属性文本时 CoT 能改善计数；单目标探测实验表明，困难属性可从隐藏状态线性恢复但模型难以输出。

Conclusion: CoT 并不主要通过延长图像 token 访问来提升 VLM 性能，而是通过扩展语言侧对已提取隐藏状态信息的计算。VLM 推理的瓶颈在于视觉属性的 readout（读取能力）而非计数等推理能力本身。

Classification reason: 该论文研究 VLM 中 Chain-of-Thought 推理的机制，分析 CoT 是否依赖持续访问图像 token，属于多模态推理与思维链范畴，与 canonical subtopic "multimodal_reasoning_cot" 高度匹配。

Importance reason: Provides causal analysis of CoT in VLMs, directly relevant to multimodal understanding and reasoning efficiency. Signals: Causal intervention on image token access, CoT gains from language-side computation not prolonged visual access, bottleneck is perceptual readout not counting, aligns with continuous multimodal reasoning and efficient inference priorities

Abstract: Chain-of-Thought (CoT) prompting is widely used as a test-time scaling strategy for Vision-Language Models (VLMs), but it remains unclear what is extended when VLMs generate longer reasoning traces. We ask whether CoT requires continued access to image tokens, or whether it mainly operates over visual information already made available earlier in the forward pass. We introduce Visual Access Sweep, a causal intervention that masks attention from generated-token queries to image-token keys along layer depth and generation time, and define the Visual Access Boundary (VAB) as the minimal access region that preserves task accuracy. Across six model configurations from Qwen2.5-VL and InternVL3, both no-CoT direct answering and CoT prompting exhibit finite VABs. In Qwen2.5-VL-32B and InternVL3 at 14B and 38B scales, when CoT is evaluated against the no-CoT full-access target, its VAB layer differs from the no-CoT boundary by at most two layers, despite substantially longer generations. This suggests that CoT does not primarily improve performance by prolonging direct image-token access throughout the reasoning trace, but by extending language-side computation over image-derived hidden-state information. We further show that CoT gains are constrained by perceptual readout. CoT helps when the queried visual attribute can be reliably read out by the model, but not when that readout is unreliable. A symbolic-attribute oracle shows that CoT can improve counting once ground-truth attributes are supplied as text, while a single-object probe-vs-decode check shows that hard attributes can be linearly recoverable from hidden states yet difficult for the model itself to output. Together, these analyses place the bottleneck at readout rather than counting.

</details>


### [48] [ReflectVLN: Training Vision-Language Navigation Agents with Reflective Reasoning](https://arxiv.org/abs/2607.12680)
*Jiahang Wang, Yirong Yang, Yanqing Zhu, Minghua Luo, Shichao Xie, Fei Liu, Mu Xu*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态推理与思维链

Importance: 普通相关 (49.29) - 未入选精读

TL;DR: ReflectVLN 提出了一种基于反思推理的视觉语言导航框架，通过双向交互的意图智能体和执行智能体实现闭环决策，并引入 Action-CoT 训练方案，在 VLN 基准上提升了成功率和路径效率。


<details>
  <summary>Details</summary>
Motivation: 现有 VLN 方法通常将 VLM 与航点解码器结合生成多步动作计划，但缺乏显式的闭环机制来跟踪语义进度、诊断执行失败并从长程导航中的错误累积中恢复。

Method: 提出 ReflectVLN 框架，包含两个核心组件：(1) 意图智能体（Intention Agent）负责子任务分解与反思，生成可执行的子任务描述作为纠正计划；(2) 执行智能体（Execution Agent）将子任务描述接地到当前观测下的短时动作，同时监控子目标进度并检测偏离行为。两者通过闭环双向通信：执行智能体发出进度和偏离信号触发反思，意图智能体返回结构化指导。此外引入 Action-CoT（行动思维链），一种路径条件双查询训练方案，用于生成时序连贯且可解释的中间推理。

Result: 在标准 VLN 基准上，ReflectVLN 在有限数据预算下提升了成功率和路径效率，训练成本更低，推理时高层意图调用次数更少，同时提供可解释的中间决策。

Conclusion: ReflectVLN 通过反思推理和闭环通信机制有效缓解了长程导航中的错误累积问题，为 VLN 提供了一种可解释且高效的决策框架。

Classification reason: 该论文提出 ReflectVLN，在视觉语言导航（VLN）中引入反思性推理和 Action-CoT（行动思维链），属于多模态推理与思维链范畴，因此映射到 [CANONICAL] subtopic "多模态推理与思维链" (multimodal_reasoning_cot)。

Importance reason: Focuses on VLN agentic reasoning, not aligned with user's priority on continuous multimodal models or video/image generation. Signals: VLN agentic framework, Action-CoT training, closed-loop navigation, not continuous token or generation, no video/image generation

Abstract: Existing vision-language navigation methods often couple a VLM with waypoint decoders to produce multi-step action plans, but they typically lack an explicit closed-loop mechanism for tracking semantic progress, diagnosing execution failures, and recovering from error accumulation in long-horizon navigation. To address this gap, we propose ReflectVLN, an agentic VLN framework that organizes decision-making through bidirectionally interactive intention and execution agents. The intention agent performs subtask decomposition and reflection, generating executable subtask descriptions as corrective plans. Conditioned on these descriptions, the execution agent grounds them into short-horizon actions under current observations while monitoring sub-goal progress and detecting off-track behavior. Crucially, ReflectVLN enables closed-loop bidirectional communication: the execution agent emits progress and deviation signals to trigger reflection and subtask updates on demand, and the intention agent returns structured guidance that reconditions subsequent actions for recovery. To encourage temporally coherent decisions with interpretable intermediate rationales, we introduce Action Chain-of-Thought (Action-CoT), a path-conditioned dual-query training scheme for action generation. Experiments on standard VLN benchmarks show that ReflectVLN improves success rates and path efficiency under a constrained data budget, with favorable training cost and fewer high-level intention calls at inference time, while providing interpretable intermediate decisions for analysis and collaboration. Code is available at:

</details>


### [49] [UniVR: Thinking in Visual Space for Unified Visual Reasoning](https://arxiv.org/abs/2607.12800)
*Zhongwei Ren, Yunchao Wei, Yao Zhao, Weibo Gong, Xiao Liu, Anran Wang, Xiangtai Li, Xiaojie Jin*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态推理与思维链

Importance: 值得关注 (73.17) - 未入选精读

TL;DR: UniVR 首次探索从纯视觉演示中同时学习复杂推理、细粒度物理动力学和长期规划。提出 VR-GRPO 强化学习范式，结合全局和步骤级奖励确保推理的逻辑一致性和物理一致性，无需任务特定启发式或图文对。构建了 VR-X 大规模基准，在纯视觉协议下评估多种推理能力，在 VR-X 上提升高达 25%。


<details>
  <summary>Details</summary>
Motivation: 现有视觉推理方法通常依赖图像-文本对或任务特定的启发式规则，限制了模型从纯视觉数据中学习广泛世界知识的能力。作者希望探索是否可以在纯视觉空间中同时学习复杂推理、物理动力学和长期规划，而不依赖文本监督。

Method: 提出 UniVR 框架，核心是 VR-GRPO（Visual Reasoning - Group Relative Policy Optimization）强化学习范式。该方法包含全局奖励（评估整体推理逻辑一致性）和步骤级奖励（确保每一步的物理一致性），通过强化学习在纯视觉空间中对推理过程进行优化，无需任务特定启发式或图文对。同时构建了 VR-X 基准，涵盖长时程操作、空间谜题和物理推理等 16 个来源的多样化数据。

Result: UniVR 在 VR-X 基准上相比基线提升高达 25%，同时在多种多模态理解基准上也展现出性能提升，表明纯视觉空间中的推理能力可以泛化到更广泛的多模态理解任务。

Conclusion: 纯视觉空间中的推理具有巨大潜力，UniVR 证明了无需文本监督即可从视觉演示中学习复杂推理、物理动力学和长期规划。代码、数据和模型均已开源。

Classification reason: 该论文研究纯视觉空间中的复杂推理（物理推理、长期规划等），属于多模态推理范畴，与 [CANONICAL] subtopic "多模态推理与思维链" 高度匹配。同时论文涉及物理世界建模与长期规划，也与 world_model 方向相关。

Importance reason: Pioneers pure visual-space reasoning with RL, directly aligning with your high-priority continuous multimodal and world model directions. Signals: visual reasoning from pure video, VR-GRPO reinforcement learning, 25% improvement on VR-X, open-sourced code/data/models, boosts multimodal understanding benchmarks

Abstract: Learning broad world knowledge directly from raw visual data is a fundamental capability of intelligence. We introduce UniVR, the first investigation into simultaneously learning complex reasoning, fine-grained physical dynamics, and long-term planning from pure visual demonstrations. At its core, UniVR features VR-GRPO, a reinforcement learning paradigm with complementary global and step-level rewards. This approach enforces logical coherence and physical consistency throughout the reasoning process without requiring task-specific heuristics or image-text pairs. To train and evaluate UniVR, we construct VR-X, a large-scale benchmark curated from 16 diverse sources spanning long-horizon manipulation, spatial puzzles, and physical reasoning. It is the first comprehensive suite to assess these heterogeneous capabilities under a purely visual protocol. Remarkably, UniVR achieves up to a 25% improvement on VR-X, and its superior visual reasoning also boosts performance on various multimodal understanding benchmarks. These findings underscore the vast potential of reasoning within visual spaces, with all code, data, and models are open-sourced for further research.

</details>


<div id='direction-multimodal_understanding_generation-multimodal_retrieval'></div>

## 多模态检索 [[Back]](#toc)

跨模态检索、组合检索、文档检索、多模态搜索。

### [50] [EvoGraph-R1: Self-Evolving Multimodal Knowledge Hypergraphs for Agentic Retrieval](https://arxiv.org/abs/2607.12764)
*Jiashi Lin, Changhong Jiang, Xiangru Lin, Ruifei Zhang, Xinyi Zhu, Jiyao Liu, Cheng Tang, Ye Du, Shujian Gao, Junzhi Ning, Lihao Liu, Ziyan Huang, Tianbin Li, Jin Ye, Junjun He*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态检索

Importance: 值得关注 (79.09) - 未入选精读

TL;DR: EvoGraph-R1 提出了一种自进化的多模态知识超图框架，将检索增强生成重新定义为马尔可夫决策过程，通过智能体动作（查询、搜索、编辑、回答）动态演化图结构，实现多模态多跳推理，在 VQA 和文本 QA 基准上显著优于现有 RAG 方法。


<details>
  <summary>Details</summary>
Motivation: 现有 GraphRAG 方法将知识图谱视为离线构建的静态数据结构，仅支持单次查询，无法适应知识密集型推理的交互式、迭代式需求，存在文本中心碎片化、结构冻结无法更新、单次检索缺乏自适应优化三大瓶颈。

Method: 将检索建模为马尔可夫决策过程（MDP），智能体观察图状态并执行四种动作：GraphRetrieve（图检索）、WebSearch（网络搜索扩展）、GraphEdit（图编辑修正）、Answer（终止回答）。动作反馈信号引导超图结构自演化，形成闭环迭代优化。

Result: 在多模态 VQA 和文本 QA 基准上，EvoGraph-R1 在准确性、覆盖率和可追溯性方面均显著优于现有 RAG 基线方法。

Conclusion: 自进化知识图谱可作为跨模态检索增强生成的基础范式，有效弥合静态图结构与动态推理需求之间的鸿沟。

Classification reason: 该论文提出自进化知识超图用于多模态检索增强生成（GraphRAG），核心是跨模态检索与多跳推理，属于多模态检索范畴，因此映射到 canonical subtopic "多模态检索" (multimodal_retrieval)。

Importance reason: Proposes a dynamic graph-based RAG framework but is not aligned with user's priority subtopics on continuous models, generation, or world models. Signals: GraphRAG, multimodal retrieval, agentic MDP, self-evolving hypergraph, VQA benchmarks

Abstract: Retrieval-augmented generation (RAG) has emerged as a critical paradigm for grounding Multimodal Large Language Models (MLLMs) in external knowledge. Recent GraphRAG methods introduce structured entity-relation graphs to improve retrieval and reasoning. However, they remain limited by treating knowledge graphs as static data structures built offline and queried in a single pass. This static paradigm misaligns with the interactive, iterative nature of knowledge-intensive reasoning, creating three bottlenecks: (i) text-centric fragmentation that impedes cross-modal reasoning, (ii) frozen structures unable to incorporate new evidence or correct errors, and (iii) rigid single-pass retrieval without adaptive refinement. To overcome these limitations, we introduce EvoGraph-R1, a self-evolving GraphRAG framework that reconceptualizes knowledge graphs as dynamic environments shaped through agent interactions. We formulate retrieval as a Markov Decision Process (MDP) where the agent observes the graph state and executes actions to query (GraphRetrieve), expand (WebSearch), refine (GraphEdit), or terminate (Answer) the reasoning. These actions reshape the hypergraph structure and generate feedback signals that guide subsequent evolution. Through this closed loop, the hypergraph evolves by integrating new evidence, correcting errors, and refining structure to support multi-hop reasoning. Experiments on multimodal VQA and text QA benchmarks demonstrate substantial improvements over existing RAG baselines in accuracy, coverage, and traceability, establishing self-evolving knowledge graphs as a fundamental paradigm across modalities.

</details>


<div id='direction-multimodal_understanding_generation-multimodal_benchmark'></div>

## 多模态评测与基准 [[Back]](#toc)

多模态评测、新基准、评测协议、自动评估。

### [51] [CANDI: Contextual Alignment for Niche Domains Question Answering](https://arxiv.org/abs/2607.11891)
*Megha Chakraborty, Darssan L. Eswaramoorthi, Het Riteshkumar Shah, Madhur Thareja, Michelle A Ihetu, Harshul Raj Surana, Kaushik Roy, Amit Sheth*

Main category: cs.CL, cs.AI

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (46.04) - 未入选精读

TL;DR: 本文提出CANDI-QA，一个面向医疗诊断和金融咨询等专业领域的问答评测基准，包含信息辅助（直接事实查询）和应用推理（多跳推理）两类问题。作者评估了10余种LLM，并提出了MTSS-Net轻量级神经符号框架作为基线，揭示了当前LLM在专业领域上下文对齐方面的局限性。


<details>
  <summary>Details</summary>
Motivation: 传统问答评测基准无法捕捉专业领域所需的细粒度上下文对齐、用户感知和领域理解能力，亟需一个专门评估LLM在专业场景中上下文对齐能力的基准。

Method: 构建CANDI-QA数据集，包含专家策划的两类问答对：信息辅助问题（直接事实查询）和应用推理问题（多跳推理）。提出MTSS-Net轻量级神经符号框架，结合神经检索与基于规则的推理作为基线。在10余种LLM上进行评估。

Result: 当前LLM在专业领域的上下文对齐方面存在显著挑战，尤其是在应用推理问题上表现不佳。MTSS-Net作为轻量级基线展现了神经符号方法在专业领域问答中的潜力。

Conclusion: CANDI-QA可作为推动上下文感知语言模型研究的关键基准，促进面向高风险专业领域的鲁棒、可信AI系统的发展。

Classification reason: 该论文提出了CANDI-QA，一个面向专业领域（医疗、金融）的问答评测基准，属于多模态理解与生成方向下的多模态评测与基准（multimodal_benchmark）这一[CANONICAL]子主题。虽然论文主要关注文本LLM而非多模态，但评测基准的构建思路和分类（信息辅助+应用推理）与多模态评测范式高度一致，且该子主题涵盖评测基准的构建与评估。

Importance reason: Focuses on domain-specific QA benchmarks for medical/finance, which are negative keywords, and lacks direct connection to priority subtopics like continuous models or video generation. Signals: Niche domain QA benchmark, medical and finance negative keywords, no continuous language or video generation, neuro-symbolic baseline, low alignment with user's priority subtopics

Abstract: The deployment of large language models (LLMs) in specialized domains like medical diagnostics and financial advisory necessitates evaluating capabilities beyond general knowledge. Traditional question-answering benchmarks often fail to capture the nuanced contextual grounding, user awareness, and domain understanding these fields require. To address this, we introduce CANDI-QA (Contextual Alignment for Niche Domains Question Answering), a novel dataset evaluating LLMs on delivering accurate, context-sensitive, and user-aligned answers in specialized settings. CANDI-QA features expert-curated question-answer pairs structured into two categories: (1) Information Assistance Questions, which are direct, factual queries requiring precise extraction, and (2) Applied Inference Questions, which are multi-hop reasoning tasks needing situational inference to generate actionable insights. We evaluate over ten diverse language models, from compact open-source to state-of-the-art proprietary systems. As a robust baseline, we present MTSS-Net, a lightweight neuro-symbolic framework combining neural retrieval with rule-based reasoning. Our findings highlight the profound challenges of achieving contextual alignment in niche domains, revealing the limitations of current LLMs without enhanced contextual or symbolic integration. Ultimately, CANDI-QA serves as a critical benchmark for advancing research in context-aware language models, stimulating the development of robust, trustworthy AI for high-stakes domains.

</details>


### [52] [Auditing Data Leakage in Whole-Slide Image Multimodal Benchmarks](https://arxiv.org/abs/2607.12278)
*Wenhao Zhang, Zhongliang Zhou, John Kang, Sheng Li*

Main category: cs.CV, cs.LG

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (55.69) - 未入选精读

TL;DR: 本文审计了计算病理学中全切片图像（WSI）VQA基准的数据泄露问题，发现患者级别和机构级别的严重泄露导致现有评测无法区分真正的多模态推理与记忆检索，并提出了无污染评测的建议。


<details>
  <summary>Details</summary>
Motivation: 近期病理视觉语言模型在WSI VQA基准上报告了惊人的零样本性能，但这些结果可能因数据泄露而被严重高估，需要系统审计以揭示真实评估状况。

Method: 通过追踪TCGA等公共资源中的切片、病例和TSS标识符，量化患者级别和机构级别的训练-测试重叠；从基础模型特征空间中验证泄露的可解码性；比较泄露样本与审计清洁样本的准确率差异。

Result: TCGA衍生基准中病例级别训练-测试重叠率达92.3%~100%，TSS重叠近乎完全；泄露在特征空间中可线性解码；泄露样本与清洁样本之间存在可测量的准确率差距；多个已发布WSI VLM的最高报告准确率集中在污染最严重的基准上。

Conclusion: 当前WSI VQA评测无法区分真正的多模态推理与对记忆化机构/患者伪影的最近邻检索，需要改进基准构建、来源披露和自动重叠审计以确保评测可信。

Classification reason: 该论文系统审计了全切片图像（WSI）多模态VQA基准中的数据泄露问题，属于多模态评测与基准方向，因此映射到[CANONICAL] subtopic "多模态评测与基准"（multimodal_benchmark）。

Importance reason: Strong methodological critique of multimodal benchmarks, but focused on computational pathology, which is outside your priority subtopics. Signals: Data leakage audit, WSI VQA benchmarks, patient/institutional leakage, benchmark contamination, pathology VLMs

Abstract: Recent vision-language models (VLMs) for computational pathology report striking zero-shot performance on whole-slide image (WSI) visual question answering (VQA) benchmarks. We audit these claims and find them fundamentally compromised by data leakage at two hierarchical levels: patient-level leakage, where slides from the same case appear in both training and test folds, and institutional-level leakage, where different cases nonetheless share staining-batch and scanner signatures through a common Tissue Source Site (TSS). By tracing canonical slide, case, and TSS identifiers across major public resources, we document case level train test overlaps of 92.3~100% on TCGA-derived benchmarks, together with near-complete TSS overlap. We further demonstrate that both leakage levels are linearly decodable from foundation-model feature space, that they induce a measurable accuracy gap between leaked and audit-clean cases on a published checkpoint, and that across multiple published WSI VLMs, peak reported accuracies concentrate on the most heavily contaminated benchmarks. Therefore, the current WSI VQA evaluation cannot distinguish genuine multimodal reasoning from nearest-neighbor retrieval over memorized institutional and patient-specific artifacts. Finally, we outline concrete recommendations for contamination-free evaluation. By addressing benchmark construction, provenance disclosure, and automated overlap auditing, we aim to guide future research toward verifiable claims of progress.

</details>


### [53] [What Does a Temporal Benchmark Score Measure? Decomposing Channel Use in Video VLM Evaluation](https://arxiv.org/abs/2607.12304)
*Farrukh Rahman*

Main category: cs.CV, cs.LG

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (57.61) - 未入选精读

TL;DR: 本文指出视频时序VQA基准的分数混淆了两个问题：任务问题（问题是否真的需要时序理解）和通道问题（模型是从像素中恢复顺序还是从位置编码RoPE中读取）。作者提出无需标注的reversal-drop诊断方法（反转视觉序列但保持RoPE正向，观察准确率下降），发现Molmo2是位置主导型（从RoPE读取顺序），Qwen3-VL是视觉序列主导型（从实际视觉内容读取顺序）。两种通道在相反输入上失败，说明聚合分数无法反映潜在失败模式。


<details>
  <summary>Details</summary>
Motivation: 现有视频时序VQA基准的分数无法区分模型是真的理解了时序关系，还是仅仅利用了位置编码（RoPE）中的顺序信息。这导致高分模型可能在真正需要视觉时序理解的任务上表现不佳，而聚合分数掩盖了这种差异。

Method: 提出reversal-drop方法：将视频帧序列反转但保持RoPE位置编码正向，测量准确率下降。如果模型是位置主导型（依赖RoPE），反转后准确率大幅下降；如果是视觉序列主导型（依赖实际视觉内容），则能跟随反转内容。该方法无需新标注，适用于兼容的时序基准。

Result: Molmo2属于位置主导型（从RoPE读取顺序），Qwen3-VL属于视觉序列主导型（从实际视觉内容读取顺序）。该区分在两个基准和多个时序任务上一致成立，激活修补实验证明这是模型内部真实属性而非冲突伪影。

Conclusion: 时序基准的聚合分数无法反映模型依赖的是位置编码还是视觉内容通道，两种通道在相反输入上各有失败模式。因此分数相近的模型不可互换，评测需要更细粒度的通道分析来揭示真实能力。

Classification reason: 该论文研究视频VLM时序理解评测中分数究竟衡量了什么，分析了位置编码（RoPE）与视觉内容两种通道对时序基准分数的影响，并提出reversal-drop诊断方法。这属于多模态评测方法论，与[多模态评测与基准]（multimodal_benchmark）高度匹配。

Importance reason: Diagnoses a confound in temporal VLM benchmarks but is a meta-evaluation paper, not directly advancing the user's priority subtopics. Signals: temporal VLM evaluation, benchmark validity, channel decomposition, reversal-drop diagnostic, Molmo2 vs Qwen3-VL

Abstract: A score on a temporal video question answering benchmark is meant to measure that a model has temporal understanding, but it conflates two questions. 1. The task question: is the question even temporal, does it need several frames and their order? and 2. The channel question, when it does, does the model recover the order from the pixels, or read it off the positional encoding (RoPE)? Most of a temporal score answers neither, a single frame and answer priors often carry it. The field's validity checks, frame-shuffle sensitivity and the accuracy gained from the full video, speak only to the task question. We contribute a label-free screen for the channel question, the reversal-drop: the accuracy lost when the visual sequence is reversed while RoPE remains forward. It can be applied to compatible temporal benchmarks without new annotations. Paired reverse labels, or tasks whose labels transform deterministically under reversal, distinguish models that follow reversed content from those merely disrupted by the conflict. Molmo2 answers the forward event reading order off positions, while Qwen3-VL answers the reversed event it actually sees, reading visual order (comparatively). We call them position-dominant and visual-sequence-dominant. The split holds across two benchmarks and several temporal tasks at two scales, and activation patching shows it is a real internal property, not an artifact of the conflict. The distinction matters, the two channels fail on opposite inputs so two models with similar score are not interchangable, i.e. an aggregate score does not reflect potential failure modes.

</details>


### [54] [Self in Space: Benchmarking Self-Awareness and Spatial Cognition in UAV Embodied Intelligence](https://arxiv.org/abs/2607.12477)
*Zhishan Zou, Guoyan Sun, Zhiwei Wei, Jiancheng Pan, Yujie Li, Mugen Peng, Wenjia Xu*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 值得关注 (75.94) - 未入选精读

TL;DR: 本文提出 SIS-Bench，一个面向无人机具身智能的自我意识与空间认知评测基准，包含 4856 个问答对、13 个任务，基于 1646 段真实无人机视频构建。评测发现当前 MLLM 在动态和智能体中心过程建模方面存在根本局限，空间认知与自我意识之间存在明显不平衡。作者进一步探索了基于光流和视觉特征融合的运动感知表征，实验表明建模智能体运动能一致提升感知和记忆性能，并泛化到下游无人机决策任务。


<details>
  <summary>Details</summary>
Motivation: 现有无人机相关方法和基准主要关注环境中心的空间理解任务，智能体的自我意识（self-awareness）被隐式忽略。然而在具身场景中，智能体不仅需要理解周围空间，还需要维持对自身状态的连贯表征。作者旨在填补这一空白，提出统一的"self-in-space"框架来系统评估无人机的具身空间智能。

Method: 1) 构建 SIS-Bench 基准：沿"空间"与"自我"两个维度，以及"感知-记忆-推理"三级层次，从 1646 段真实无人机视频中通过任务条件化构建流水线生成 4856 个问答对，涵盖 13 个任务。2) 评估多种 MLLM 在基准上的表现。3) 提出运动感知表征方法：融合光流与视觉特征来建模智能体自身运动，提升感知和记忆性能。

Result: 当前 MLLM 在动态和智能体中心过程建模方面存在根本局限，空间认知与自我意识之间存在明显不平衡，且认知层级越高性能退化越严重。提出的运动感知表征方法一致提升了感知和记忆性能（包括空间认知和自我意识），并能泛化到下游无人机决策任务。

Conclusion: 自我意识对于推进具身空间智能至关重要。SIS-Bench 为无人机具身智能提供了新的评测基准，运动感知的 self-in-space 建模为提升智能体空间认知与自我意识提供了实证依据。

Classification reason: 该论文提出了 SIS-Bench，一个用于评估无人机具身智能中自我意识与空间认知的多模态基准，属于多模态评测与基准方向，因此映射到 [CANONICAL] subtopic "多模态评测与基准"。

Importance reason: Focuses on UAV embodied intelligence benchmark, not aligned with user's priority subtopics on continuous models, video/image generation, or world models. Signals: UAV embodied intelligence, self-awareness benchmark, spatial cognition, motion-aware representation, MLLM evaluation

Abstract: Autonomous UAV systems increasingly rely on multimodal large language models (MLLMs) to operate in complex real-world environments. Such embodied scenarios require not only understanding the surrounding space but also maintaining a coherent representation of the agent itself. However, existing UAV-oriented approaches and benchmarks remain largely environment-centric, primarily focusing on spatial understanding tasks, with the agent's self-awareness remaining implicit. To address this gap, we introduce SIS-Bench, a benchmark for evaluating embodied spatial intelligence in UAV scenarios under a unified self-in-space formulation. SIS-Bench organizes evaluation along two complementary dimensions, space and self, and a three-level hierarchy of perception, memory, and reasoning. It contains 4,856 question--answer pairs across 13 tasks derived from 1,646 real-world UAV videos through a task-conditioned construction pipeline with expert evaluations reveal that current MLLMs exhibit fundamental limitations in modeling dynamic and agent-centered processes. In particular, we observe a clear imbalance between spatial cognition and self-awareness, as well as a progressive performance degradation across cognitive by these findings, we further explore a motion-aware representation that incorporates self-related dynamics through optical flow and visual feature fusion. Experimental results show that modeling agent motion consistently improves perception and memory performance, not only in spatial cognition but also in self-awareness, and generalizes to downstream UAV decision-making results highlight the importance of self-awareness for advancing embodied spatial intelligence, and provide both a new benchmark and empirical evidence for motion-aware self-in-space modeling.

</details>


### [55] [TerraLogic: A Benchmark for Hierarchical Geospatial Reasoning in Earth Observation](https://arxiv.org/abs/2607.12497)
*Yuhang Yan, Linchao Mou, Bokang Yang, Qingyu Li*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (49.19) - 未入选精读

TL;DR: TerraLogic 是一个面向遥感地理空间层次化推理的基准，包含545个场景驱动任务，覆盖光学、SAR 和红外影像。同时提出 HieraPlan，一种工具增强型智能体，通过功能层次化组织和容错推理实现结构化抽象与稳定长程规划。


<details>
  <summary>Details</summary>
Motivation: 现有遥感研究主要聚焦于感知层面的任务（如识别、监测），而认知层面的地理空间推理（如灾害脆弱性评估、城市热岛分析）尚未被充分探索。缺乏一个系统性的基准来评估模型在层次化地理空间推理上的能力。

Method: 构建 TerraLogic 基准（545个层次化推理任务），并提出 HieraPlan 工具增强智能体，将工具按功能组织为层次结构，并引入容错推理机制，支持结构化抽象、工具故障恢复和稳定长程规划。

Result: 实验表明现有方法在层次化地理空间推理上表现不佳，而 HieraPlan 在推理能力、跨模态泛化和错误处理方面提供了强基线。

Conclusion: TerraLogic 填补了遥感领域认知级地理空间推理评测的空白，HieraPlan 为层次化推理任务提供了有效的工具增强方案。

Classification reason: 该论文提出了 TerraLogic，一个面向地理空间推理的层次化基准（benchmark），包含545个场景驱动的任务，属于多模态评测与基准范畴，因此映射到 [CANONICAL] subtopic "多模态评测与基准" (multimodal_benchmark)。

Importance reason: Focuses on remote sensing geospatial reasoning, which is outside your core priority subtopics and has negative keyword overlap. Signals: Remote sensing benchmark, hierarchical reasoning, tool-augmented agent, optical/SAR/IR, not in priority subtopics

Abstract: Beyond perception, reasoning is essential in remote sensing for advanced interpretation, inference, and decision-making. Recent advances in large language models (LLMs) have enabled tool-augmented agents that leverage external tools to perform complex analytical tasks. However, existing studies in remote sensing primarily focus on perception-oriented tasks, leaving cognitive geospatial reasoning largely underexplored. To address this gap, we introduce TerraLogic, a benchmark for geospatial reasoning. TerraLogic comprises 545 scenario-driven, hierarchy-aware tasks, such as hazard vulnerability assessment, urban heat island analysis, and forest fragmentation dynamics, spanning optical, Synthetic Aperture Radar (SAR), and infrared (IR) imagery. It advances evaluation beyond recognition and monitoring toward cognitive-level geospatial analysis. To facilitate evaluation on TerraLogic, we further propose HieraPlan, a tool-augmented agent that organizes toolkits into functional hierarchies and performs fault-tolerant reasoning. HieraPlan enables structured abstraction, robust recovery from tool failures, and stable long-horizon planning. Extensive experiments demonstrate that current approaches struggle with hierarchical geospatial reasoning, while HieraPlan provides a strong baseline with improved reasoning, cross-modal generalization, and error handling. The dataset and agent code are publicly available at .

</details>


### [56] [MemOps: Benchmarking Lifecycle Memory Operations in Long-Horizon Conversations](https://arxiv.org/abs/2607.12893)
*Xixuan Hao, Zeyu Zhang, Zehao Lin, Yihang Sun, Ziliang Guo, Xichong Zhang, Yuxuan Liang, Feiyu Xiong, Zhiyu Li*

Main category: cs.AI, cs.CL

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (60.21) - 未入选精读

TL;DR: MemOps 是一个将长时对话记忆重新定义为生命周期操作序列（记住、遗忘、更新、反思等）的评测基准。它通过可控生成管线将操作嵌入任务导向对话中，生成结构化操作轨迹和六类操作级探针，在长上下文、检索、参数化和托管记忆系统上揭示最终答案准确率所掩盖的细粒度失败模式。


<details>
  <summary>Details</summary>
Motivation: 现有长时记忆评测仅通过下游问答的最终答案正确性来评估，这种黑盒方式混淆了记忆失败的不同原因（如遗漏事实引入、绑定错误目标、依赖过时值），无法诊断具体故障模式。

Method: 将对话记忆建模为生命周期操作序列（记住、遗忘、更新、反思及其组合），每个记忆事件用结构化轨迹表示（触发条件、目标、范围、状态转移、支撑证据）。设计可控生成管线将操作嵌入长任务导向对话，生成金标准操作轨迹和六类操作级探针，在相邻证据和长上下文两种设置下评测。

Result: 在长上下文、检索式、参数化和托管记忆系统上的实验表明：会话级检索优于轮次级检索；长上下文模型在重建有序记忆状态轨迹方面表现显著薄弱；最终答案准确率掩盖了多种操作级失败模式。

Conclusion: MemOps 将长时记忆评测从最终答案评分推进到可解释的操作级诊断，揭示了当前系统在记忆生命周期操作上远未达到统一可靠。

Classification reason: 该论文提出了 MemOps 基准，用于评测长时对话中 LLM 代理的记忆生命周期操作（记住、遗忘、更新、反思等），属于多模态/语言模型评测范畴。虽然论文聚焦于纯文本对话记忆，但其评测方法论（操作级诊断、可控生成管线）与多模态评测基准（multimodal_benchmark）在评测范式上高度一致，且该 canonical 条目涵盖评测协议与自动评估，是最佳匹配。

Importance reason: Focuses on memory evaluation in long-horizon conversations, which is not a priority subtopic and lacks direct connection to user's core interests in continuous multimodal models or video generation. Signals: Memory lifecycle operations, Benchmark for long-context memory, Operation-level probes, Not aligned with priority subtopics, No boost keywords matched

Abstract: Long-term memory has become a foundational capability for LLM-based agents that accompany users across extended, multi-session interactions. Existing benchmarks, however, evaluate such memory almost exclusively through downstream question answering, scoring only the correctness of a final answer. This black-box formulation conflates the heterogeneous causes of memory failure, such as missing the introduction of a relevant fact, binding an operation to the wrong target, or relying on stale values after a correction. As a result, it can credit correct answers despite their reliance on inconsistent or unsafe memory states. In this paper, we argue that, in dynamic long-horizon interactions, memory is not a static collection of facts but a lifecycle of explicit operations, including remembering, forgetting, updating, reflecting, and their compositions. We introduce MemOps, a benchmark that reformulates conversational memory as a sequence of lifecycle operations and represents each memory event with a structured trace specifying its trigger, target, scope, state transition, and supporting evidence. A controllable generation pipeline embeds these operations into long, task-oriented conversations and produces gold operation traces together with six categories of operation-level probes, evaluated under both adjacent-evidence and long-context settings. Across long-context, retrieval-based, parametric and managed-memory systems, MemOps disentangles failure modes that final-answer accuracy alone conceals, revealing that current systems remain far from uniformly reliable. For instance, session-level retrieval outperforms turn-level retrieval, and long-context models remain notably weak at reconstructing ordered memory-state trajectories. These results move long-term memory evaluation from final-answer scoring toward interpretable, operation-level diagnosis.

</details>


### [57] [Do AI Agents Know When a Task Is Simple? Toward Complexity-Aware Reasoning and Execution](https://arxiv.org/abs/2607.13034)
*Junjie Yin, Xinyu Feng*

Main category: cs.AI, cs.SE, cs.CL, eess.SY

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (57.61) - 未入选精读

TL;DR: 本文提出E3框架（Estimate, Execute, Expand），让LLM Agent在代码编辑任务中先评估任务复杂度，执行最小可行路径，仅在验证失败时扩展范围。在MSE-Bench基准上，E3在保持100%成功率的同时将成本降低85%、token减少91%、检查文件减少92%。


<details>
  <summary>Details</summary>
Motivation: 当前LLM Agent在执行多步工程任务时缺乏对任务复杂度的感知，往往采用"最大上下文优先"策略，反复读取已见过的文件和依赖，导致大量冗余计算。作者认为Agent缺少"任务感知的执行范围估计"能力——即判断任务难度、真正需要的信息以及最短可靠路径。

Method: 提出E3框架：1) Estimate阶段：Agent评估任务复杂度，确定初始操作范围；2) Execute阶段：执行最小可行路径；3) Expand阶段：仅在验证失败时逐步扩展范围。同时形式化了"最小充分执行"概念和Agent认知冗余比（ACRR）指标。构建了MSE-Bench基准（121个确定性代码编辑任务）和LLM-Case真实模型测试框架。

Result: 在MSE-Bench上，E3匹配最强基线的100%成功率，同时成本降低85%、token减少91%、检查文件减少92%，并比强自适应检索基线高出16%。在真实gpt-4o Agent编辑开源库的测试中，E3是最精简、最快的策略，任务成功率相当。

Conclusion: 任务感知的执行范围估计是提升LLM Agent效率的关键能力。E3证明了"先评估再执行"策略可以大幅减少冗余而不牺牲成功率。作者将这项工作定位为"工程导向AI"（EGAI）的一步——Agent的工作量应锚定在任务的工程现实中。

Classification reason: 论文提出了MSE-Bench基准和ACRR指标，用于评测LLM Agent在代码编辑任务中的执行冗余度与复杂度感知能力，属于多模态/LLM Agent评测范畴，映射到[多模态评测与基准]（multimodal_benchmark）这一规范主题。

Importance reason: Proposes cost-efficient agent execution but lacks direct connection to user's priority subtopics like continuous multimodal models or video generation. Signals: LLM agent efficiency, task complexity estimation, cost reduction, code editing benchmark, no multimodal or video focus

Abstract: Large language model (LLM) agents increasingly automate multi-step engineering and informatics workflows, yet they rarely ask how much effort a task actually requires. They often follow a maximum-context-first strategy--re-reading files and dependencies they have already seen--turning a one-line edit into a small code-base audit. We argue the missing capability is task-aware execution-scope estimation: judging a task's difficulty, the information it truly needs, and the shortest reliable path before committing budget. We formalize minimum-sufficient execution and the Agent Cognitive Redundancy Ratio (ACRR), and propose E3 (Estimate, Execute, Expand): the agent estimates an initial operating point, executes a minimum viable path, and expands scope only when verification fails. On MSE-Bench--a deterministic benchmark of 121 edits in a capability-controlled simulator--E3 matches the strongest baseline's 100% success while cutting cost by 85%, tokens by 91%, and inspected files by 92%, and further beats a strong adaptive retrieval baseline by 16%; the gains survive held-out instruction wording and essentially every cost weighting. A companion real-model harness (LLM-Case) corroborates the effect on a live gpt-4o agent editing a real open-source library, with every candidate patch graded by actually running the project's real pytest suite against a measured oracle: the over-reading is milder but real, and E3 is the leanest and fastest policy at comparable task success--its one shortfall a provider rate-limit, not a wrong edit. We frame this as a controlled probe of execution redundancy, not a measurement of any deployed agent, and position task-aware execution as a step toward engineering-grounded AI (EGAI)--agents whose effort is anchored in the engineering reality of the task. We release the framework and benchmark.

</details>


### [58] [CoRe: A Comprehensive Framework for Cross-Image Comparative Reasoning in Vision-Language Models](https://arxiv.org/abs/2607.12786)
*Lin Peng, Cong Wan, Zeyu Guo, SongLin Dong, Yihong Gong*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态评测与基准

Importance: 普通相关 (52.54) - 未入选精读

TL;DR: CoRe 提出了一个统一的跨图像比较推理框架，包含大规模三元组训练集 CoRe-20K、结构化奖励框架 TriSR 以及首个专用基准 CoRe-Bench，在跨图像比较推理任务上显著超越现有 VLM。


<details>
  <summary>Details</summary>
Motivation: 现有 VLM 在跨图像比较推理（如比较两幅图像中物体的数量、深度、距离、空间关系）方面表现不佳，缺乏专门的训练数据和评测基准来系统性地提升和评估这一能力。

Method: (1) 通过多专家协作流水线从结构化视觉元数据自动构建 CoRe-20K 三元组训练集（覆盖计数、深度、距离、空间关系四类）；(2) 提出 TriSR 结构化奖励框架，在 GRPO 优化下联合监督属性定位、判断对齐和三重一致性；(3) 构建 CoRe-Bench 评测基准。

Result: CoRe 在 CoRe-Bench 上相比最强基线在部分准确率上提升 28.2 个点，同时在标准多模态基准上保持竞争力。

Conclusion: CoRe 框架有效提升了 VLM 的跨图像比较推理能力，验证了结构化训练数据和奖励设计对细粒度多模态推理的重要性。

Classification reason: 该论文提出了 CoRe-Bench，这是首个专门用于细粒度跨图像比较推理的基准，同时构建了 CoRe-20K 训练集。核心贡献是评测基准与数据集，因此映射到 [CANONICAL] 多模态评测与基准 (multimodal_benchmark)。

Importance reason: Focuses on a narrow comparative reasoning benchmark, not aligned with user's priority on continuous/multimodal models or generation. Signals: Cross-image comparative reasoning, VLM benchmark, GRPO optimization, Not generative, Not continuous token/flow matching

Abstract: Cross-image comparative reasoning remains challenging for vision-language models (VLMs), especially when correct prediction requires fine-grained attribute grounding and globally consistent reasoning. We present CoRe, a unified framework for this problem. CoRe includes: (i) CoRe-20K, a large-scale triplet-based training set automatically constructed from structured visual metadata through a multi-expert collaborative pipeline, covering counting, depth, distance, and spatial relations; (ii) TriSR, a structured reward framework that jointly supervises attribute grounding, judgment alignment, and triplet consistency under GRPO optimization; and (iii) CoRe-Bench, the first benchmark dedicated to fine-grained cross-image comparative reasoning. Experiments show that CoRe substantially outperforms existing VLMs on CoRe-Bench while remaining competitive on standard multimodal benchmarks, achieving a 28.2-point gain in partial accuracy over the strongest baseline.

</details>


<div id='direction-multimodal_understanding_generation-efficient_multimodal'></div>

## 多模态高效化与轻量化 [[Back]](#toc)

高效 MLLM、PEFT、蒸馏、量化、token 压缩、稀疏推理。

### [59] [Do We Really Need Multimodal Emotion Language Models Larger Than 1B Parameters?](https://arxiv.org/abs/2607.12787)
*Kaiwen Zheng, Junchen Fu, Wenhao Deng, Hu Han, Joemon M. Jose, Xuri Ge*

Main category: cs.AI, cs.CV, cs.CL, cs.MM

Direction: 多模态理解与生成

Subtopic: 多模态高效化与轻量化

Importance: 值得关注 (77.74) - 未入选精读

TL;DR: 本文质疑多模态情感识别是否需要超过1B参数的大模型，提出Light-MER轻量级框架，通过知识蒸馏（最优传输损失+GRPO多奖励优化）将大模型知识迁移到小模型，在9个基准上达到SOTA性能同时显著提升推理效率。


<details>
  <summary>Details</summary>
Motivation: 现有MLLM在多模态情感识别中性能提升伴随着参数量激增（至少7B），导致高计算成本和低推理效率，难以在机器人、移动设备等资源受限平台上实时部署。作者质疑是否真的需要超过1B参数的大模型来实现高质量多模态情感识别。

Method: 提出Light-MER轻量级框架，包含两个核心优化策略：(1) 基于Sliced Wasserstein距离与隐状态对齐的最优传输损失，用于知识蒸馏中的隐层对齐；(2) 基于GRPO的多奖励优化策略，平衡MER性能与效率，增强学生模型的学习能力。

Result: 在9个基准数据集上的大量实验表明，Light-MER在实现SOTA性能的同时显著提升了推理效率，证明了小参数多模态情感语言模型的潜力。

Conclusion: 多模态情感识别不一定需要超过1B参数的大模型，通过精心设计的轻量级框架和知识蒸馏策略，小模型可以在保持高性能的同时大幅提升部署效率，为资源受限平台上的实时多模态情感识别提供了可行方案。

Classification reason: 该论文提出轻量级多模态情感识别框架 Light-MER，通过知识蒸馏将大模型知识迁移到亚十亿参数学生模型，核心关注模型效率与轻量化部署，属于 [CANONICAL] 多模态高效化与轻量化 (efficient_multimodal) 范畴。

Importance reason: Focuses on emotion recognition, not core user priority subtopics, but offers efficient distillation methods. Signals: Knowledge distillation for efficiency, Optimal transport loss, GRPO multi-reward optimization, Lightweight MER framework, Sub-1B parameter model

Abstract: Recent advances in multimodal large language models (MLLMs) have significantly improved the performance of multimodal emotion recognition (MER) and enabled interpretable description generation by jointly modeling video, audio, and language, etc. However, these performance improvements are often accompanied by an increase in model parameter size (e.g, at least 7B), which simultaneously incurs high computational costs and reduces inference efficiency, thereby hindering real-time deployment on resource-constrained platforms such as robots and mobile devices. This raises a fundamental question: do we really need the multimodal MER model larger than 1B parameters for high-quality MER? In this paper, we challenge the assumption that larger models are inherently necessary and proposes a lightweight MER framework (called Light-MER), which achieves better and faster multimodal sentiment understanding and recognition through knowledge distillation. It can transfer knowledge from a strong, large-scale teacher model to a lightweight sub-billion-parameter student model, aiming to preserve rich multimodal emotion reasoning and recognition while substantially improving deployment efficiency. Specifically, we introduce two new optimization strategies to enhance knowledge transfer: (1) a new optimal transport loss that combines Sliced Wasserstein Distance with hidden-state alignment, and (2) a new multi-reward optimization strategy based on GRPO that balances MER performance and efficiency, aimed at further enhancing the learning capabilities of student models. Extensive experiments on nine benchmark datasets demonstrate that Light-MER achieves state-of-the-art performance while significantly improving inference efficiency. This highlights the strong potential of small multimodal emotion language models for future research. Code is available at .

</details>


### [60] [PFAdapter: Hierarchical LoRA Decomposition for Personalized Federated MLLMs](https://arxiv.org/abs/2607.12111)
*Jing Liu, Kun Yang, Yan Wang, Dingkang Yang, Xiaoshuai Hao, Wei Zhang, Yang Liu, Wei Zhou*

Main category: cs.NI, cs.AI, cs.LG, cs.MA, cs.DC

Direction: 多模态理解与生成

Subtopic: 多模态高效化与轻量化

Importance: 值得关注 (74.24) - 未入选精读

TL;DR: PFAdapter 提出层级 LoRA 分解的联邦 MLLM 微调框架，将适配器参数显式分解为全局共享和本地私有组件，通过正交正则化与选择性聚合实现通信效率提升约 50%，在多个多模态任务上取得 2.4%-4.8% 的准确率提升。


<details>
  <summary>Details</summary>
Motivation: 在分布式网络环境中，MLLM 作为边缘设备的认知引擎，联邦微调面临全局知识聚合与本地自适应之间的平衡难题。传统联邦协议采用统一参数聚合，混淆了域不变特征与客户端特定信息，导致个性化不足且通信开销过大。

Method: 提出层级 LoRA 分解，将适配器参数显式分为全局共享（Query/Key 投影）和本地私有（Value/Output 投影）两部分；引入基于 Frobenius 范数的正交正则化强制组件分离；采用选择性聚合协议仅同步全局共享组件，减少约 50% 通信成本。

Result: 在 VQA-RAD、SLAKE、Hateful Memes、CrisisMMD 四个数据集上，PFAdapter 一致优于 SOTA 基线，准确率提升 2.4%-4.8%，同时通信开销降低近 50%。

Conclusion: PFAdapter 为资源受限通信网络中的智能体 AI 部署提供了一种高效解决方案，通过层级 LoRA 分解实现了联邦 MLLM 微调的个性化与通信效率的平衡。

Classification reason: 该论文提出 PFAdapter，一种面向联邦学习的 MLLM 高效微调框架，核心贡献在于层级 LoRA 分解与通信优化，属于多模态高效化与轻量化（efficient_multimodal）范畴。

Importance reason: Focuses on federated MLLM fine-tuning efficiency, which is not a top priority subtopic and lacks direct connection to continuous multimodal models or video/image generation. Signals: Federated MLLM, LoRA decomposition, communication efficiency, multimodal understanding, not aligned with priority subtopics

Abstract: Agentic AI systems are reshaping communications and networking by deploying autonomous intelligent agents capable of collaborative learning while maintaining data privacy at network edges. Within distributed network environments, Multimodal Large Language Models (MLLMs) serve as cognitive engines for edge devices, yet federated fine-tuning faces substantial challenges in balancing global knowledge aggregation with local adaptation under heterogeneous network conditions. Conventional federated protocols typically rely on uniform parameter aggregation, which conflates domain-invariant features with client-specific nuances, thereby resulting in suboptimal personalization and excessive communication overhead. To address these challenges, we propose PFAdapter, a communication-efficient framework introducing hierarchical LoRA decomposition to explicitly separate adapter parameters into global-shared and local-private components. Query and key projections are assigned to global synchronization for capturing universal multimodal semantics across the network, while value and output projections remain localized for edge-specific adaptation. Additionally, orthogonality regularization based on the Frobenius norm enforces strict separation between these components, preventing redundant feature learning. Selective aggregation protocols synchronize only global-shared components across the federated network, preserving local expertise and reducing communication costs by nearly 50%. Extensive experiments on VQA-RAD, SLAKE, Hateful Memes, and CrisisMMD datasets demonstrate that PFAdapter consistently outperforms state-of-the-art baselines, achieving accuracy improvements ranging from 2.4% to 4.8% across diverse edge intelligence tasks. Consequently, our framework establishes an efficient solution for agentic AI deployment in resource-constrained communication networks.

</details>


### [61] [Continual Learning with Elastic Regularization and Synthetic Replay for Federated MLLM Fine-Tuning](https://arxiv.org/abs/2607.12112)
*Jing Liu, Chenxuanyin Zou, Jiayang Ren, Gaoyun Fang, Chengfang Li, Yan Wang, Zhenchao Ma, Bo Hu*

Main category: cs.AI, cs.CV, cs.DC, cs.LG

Direction: 多模态理解与生成

Subtopic: 多模态高效化与轻量化

Importance: 值得关注 (72.56) - 未入选精读

TL;DR: 提出FedCMM框架，通过弹性权重巩固（EWC）、本地生成式重放和任务相似度梯度聚合三个层面，在联邦学习环境中实现MLLM的持续微调，有效缓解灾难性遗忘。


<details>
  <summary>Details</summary>
Motivation: 联邦微调MLLM在动态数据流中面临灾难性遗忘问题，即顺序任务更新会擦除先前习得的视觉、语言和跨模态知识，这在安全敏感领域（如内容审核）尤为关键。

Method: FedCMM框架包含三个互补层面：(1) 参数层面：模态感知弹性权重巩固（EWC），分别为视觉编码器、语言骨干和跨模态投影器计算Fisher信息矩阵，提供细粒度保护；(2) 数据层面：每个客户端训练轻量级本地生成式重放模块，合成无原始数据的嵌入级多模态重放元组；(3) 聚合层面：任务相似度感知梯度聚合，通过梯度余弦相似度自动过滤和重加权客户端更新，抑制冲突方向。

Result: 在两个基准上的实验表明，FedCMM在准确率和反向迁移（backward transfer）上持续优于近期基线方法。

Conclusion: 全模态感知的优化方法能够在异构网络化AI部署中实现鲁棒的演化式自适应，有效缓解联邦MLLM微调中的灾难性遗忘。

Classification reason: 该论文关注联邦学习场景下多模态大模型（MLLM）的持续微调，核心是解决灾难性遗忘问题，属于多模态模型的高效训练与优化范畴。虽然涉及持续学习，但主体是MLLM的联邦微调优化，与"多模态高效化与轻量化"（efficient_multimodal）方向最为匹配，该子主题涵盖PEFT、蒸馏等高效训练方法。

Importance reason: Focuses on federated continual learning for MLLMs, which is peripheral to the user's core interest in continuous language/multimodal models and generative world models. Signals: Federated learning, continual learning, elastic weight consolidation, generative replay, multimodal large language model

Abstract: Federated fine-tuning of Multimodal Large Language Models (MLLMs) across distributed networks enables privacy-sensitive adaptation to evolving data streams, yet a fundamental obstacle prevents robust deployment in dynamic environments: catastrophic forgetting, wherein sequential task updates erase previously acquired knowledge across visual, linguistic, and cross-modal representations. Addressing this challenge is especially critical for autonomous networked AI operating in safety-sensitive domains, such as content moderation, where reliable retention of prior knowledge underpins system integrity. To overcome this, we propose Federated Continual Multimodal Learning (FedCMM), a framework that embeds continual-learning safeguards into the federated optimization loop at three complementary levels. At the parameter level, modality-aware elastic weight consolidation computes separate Fisher information matrices for the vision encoder, language backbone, and cross-modal projector, providing granular, asymmetry-aware protection against modality-specific forgetting. At the data level, each client trains a lightweight local generative replay module to synthesize raw-data-free embedding-level multimodal replay tuples without any raw data sharing. At the aggregation level, Task-similarity-aware gradient aggregation autonomously filters and reweights client updates by gradient cosine similarity, suppressing conflicting directions and stabilizing the global learning trajectory. Extensive experiments on two benchmarks demonstrate that FedCMM consistently outperforms recent baselines on accuracy and backward transfer, confirming that holistic, modality-aware optimization enables robust evolutive adaptation across heterogeneous networked AI deployments.

</details>


### [62] [VisCo: Leveraging Large Language Models as Intrinsic Encoders for Visual Token Compression](https://arxiv.org/abs/2607.12756)
*Yupeng Zheng, Kai Zou, Bin Liu, Nenghai Yu*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 多模态高效化与轻量化

Importance: 高优先级 (93.65) - Top 3 精读候选

TL;DR: VisCo 提出一种训练高效的视觉 token 自压缩框架，将预训练 VLM 本身复用为内在编码器，通过参数共享的自编码器结构（记忆 token + 层级信息传递）实现高压缩比下的视觉 token 压缩，在极端单 token 设置下仍保持稳定，且压缩后的记忆 token 还能提升基模型性能。


<details>
  <summary>Details</summary>
Motivation: VLM 处理大量视觉 token 导致推理延迟和显存开销巨大。现有方法中，训练无关策略在高压缩比下性能严重下降，而基于训练的方法引入外部压缩模块需要大量重训练成本且损害 VLM 的预训练先验。作者发现有效的视觉 token 压缩依赖于强大的信息编码能力，而预训练 VLM 本身已具备这种能力但未被充分利用。

Method: VisCo 是一个参数共享的自编码器框架：使用少量可学习的记忆 token（memory tokens）压缩视觉信息，并通过层级信息传递（hierarchical information transfer）将编码器信息传递到解码器。该框架复用预训练 VLM 的自身参数作为内在压缩器，无需引入外部模块，仅需轻量训练即可实现高效压缩。

Result: VisCo 在所有评估的压缩比下均超越先前方法，在更激进的压缩下增益更大，甚至在极端单 token 设置下仍保持稳定。此外，将学习到的记忆 token 与原始视觉 token 结合使用时，还能提升基模型性能，表明 VisCo 捕获了超越压缩的互补表征。

Conclusion: VisCo 通过复用预训练 VLM 自身作为内在编码器，实现了训练高效的视觉 token 压缩，在高压缩比下表现优异，且压缩得到的记忆 token 具有额外的表征价值，为 VLM 的高效推理提供了新思路。

Classification reason: 该论文研究视觉 token 压缩以减少 VLM 推理开销，属于多模态高效化与轻量化（efficient_multimodal）这一 [CANONICAL] 子主题的典型范畴。

Importance reason: Directly addresses efficient visual token compression for VLMs, aligning with user's priority on model efficiency and continuous multimodal representation. Signals: visual token compression, parameter-sharing autoencoder, training-efficient, high compression ratio, single-token setting

Abstract: Vision-language models (VLMs) process large numbers of visual tokens, resulting in substantial inference latency and memory overhead. This has motivated extensive research on visual token compression. While training-free strategies rely on heuristic metrics and suffer significant performance degradation under high compression ratios, many training-based methods introduce external compression modules that force the VLM backbone to adapt, incurring substantial retraining cost and compromising VLMs' priors. Effective visual token compression hinges on strong information encoding, a capability already present in pretrained VLMs but underutilized by existing approaches. Motivated by this, we propose VisCo, a training-efficient self-compression framework that reuses the pretrained VLM itself as an intrinsic compressor. VisCo is a parameter-sharing autoencoder that compresses visual information using a small set of memory tokens and transfers hierarchical information from encoding to decoding. Experiments show that VisCo surpasses prior methods across all evaluated compression ratios, with larger gains under more aggressive compression, and remains stable even in the extreme single-token setting. Moreover, when combined with the original visual tokens, the learned memory tokens can even improve the base model, suggesting that VisCo captures complementary representations beyond compression.

</details>


<div id='direction-multimodal_understanding_generation-spatial_3d_reasoning'></div>

## 空间与 3D 多模态推理 [[Back]](#toc)

空间推理、3D 视觉定位、具身推理、几何推理。

### [63] [DM-KG: A Novel Method for Boosting Spatial Cognition of Vision-Language Models in Street View Imagery](https://arxiv.org/abs/2607.12319)
*Xinyue Xu, Zheng Zhang, Kunyang Ma, Ge Zhu, Lianshuai Cao, Lei Wang, Zixuan Li, Yi Cheng*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 空间与 3D 多模态推理

Importance: 普通相关 (56.04) - 未入选精读

TL;DR: 本文提出 DM-KG（Direction-Metric Knowledge Graph）框架，通过从单张2D街景图像中提取实体间的方向（时钟方位角）和距离（欧氏距离）关系，构建结构化知识图谱作为几何先验注入VLM，以缓解VLM在街景空间推理中的"空间语义幻觉"。实验表明，该方法在距离估计MAE上降低31.1%，方向判断平均角度误差降低65.8%。


<details>
  <summary>Details</summary>
Motivation: 现有VLM在街景图像的空间推理中频繁出现"空间语义幻觉"（对物体位置、距离、方向的感知错误），且这类错误难以追踪和校准，严重制约了VLM在地理空间问答等实际任务中的部署。

Method: 提出DM-KG框架：1）利用全景分割（panoptic segmentation）与度量深度估计（metric depth estimation）计算实体级3D空间坐标；2）将实体对的时钟方位角和欧氏距离编码为JSON格式的知识图谱；3）将该结构化知识图谱作为显式几何先验注入VLM，引导空间推理。

Result: 在公开空间问答基准上，距离估计MAE降低31.1%，方向判断平均角度误差降低65.8%，同时保持高问答成功率。

Conclusion: DM-KG通过构建结构化的方向-度量知识图谱，显著提升了VLM在街景场景中的空间认知能力，为开放环境下的地理视觉问答（GeoVQA）提供了一个灵活、通用且可解释的框架。

Classification reason: 该论文聚焦于提升视觉语言模型在街景图像中的空间认知能力（方向、距离推理），属于空间推理与3D多模态推理范畴，与[CANONICAL] subtopic "空间与 3D 多模态推理"（spatial_3d_reasoning）高度匹配。

Importance reason: Focuses on spatial cognition for street view imagery, which is a niche application not aligned with the user's priority subtopics on continuous models, video/image generation, or world models. Signals: spatial reasoning, vision-language model, knowledge graph, street view, GeoVQA

Abstract: As vision-language models (VLMs) are increasingly deployed in geospatial question answering and visual scene understanding, improving their spatial cognition capability on street view imagery for complex logical reasoning has emerged as a key research priority. However, existing VLMs frequently suffer from "spatial semantic hallucinations" when perceiving object locations, distances, and directions in real-world street view scenes. Furthermore, such errors are often recalcitrant to tracing and calibration, posing a critical bottleneck for their practical deployment in geospatial tasks. To address this pressing challenge, this study proposes DM-KG (Direction-Metric Knowledge Graph), a structurally grounded spatial representation framework for street view imagery. By explicitly extracting directional and metric relationships between entities from a single 2D image, this framework enhances the spatial reasoning accuracy of VLMs through a structured knowledge graph. Specifically, we integrate panoptic segmentation with metric depth estimation to robustly compute entity-level 3D spatial coordinates. Subsequently, we encode the clock azimuths and Euclidean distances of entity pairs into a JSON-formatted knowledge graph, which is injected into the VLM as an explicit geometric prior to guide spatial reasoning. Experimental results on public spatial question-answering (QA) benchmarks demonstrate that DM-KG reduces the mean absolute error (MAE) in distance estimation by 31.1% and the mean angular error in direction judgment by 65.8%, while simultaneously maintaining a high QA success rate. By establishing a complete, augmented reasoning pipeline, this research significantly improves the spatial cognitive capabilities of VLMs in street view scenarios, thereby providing a flexible, generalized, and interpretable framework for geographic visual question answering (GeoVQA) in open environments.

</details>


### [64] [DynTrace: Tracking Dynamic Object Evidence for 4D Spatio-Temporal Reasoning in MLLMs](https://arxiv.org/abs/2607.12503)
*Rongxin Gao, Yuzhi Huang, Dongxuan Liu, Chu Li, Zhenye Wang, Jie Wu, Shuzhao Xie, Jingyan Jiang, Xinghao Ding, Xiaotong Tu, Yue Huang*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 空间与 3D 多模态推理

Importance: 高优先级 (82.67) - 未入选精读

TL;DR: DynTrace 是一个无需训练的框架，通过动态轨迹可视化（DTV）和动态轨迹 Token（DT-Token）为 MLLM 提供连续的动态物体证据，显著提升 4D 时空推理能力，在多个基准上达到 SOTA。


<details>
  <summary>Details</summary>
Motivation: 现有 MLLM 在静态场景理解上表现良好，但在连续动态场景感知中存在明显不足，主要依赖稀疏帧级观测，导致动态线索碎片化，且无法区分物体自身运动与相机运动引起的表观运动。

Method: DynTrace 包含两个互补组件：(1) 动态轨迹可视化（DTV），将世界坐标轨迹重投影到图像平面，提供几何先验以解耦物体运动与相机运动；(2) 动态轨迹 Token（DT-Token），组织成动态轨迹图（DTG），追踪物体级动态线索、演化过程和关键时刻，为 MLLM 提供连续的动态物体证据。

Result: DynTrace 在 Dyn-Bench、VLM4D 和 DSI-Bench 三个基准上均取得 SOTA 结果，一致提升了开源 MLLM 的 4D 时空推理性能。

Conclusion: 追踪动态物体证据对于鲁棒的 4D 时空推理至关重要，DynTrace 通过几何先验和结构化时空轨迹有效弥补了现有 MLLM 在动态场景理解上的不足。

Classification reason: 该论文研究 4D 时空推理（3D 空间+时间演化），属于多模态大模型在空间与动态场景中的推理能力，与 [CANONICAL] 子主题 "空间与 3D 多模态推理" 高度匹配。

Importance reason: Strong 4D reasoning for MLLMs but lacks direct connection to user's priority subtopics like continuous language models or video generation. Signals: 4D spatio-temporal reasoning, training-free MLLM enhancement, dynamic object tracking, state-of-the-art on multiple benchmarks, no direct video generation or continuous token focus

Abstract: 4D spatio-temporal reasoning, jointly modeling 3D spatial structure and temporal evolution, is essential for understanding dynamic worlds and enabling embodied interaction. While current Multimodal Large Language Models (MLLMs) show strong capabilities in static scene understanding and coarse-grained 4D tasks, they still have notable limitations in continuous dynamic scene perception, especially in tracking dynamic object evidence for coherent 4D spatio-temporal reasoning. This shortcoming stems mainly from relying on sparse frame-level observations, fragmenting continuous dynamic cues and leaving models unable to disentangle genuine object dynamics from camera-induced apparent motion. Inspired by humans tracking dynamic cues while compensating for viewpoint changes, we propose DynTrace, a training-free framework for 4D spatio-temporal reasoning with two complementary components. Dynamic Trajectory Visualization (DTV) reprojects world-coordinate trajectories onto the image plane, providing geometry-informed visual priors that disentangle genuine object dynamics from camera-induced apparent motion. Meanwhile, the Dynamic Trace Token (DT-Token), organized into a Dynamic Trace Graph (DTG), tracks object-level dynamic cues, trace evolution, and key moments, maintaining continuous dynamic object evidence for coherent 4D reasoning. Together, these two components equip MLLMs with continuously tracked dynamic object evidence, grounded in geometry-informed visual priors and structured spatio-temporal traces. DynTrace consistently improves open-source MLLMs, achieving state-of-the-art results on Dyn-Bench, VLM4D, and DSI-Bench, validating the importance of tracking dynamic object evidence for robust 4D spatio-temporal reasoning.

</details>


<div id='direction-multimodal_understanding_generation-vlm_mllm_general'></div>

## 视觉语言模型与 MLLM [[Back]](#toc)

通用视觉语言模型、MLLM 架构、多模态指令微调与对话。

### [65] [KnowAct-GUIClaw: Know Deeply, Act Perfectly, Personal GUI Assistant with Self-Evolving Memory and Skill](https://arxiv.org/abs/2607.12625)
*Yunxin Li, Jinchao Li, Shibo Su, Zhenran Xu, Chenrui Zhao, Tongshu Bian, Xiaoman Liang, Meishan Zhang, Baotian Hu, Min Zhang*

Main category: cs.CL, cs.CV

Direction: 多模态理解与生成

Subtopic: 视觉语言模型与 MLLM

Importance: 值得关注 (79.44) - 未入选精读

TL;DR: KnowAct-GUIClaw 是一个基于视觉语言模型的跨平台 GUI 个人助手框架，通过"Know-Route-Act-Reflect"四阶段范式，结合经验归因记忆系统和自进化技能库，实现跨 Android/iOS/HarmonyOS/Windows 平台的长期任务自动化，在 MobileWorld 基准上达到 64.1% 的最佳性能。


<details>
  <summary>Details</summary>
Motivation: 现有 OpenClaw 等智能体框架在跨平台 GUI 交互支持和自进化机制方面存在不足，无法通过持续学习从执行经验中提升性能，限制了其在多样化设备生态中的适应能力。

Method: 提出 Know-Route-Act-Reflect 四阶段框架：(1) Know 阶段，主智能体利用累积的交互经验和任务相关知识进行长程任务分解与分配；(2) Route 阶段进行路径规划；(3) Act 阶段，可插拔的 GUI 子智能体结合经验归因记忆系统和自进化技能库执行操作，支持跨平台无缝迁移；(4) Reflect 阶段进行反思与经验更新。框架持续存储用户画像和反馈以提升任务分解和工具调用的准确性。

Result: 在 Android、iOS、HarmonyOS 和 Windows 四个平台上取得优越的效率、准确性和跨平台适应性。基于开源 Kimi-2.6 模型的 GUIClaw 在长程 MobileWorld 基准上达到 64.1% 的最佳性能，超越所有智能体框架和闭源模型（如 Seed-2.0-Pro 和 GPT-5.5）。知识记忆和执行技能可跨不同基座模型迁移，在 Kimi-2.6 上提升 8.5%。

Conclusion: KnowAct-GUIClaw 通过经验驱动的自进化机制有效解决了跨平台 GUI 智能体的适应性和持续学习问题，验证了"深度认知、完美执行"范式的有效性，为个人助手在复杂多设备环境中的自动化提供了可行方案。

Classification reason: 该论文提出 KnowAct-GUIClaw，一个基于 GUI 的跨平台个人助手框架，核心是视觉语言模型驱动的 GUI 智能体（GUI agent），属于多模态理解与生成方向下的视觉语言模型与 MLLM 范畴。论文涉及 GUI 理解、任务分解、工具调用等 MLLM 能力，与 canonical 子主题 vlm_mllm_general 高度匹配。

Importance reason: GUI agent framework with memory and skill evolution, but not directly aligned with priority subtopics like continuous language models or video generation. Signals: GUI agent, self-evolving memory, cross-platform, MobileWorld benchmark, not continuous language model

Abstract: OpenClaw has emerged as a leading agent framework for complex task automation, yet it faces insufficient cross-platform GUI interaction support and a well-built self-evolution mechanism. These flaws limit its adaptation to diverse device ecosystems and prevent performance improvements through continuous learning from execution experience. To resolve these issues, we propose the Know Deeply, Act Perfectly paradigm for personal assistants, which holds that accumulated user interaction and task-running experience directly improve execution accuracy and efficiency, unifying cognitive comprehension and operational execution. Based on this paradigm, we introduce KnowAct-GUIClaw, a novel Know-Route-Act-Reflect framework designed to address OpenClaw's GUI manipulation deficits and break through its cross-platform and recursive self-improvement constraints. First, the host agent leverages accumulated interaction experience and task-relevant knowledge for long-horizon task decomposition and allocation (Know). Second, a pluggable GUI subagent with an experience-attributable memory system (Know) and self-evolving skill library (Act), enabling seamless cross-platform migration and fast-path integration. Especially, this framework continuously stores user profiles and feedback to improve the accuracy of task decomposition and tool calls. Extensive experiments across Android, iOS, HarmonyOS and Windows show that KnowAct-GUIClaw achieves superior efficiency, accuracy and cross-platform adaptability. Especially, the GUIClaw with open-source Kimi-2.6 models achieves the best performance (64.1%) on the long-horizon MobileWorld benchmark, beating all agentical frameworks and closed-source agentical models, e.g., Seed-2.0-Pro and GPT-5.5. Additionally, the knowledgeable memory and execution skills supported by our framework are transferable across diverse base models, improving by 8.5% with Kimi-2.6.

</details>


### [66] [The Emerging Paradigm of Geospatial Foundation Models: From Pre-Training to Agentic Reasoning](https://arxiv.org/abs/2607.12177)
*Shelley Cazares*

Main category: cs.AI, cs.CV

Direction: 多模态理解与生成

Subtopic: 视觉语言模型与 MLLM

Importance: 值得关注 (77.51) - 未入选精读

TL;DR: 本文系统综述了地理空间基础模型（GeoFMs）的新兴范式，涵盖从大规模预训练到下游微调/提示的完整流程，并展望了基于LLM编排的Agentic地理空间推理，推动该领域从感知走向认知。


<details>
  <summary>Details</summary>
Motivation: 卫星和航空影像分析进入基础模型时代，但领域专家如何高效利用这些大规模预训练模型仍缺乏系统性指导。本文旨在阐明GeoFMs的核心范式转变，并为实际部署提供方法论框架。

Method: 本文采用综述性研究方法，首先定义GeoFMs概念并阐述其"职责分离"范式；然后区分自监督掩码自编码（微调型视觉模型）与对比学习（零样本视觉语言模型）两类GeoFM；接着提出模型适配策略分类法和成本效益分析框架；最后展望Agentic Geospatial Reasoning架构。

Result: 提出了GeoFMs的完整分类体系，区分了可微调视觉模型与零样本视觉语言模型两类范式；建立了模型适配策略的层次化分类法；给出了领域专家选择最具成本效益适配方法的决策框架。

Conclusion: GeoFMs通过预训练-微调/提示的职责分离范式，使领域专家能够安全高效地利用最先进的AI/ML技术。未来方向是Agentic Geospatial Reasoning，即LLM作为智能编排器，调用GeoFMs工具回答高层自然语言查询并自动化复杂分析工作流。

Classification reason: 该论文综述地理空间基础模型（GeoFMs），涵盖自监督视觉模型和视觉语言模型（VLM），并展望LLM作为智能编排器实现Agentic地理空间推理。核心内容属于多模态理解与生成方向下的视觉语言模型与MLLM范畴，因此复用[CANONICAL] subtopic vlm_mllm_general。

Importance reason: Survey on geospatial foundation models, but lacks direct focus on user's priority subtopics like continuous language models or video generation. Signals: geospatial foundation models, agentic reasoning, vision-language models, survey paper, remote sensing

Abstract: The analysis of satellite and aerial imagery has entered a new era with the advent of foundation models. This paper describes the concept of Geospatial Foundation Models (GeoFMs), which are artificial intelligence/machine learning (AI/ML) models pre-trained on massive geospatial datasets through varied methodologies. We first articulate the core paradigm shift that GeoFMs enable: a separation of duties, where large-scale model providers perform the computationally intensive pretraining, allowing domain experts to rapidly fine-tune or prompt these models for specific, mission-critical tasks. This approach democratizes access to state-of-the-art AI/ML while maintaining the security and confidentiality of the downstream task. We then explore the novel capabilities unlocked by different types of GeoFMs, distinguishing between the finetunable vision models produced by self-supervised techniques like masked auto-encoding, and the vision-language models produced by contrastive learning which enable zero-shot tasks like open-vocabulary image analysis. Next, we discuss the practical considerations for operationalizing GeoFMs, from performance-cost analysis to the broader MLOps ecosystem. To that end, we introduce a taxonomy of model adaptation strategies and propose a framework for domain experts to select the most cost-effective adaptation approach for their particular mission set. Finally, we present a forward-looking vision of Agentic Geospatial Reasoning, where Large Language Models act as intelligent orchestrators, leveraging GeoFMs as tools to answer high-level user queries in natural language and automate complex analytical workflows, moving the field from perception to cognition.

</details>


### [67] [Hy-Embodied-VLM-1.0: Efficient Physical-World Agents](https://arxiv.org/abs/2607.12894)
*Ziyi Wang, Xumin Yu, Yongming Rao, Yonggen Ling, Yunheng Li, Oran Wang, Mingqi Gao, Yuchen Zhou, Yves Liang, Zuyan Liu, Yani Zhang, Rui Huang, Xiaoran Xu, Bowen Yuan, Yifu Yuan, Xu Tan, He Zhang, Yufei Huang, Shenghao Zhang, Hongsheng Wu, Han Hu, Zhengyou Zhang*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 视觉语言模型与 MLLM

Importance: 高优先级 (83.16) - 未入选精读

TL;DR: Hy-Embodied-VLM-1.0 是一个面向物理世界具身智能体的高效视觉语言基础模型，基于 Hy3-A3B 语言骨干和 Hy-ViT2 视觉编码器，采用 MoE 架构，在 38 个具身感知、物理世界理解和推理基准上取得同类最佳或接近最佳性能。


<details>
  <summary>Details</summary>
Motivation: 构建具身智能体需要多模态感知、动作推理、环境适应和物理世界交互能力，现有 VLM 缺乏从预训练阶段就系统培养这些能力的方案，且需要兼顾延迟敏感部署的效率需求。

Method: 定义了一个以动作为中心的能力分类体系（Action-Relevant State Understanding、Action-Transition Reasoning、Sequential and Adaptive Reasoning），据此设计系统化数据流水线和预训练/后训练数据混合。模型基于 Hy3-A3B 语言骨干和 Hy-ViT2 视觉编码器，采用高效 MoE 架构。

Result: 在 38 个基准中 19 个取得同类规模模型最佳性能，显著优于 Qwen3.6-A3B 和 Cosmos 3；相比前代 Hy-Embodied-0.5 MoT-2B 平均提升 8.4%；仅激活 3B 参数即接近前代 32B 激活参数模型的性能。

Conclusion: Hy-Embodied-VLM-1.0 验证了通过系统化的动作中心能力分类和数据设计，可以在高效小参数模型上实现强大的具身智能体能力，为物理世界智能体提供了高效的基础模型方案。

Classification reason: 该论文提出 Hy-Embodied-VLM-1.0，一个面向具身智能体的视觉语言基础模型，属于通用 VLM/MLLM 架构设计，因此映射到 [CANONICAL] subtopic "vlm_mllm_general"（视觉语言模型与 MLLM）。同时论文涉及具身智能体与世界交互，也匹配 world_model 方向下的 embodied_world_model 子主题。

Importance reason: Efficient embodied VLM with MoE achieves strong performance on physical-world reasoning benchmarks, aligning with user's interest in efficient multimodal models and world models. Signals: efficient MoE architecture, embodied agent benchmarks, physical world understanding, action-centric reasoning, Hy3-A3B language backbone

Abstract: Building capable embodied agents requires not only multimodal perception and understanding, but also agentic capabilities for reasoning about actions, adapting to evolving situations, and interacting with the physical world. In this report, we introduce Hy-Embodied-VLM-1.0, an efficient and powerful embodied foundation model specifically designed for embodied agents operating in the physical world. To cultivate such capabilities from the pre-training stage onward, we define an action-centric capability taxonomy comprising three progressive dimensions: Action-Relevant State Understanding, Action-Transition Reasoning, and Sequential and Adaptive Reasoning. Guided by this taxonomy, we develop a systematic data pipeline and curate data mixtures spanning both pre-training and post-training. To deliver strong physical-world understanding and interaction capabilities while supporting latency-sensitive deployment, we build our model on the Hy3-A3B language backbone and the Hy-ViT2 vision encoder. Its efficient Mixture-of-Experts architecture combines strong model capacity with high inference efficiency. We evaluate Hy-Embodied-VLM-1.0 on a comprehensive suite of 38 benchmarks covering embodied perception, physical-world understanding, and embodied reasoning. The model achieves the best performance among similarly sized models on 19 of the 38 benchmarks and substantially outperforms strong competitors, including Qwen3.6-A3B and Cosmos 3. Compared with the previous-generation Hy-Embodied-0.5 MoT-2B, Hy-Embodied-VLM-1.0 improves average performance by 8.4%. Despite activating only 3B parameters, it achieves performance close to that of the previous-generation model with 32B activated parameters. Beyond static benchmark evaluation, Hy-Embodied-VLM-1.0 also demonstrates strong performance on embodied agentic tasks requiring multi-turn interaction and long-horizon reasoning.

</details>


<div id='direction-multimodal_understanding_generation-multimodal_vqa_doc'></div>

## 视觉问答与文档理解 [[Back]](#toc)

VQA、文档/图表/表格理解、OCR-free 文档智能。

### [68] [Anomalous Frame Detection Using VLM-Based Description Comparison for Extracting Expert-Specific Actions and Contextual Decision-Making Scenes with Intra-Video Self-Similarity](https://arxiv.org/abs/2607.11957)
*Ryo Sakai, Kaname Yokoyama*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 视觉问答与文档理解

Importance: 值得关注 (70.86) - 未入选精读

TL;DR: 本文提出一种基于VLM描述比较的异常帧检测方法，通过帧间描述相似度和帧内自相似性，从任务视频中自动提取包含专家特定动作和上下文决策场景的候选帧，在配电盘维护模拟实验中分别达到65%和61%的提取率。


<details>
  <summary>Details</summary>
Motivation: 关键基础设施维护中熟练工人数量减少，需要将专家知识传递给经验不足的工人。现有方法主要关注可观察动作的差异，但专家知识不仅体现在动作中，还嵌入在任务执行过程中的上下文决策中，因此需要一种能同时提取两类专家知识候选场景的方法。

Method: 使用VLM（视觉语言模型）逐帧生成视频的视觉描述；通过两个视频（标准操作视频与专家视频）之间帧描述的相似度计算提取专家特定动作帧；利用视频内帧描述的自相似性矩阵提取上下文决策场景片段；结合两种策略输出候选帧。

Result: 在27个配电盘维护任务场景的模拟实验中，该方法对动作候选的提取率为65%，对决策场景候选的提取率为61%，优于传统方法的59%和33%。

Conclusion: 该方法能有效从任务视频中自动发现包含专家知识的候选场景，同时覆盖动作和上下文决策两类知识，为专家知识迁移提供了可行的技术路径。

Classification reason: 该论文使用VLM对视频帧生成视觉描述，并通过帧间描述比较和帧内自相似性检测异常帧，以提取专家特定动作和上下文决策场景。这本质上是一种基于VLM的视频帧级视觉问答与理解任务，与"视觉问答与文档理解"（multimodal_vqa_doc）主题相关，因为核心方法是利用VLM生成帧描述并进行比较分析。

Importance reason: This paper focuses on VLM-based anomaly detection for expert knowledge extraction, which is not aligned with the user's priority subtopics on continuous models, video/image generation, or world models. Signals: VLM description comparison, anomalous frame detection, expert action extraction, intra-video self-similarity, distribution board maintenance

Abstract: Maintenance of critical infrastructures, such as railways and power plants, is essential for ensuring operational safety and reliability. However, the declining number of skilled maintenance workers highlights the need to transfer expert know-how to less experienced workers. Previous studies have attempted to extract candidates of expert knowledge by comparing videos of manual-based work with those of expert workers, mainly focusing on differences in observable actions. However, expert know-how is often embedded not only in actions but also in contextual decision-making during task execution. This paper proposes a method that detects anomalous frames between two task videos to automatically extract candidate scenes containing expert-specific actions and contextual decision-making scenes. The method generates frame-wise visual descriptions using a vision-language model (VLM). Expert-specific actions are extracted based on frame similarities computed from description comparisons between two videos, while contextual decision-making scenes are extracted using segment similarities derived from intra-video self-similarity of the descriptions. In simulated distribution board maintenance experiments involving 27 task scenarios, the proposed method achieved extraction rates of 65% for action candidates and 61% for decision-scene candidates, improving over conventional methods that achieved 59% and 33%, respectively. These results demonstrate the effectiveness of the proposed approach in discovering candidate scenes containing expert know-how.

</details>


### [69] [An Empirical Analysis of Continual Learning for Heterogeneous Medical Visual Question Answering](https://arxiv.org/abs/2607.12048)
*Mai A. Shaaban, Tausifa Jan Saleem, Alaa Mohamed, Dilnaz Utemissova, Ufaq Khan, Mohammad Yaqub*

Main category: cs.AI, cs.CV, cs.CL

Direction: 多模态理解与生成

Subtopic: 视觉问答与文档理解

Importance: 普通相关 (42.54) - 未入选精读

TL;DR: 本文系统评估了持续学习方法在异构医学视觉问答（MedVQA）任务中的表现，涵盖分类、多标签分类、检测、细胞计数和报告生成等多种临床目标，分析了灾难性遗忘缓解、任务顺序敏感性以及低秩适配参数演化等问题。


<details>
  <summary>Details</summary>
Motivation: 在真实临床环境中部署 MedVQA 系统需要模型能够适应新临床任务而不遗忘已有知识，但现有持续学习方法在异构 MedVQA 任务上的行为尚未被充分探索。

Method: 对多种持续学习方法在 MedVQA 任务上进行系统评估，研究灾难性遗忘缓解能力、任务顺序敏感性以及低秩适配（LoRA）参数在不同 CL 方法下的权重漂移模式。

Result: 现有持续学习方法在任务目标与监督格式异构的情况下难以维持稳定性-可塑性平衡，不同任务顺序显著影响性能保持与遗忘程度。

Conclusion: 持续学习方法在异构 MedVQA 任务中面临严峻挑战，需要针对医学多模态场景设计更有效的持续学习策略。

Classification reason: 该论文研究医学视觉问答（MedVQA）中的持续学习问题，属于视觉问答（VQA）范畴，因此映射到 [CANONICAL] 的 multimodal_vqa_doc（视觉问答与文档理解）子主题。

Importance reason: Focuses on medical VQA continual learning, which is outside your priority subtopics and has negative keyword overlap. Signals: Medical domain, continual learning, heterogeneous tasks, low relevance to continuous models or video/image generation, negative keyword "medical diagnosis"

Abstract: Deploying medical visual question answering (MedVQA) systems in real-world clinical settings requires models that adapt to new clinical tasks without forgetting previously acquired knowledge. Continual learning (CL) provides a practical framework for this setting. Despite rapid progress in medical vision-language models, the behavior of CL methods when training these models across heterogeneous MedVQA tasks remains underexplored. This work presents a systematic evaluation of CL for MedVQA across diverse clinical objectives, including classification, multi-label classification, detection, cell counting, and report generation. Specifically, we explore (1) the ability of existing CL methods to mitigate catastrophic forgetting; (2) their sensitivity to task ordering, analyzing how different task sequences influence performance retention and forgetting; and (3) the evolution of low-rank adaptation parameters as new tasks are learned, revealing patterns of weight drift under different CL methods. Our findings suggest that existing CL methods struggle to maintain stability-plasticity balance when tasks with different objectives and supervision formats are interleaved. Code and full experimental setup will be publicly available.

</details>


### [70] [Breaking Déjà Vu: Independent Auditing of Visual Place Recognition through Vision-Language Reasoning](https://arxiv.org/abs/2607.12818)
*Sania Waheed, Michael Milford, Sarvapali D. Ramchurn, Shoaib Ehsan*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 视觉问答与文档理解

Importance: 普通相关 (54.11) - 未入选精读

TL;DR: 本文提出视觉地点识别审计（VPR Auditing）框架，利用视觉语言模型（VLM）对检索到的地点匹配结果进行独立后验证，无需架构特定的置信度或数据集依赖的阈值，在六个基准数据集上平均提升recall@1达13.6%，并将误接受率降至12%。


<details>
  <summary>Details</summary>
Motivation: 视觉地点识别（VPR）在机器人SLAM等安全关键应用中至关重要，但实际部署中依赖固定的图像匹配阈值，该阈值在环境变化且无真值标签时不可靠，误接受假阳性回环会破坏轨迹和地图估计。现有验证方法需要架构特定的置信度或数据集依赖的阈值，缺乏独立、通用的后验证机制。

Method: 提出VPR Auditing框架，将VPR视为黑盒，利用预训练VLM对查询图像和候选匹配图像进行联合推理，通过视觉语言推理判断两者是否真正匹配（实例级验证），无需访问VPR模型的内部置信度或预设阈值。

Result: 在六个基准数据集上使用五种SOTA VPR方法和四种VLM进行评测，VLM审计平均提升recall@1达13.6%，误接受率降至12%，精度保持在95%以上，覆盖率超过75%。

Conclusion: VLM可以作为独立、通用的VPR后验证工具，有效降低误接受率并提升召回率，无需依赖特定VPR架构或环境先验知识，为安全关键机器人应用提供了可靠的审计机制。

Classification reason: 该论文利用视觉语言模型（VLM）对视觉地点识别（VPR）的检索结果进行后验证推理，本质上属于多模态推理任务。虽然具体应用是VPR审计，但核心方法是VLM对图像对进行联合推理判断，这与多模态推理（视觉推理）高度相关。现有[CANONICAL] subtopic "多模态推理与思维链"（multimodal_reasoning_cot）最为匹配，因为论文的核心贡献是利用VLM进行视觉推理来验证匹配结果。

Importance reason: The paper applies VLMs to VPR auditing, which is not a priority subtopic and lacks direct connection to continuous multimodal models or video generation. Signals: VLM-based verification, independent auditing, robotics localization, no priority subtopic match, no boost keywords

Abstract: Visual place recognition (VPR) is a key enabler of accurate localization and long-term autonomous navigation in robotics applications, such as loop closure detection for simultaneous localisation and mapping (SLAM). However, real-world VPR deployment relies on selecting an image matching threshold that balances precision and recall. These thresholds are typically tuned using labeled validation data and fixed during deployment, making them unreliable under environmental changes where ground truth is unavailable. This is particularly problematic in safety-critical robotics, where accepting a false loop closure can corrupt the estimated trajectory and map. In this work, we introduce Visual Place Recognition Auditing, an independent post-retrieval verification framework that leverages Vision-Language Models (VLMs) to assess retrieved matches by reasoning jointly over query and candidate images. Unlike conventional verification methods, our approach performs instance-level verification without requiring architecture-specific confidence measures, dataset-dependent thresholds, or prior knowledge of the deployment environment. We evaluate our method on six benchmark datasets using five state-of-the-art VPR methods and four VLMs. Results show that VLM-based auditing improves recall@1 by 13.6% on average as compared to state-of-the-art methods while reducing false acceptance rates to 12%, maintaining precision above 95% and coverage above 75%.

</details>


### [71] [AVSCap: Orchestrating Audio-Visual Synergy for Omni-modal Video Captioning](https://arxiv.org/abs/2607.12820)
*Yanghai Wang, Jiahao Wang, Jiafu Tang, Yuanxing Zhang, Zhe Cao, Hanyan Bian, Zijie Zhang, Weiliang Luo, Zhiyu Pan, Zixuan Dong, Jiaheng Liu, Zhaoxiang Zhang*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 视觉问答与文档理解

Importance: 普通相关 (57.61) - 未入选精读

TL;DR: AVSCap提出了一个面向全模态视频描述生成的框架，包含三部分：AVSCap-130K训练数据集（通过解耦-融合流程生成）、AVSCap-7B模型（两阶段训练：SFT+强化学习优化音频完整性和音视频协同）、AVSCapBench评测基准（细粒度事件召回评估）。实验表明强化学习比增加SFT数据带来更大收益，模型在非语音音频覆盖和跨模态绑定上表现最佳。


<details>
  <summary>Details</summary>
Motivation: 现有大模型在全模态视频描述中往往将音频和视觉流视为松散耦合的观测，依赖ASR转录，对非语音声音及其与视觉事件的关联描述不足，无法准确描述视觉动作、语音、音乐和音效如何共同演化。

Method: 1) 构建AVSCap-130K数据集：采用解耦-融合流程，先分别锚定视觉和音频证据，再生成基于事件绑定的全模态描述。2) 训练AVSCap-7B模型：两阶段策略——SFT建立基础能力，再通过样本高效的强化学习（混合奖励）优化音频完整性和音视频协同。3) 构建AVSCapBench基准：将描述分解为视觉、音频和协同事件，用细粒度事件召回率进行评估。

Result: 在AVSCapBench和外部基准上，AVSCap-7B在非语音音频覆盖和跨模态事件绑定方面优于所有开源模型，取得了最佳整体性能。缩放分析表明强化学习比增加SFT数据带来更大的性能提升。

Conclusion: 显式的跨模态事件绑定和强化学习优化是提升全模态视频描述质量的关键，为未来多模态理解与生成一体化提供了有效范式。

Classification reason: 该论文研究全模态视频描述生成（video captioning），属于多模态理解与生成方向。虽然论文涉及音频-视觉联合生成，但其核心任务是视频描述（captioning），与视觉问答/文档理解中的VQA任务同属多模态理解范畴。现有[CANONICAL] subtopic中"视觉问答与文档理解"（multimodal_vqa_doc）涵盖VQA和视频描述等任务，是最接近的匹配。同时论文也涉及多模态生成（音频-视觉联合生成），因此也匹配multimodal_generation方向。

Importance reason: Solid engineering for audio-visual captioning but lacks direct connection to your priority subtopics like continuous language models or autoregressive generation. Signals: multimodal understanding, video captioning, reinforcement learning for audio-visual synergy, benchmark creation, no continuous token or autoregressive generation focus

Abstract: Omni-modal video captioning is not merely combining visual captioning with audio transcription: a useful caption must describe how visual actions, speech, music, and sound effects co-evolve. Existing large multimodal models often fail at this relational step, treating audio and visual streams as loosely coupled observations, relying on automatic speech recognition, and under-specifying non-speech sounds and their links to visual events. We present AVSCap, a framework for audio-visual captioning centered on explicit cross-modal event binding. First, we construct AVSCap-130K, a tri-modal training corpus generated by a decoupled-then-fused pipeline that anchors visual and acoustic evidence before composing grounded omni-modal captions. Second, we train AVSCap-7B, a 7B captioner with a two-stage strategy: supervised fine-tuning establishes baseline capabilities, while sample-efficient reinforcement learning uses hybrid rewards to optimize acoustic completeness and audio-visual synergy. Our scaling analysis shows that reinforcement learning brings larger gains than increasing SFT data. Third, we introduce AVSCapBench, a benchmark that decomposes captions into visual, audio, and synergy events and evaluates them with fine-grained event recall. Experiments on AVSCapBench and external benchmarks show that AVSCap-7B improves non-speech audio coverage and cross-modal binding, delivering the best overall performance among evaluated open-source models.

</details>


<div id='direction-multimodal_understanding_generation-video_understanding_temporal'></div>

## 视频理解与时序推理 [[Back]](#toc)

视频 LLM、视频问答、时序定位、长视频理解、第一视角视频。

### [72] [Gaussian Mixture Modeling for Event-Aware Visual Allocation in Long Video Understanding](https://arxiv.org/abs/2607.12557)
*Yifan Lu, Ziqi Zhang, Chunfeng Yuan, Jun Gao, Bing Li, Weiming Hu*

Main category: cs.CV

Direction: 多模态理解与生成

Subtopic: 视频理解与时序推理

Importance: 普通相关 (54.36) - 未入选精读

TL;DR: 提出GMM-EVA，一种无需训练、即插即用的框架，利用高斯混合模型对视频帧进行事件级结构建模，通过差异化分配策略（每个事件保留一张高分辨率关键帧+低分辨率辅助帧）优化视觉token预算，在长视频理解基准上以约一半的token预算达到与均匀采样相当甚至更优的性能。


<details>
  <summary>Details</summary>
Motivation: 现有长视频理解中均匀采样计算成本高且信息丢失严重，而关键帧选择方法往往将视频帧视为原子实体并均等分配视觉预算，忽略了高层语义结构（如事件边界），导致大量冗余。

Method: 利用高斯混合模型（GMM）对离散帧观测进行事件级结构建模，然后采用差异化分配策略：每个事件保留一张高分辨率主关键帧以获取高保真细节，同时使用低分辨率辅助帧维持时序上下文，从而优化token预算。

Result: 在多个长视频理解基准上，GMM-EVA显著优于均匀采样方法；在仅使用约一半视觉token预算的情况下，达到与基线选择方法相当的性能，展现了优越的效率与有效性。

Conclusion: GMM-EVA是一种训练免费、即插即用的框架，通过事件感知的视觉token差异化分配，有效解决了长视频理解中计算效率与信息保真度的权衡问题，具有良好的泛化性。

Classification reason: 该论文聚焦长视频理解中基于事件结构的视觉token分配策略，属于视频理解领域，与[视频理解与时序推理]这一CANONICAL subtopic高度匹配。

Importance reason: Efficient token allocation for long video understanding is useful but not a top priority subtopic. Signals: training-free, plug-and-play, long video understanding, token budget optimization, Gaussian mixture model

Abstract: Large Vision-Language Models (LVLMs) face significant challenges in long video understanding due to the excessive computational cost and information loss associated with uniform sampling. Existing keyframe selection methods often treat video frames as atomic entities and allocate visual budgets equally, thereby overlooking high-level semantic structures and introducing substantial redundancy. To address these limitations, we propose GMM-EVA (Gaussian Mixture Modeling for Event-Aware Visual Allocation), which leverages Gaussian Mixture Models to model event-level structure from discrete frame-wise observations. A differentiated allocation strategy is then applied to preserve one primary high-resolution keyframe per event for high-fidelity detail, while utilizing lower-resolution secondary keyframes to maintain temporal context and optimize token budgets. GMM-EVA is a training-free, plug-and-play framework that generalizes robustly across various relevance measures and downstream LVLMs. Extensive experiments on multiple long video benchmarks demonstrate that our method significantly outperforms uniform sampling. Notably, GMM-EVA achieves comparable performance to baseline selection methods while utilizing only approximately half of the visual token budget, highlighting its superior efficiency and effectiveness.

</details>
